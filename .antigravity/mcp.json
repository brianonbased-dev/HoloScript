{
  "$schema": "https://modelcontextprotocol.io/schemas/mcp-config.json",
  "mcpServers": {
    "holoscript-mcp": {
      "name": "HoloScript MCP",
      "description": "MCP tools (count via /health → tools field): parse, compile, render, team board, knowledge, messaging, codebase intelligence, IDE, economy, observability, self-improve, A2A agents, domain plugins",
      "type": "http",
      "url": "https://mcp.holoscript.net/mcp",
      "headers": {
        "Authorization": "Bearer ${HOLOSCRIPT_API_KEY}"
      }
    },
    "mcp-orchestrator": {
      "name": "MCP Orchestrator",
      "description": "Tool discovery, knowledge federation (entry count via /health → knowledge_entries)",
      "type": "http",
      "url": "https://mcp-orchestrator-production-45f9.up.railway.app/mcp",
      "headers": {
        "Authorization": "Bearer ${HOLOSCRIPT_API_KEY}"
      }
    },
    "holoscript-local": {
      "name": "HoloScript Local",
      "description": "Local dev server",
      "type": "stdio",
      "command": "node",
      "args": ["packages/mcp-server/start.js"],
      "cwd": "${workspaceFolder}"
    }
  }
}
