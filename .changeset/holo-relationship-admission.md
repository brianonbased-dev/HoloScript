---
'@holoscript/core': patch
---

Require authored `.holo` `seeks` and selected-barrier `shields` relationships before the bounded
HSI causal loop can resolve or act, with deterministic fail-closed receipts for unbound queries.
