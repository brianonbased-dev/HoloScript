---
'@holoscript/cli': patch
'@holoscript/uaal': patch
---

Publish deterministic `.hs` compiler/UAAL execution provenance and an additive cross-format source-run receipt that explicitly distinguishes source re-execution from hash-anchored `.holo` and `.hsplus` evidence.
