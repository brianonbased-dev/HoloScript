---
'@holoscript/mcp-server': minor
---

Add an opt-in `parse_hs` projection for parser-admitted `.hsplus` `@unknown` struct fields,
including stable source identity and fail-closed HoloMeaning stage diagnostics.
