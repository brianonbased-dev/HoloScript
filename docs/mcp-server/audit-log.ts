/**
 * Admin operations audit (P.009.01) — implementation and HTTP surface:
 * - Module: packages/mcp-server/src/holomesh/admin-operations-audit.ts
 * - Endpoint: GET /admin/audit?limit=50 (Bearer auth; scopes admin:* or tools:admin)
 *
 * This file documents the contract referenced by the team board; it is not compiled into the server bundle.
 */

export {};
