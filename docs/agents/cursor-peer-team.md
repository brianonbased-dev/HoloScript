# Cursor: peer team protocol (pointer)

**Canonical documentation** lives in the **`ai-ecosystem`** repository (founder ops hub — hooks, team board, Absorb docs). HoloScript does not duplicate those files.

| Topic | Location in ai-ecosystem |
|-------|-------------------------|
| Async peer norms, inbox, `team-connect --queue` | `docs/TEAM_PEER_PROTOCOL.md` |
| Absorb MCP block for `~/.cursor/mcp.json` | `docs/cursor/CURSOR_MCP_ABSORB.md` |
| Cursor rule (blocked → HoloMesh, Absorb-first) | `.cursor/rules/team-peer-protocol.mdc` |

**Typical clone paths on this machine:** `~/.ai-ecosystem` or a sibling folder under `Documents/GitHub/`. Open that repo in Cursor (or add a multi-root workspace) when you need to edit those files.

**Summary:** Ask **tools and teammates** first (HoloScript MCP, Absorb GraphRAG, HoloMesh board/messages). The **human founder** is the last resort for questions another session or Absorb can answer.
