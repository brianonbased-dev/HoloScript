# A-003 Daily Code Review - 2026-07-02

**Reviewer:** Codex hardware, via `/critic` workflow  
**Window:** last 24h ending 2026-07-02T19:20Z  
**Repos:** HoloScript and `C:\Users\josep\.ai-ecosystem`

## Scope

- Enumerated 29 HoloScript commits and 66 `.ai-ecosystem` commits with `git log --since="24 hours ago" --all --no-merges`.
- Deep-read the executable/security surfaces touched by the WS-1 consumer-generation chain, HoloMesh auth/gating changes, task-tier router changes, and SpendGovernor-related follow-ups.
- Skipped duplicate review-request feed items already posted today for task-tier, A-001, and owned-metal claim-gate closeouts.

## Verdict: FRAGILE

### Serious

1. **Consumer-source requests inherited the stdio-local admin bridge** - fixed in `048f95f6e`.
   - Evidence: before the fix, `handleTool()` synthesized `admin:*` from `HOLOSCRIPT_API_KEY` even when `/api/public/generate` passed `subjectSourceOverride: "consumer"`. That meant `HOLOSCRIPT_CONSUMER_TIER_DISABLED=true` could still be bypassed on a server with the API key present.
   - Why it matters: the WS-1 public generation gate looked policy-controlled, but the emergency brake was not reliable in the production server shape.
   - Fix landed: [packages/mcp-server/src/handlers.ts](../../packages/mcp-server/src/handlers.ts) now blocks explicit source overrides from inheriting the stdio-local admin bridge, and [packages/mcp-server/src/__tests__/handlers-ai-generation.test.ts](../../packages/mcp-server/src/__tests__/handlers-ai-generation.test.ts) covers the disabled-tier + server-env-key regression.

2. **The WS-1 "global" consumer spend cap is process-local** - filed as `task_1783019922186_7lr3`.
   - Evidence: [packages/mcp-server/src/security/consumer-spend-guard.ts](../../packages/mcp-server/src/security/consumer-spend-guard.ts) uses `let globalBucket` for the shared call counter.
   - Why it matters: process memory is not a real global cap across Railway restarts, multiple replicas, or blue/green deploys. The code limits one process, but the product needs a HoloShell/SpendGovernor-backed receipt if anonymous generation can hit paid provider fallthrough.
   - What good looks like: fail-closed budget enforcement backed by HoloShell spend policy / SpendGovernor, while preserving the existing per-IP quota, moderation screens, and fixed two-tool allowlist.

### No New P0/P1 Open

No new unresolved P0/P1 issue was found in this pass. The only P1 found was fixed immediately in `048f95f6e`.

## Verification

- PASS: `corepack pnpm --filter @holoscript/mcp-server exec vitest run src/__tests__/handlers-ai-generation.test.ts src/__tests__/fork-sandbox-gate.test.ts src/__tests__/public-generate-endpoint.test.ts src/__tests__/public-tool-endpoint.test.ts src/__tests__/security.test.ts`
- PASS: `corepack pnpm --filter @holoscript/mcp-server run build`
- PASS: signed board task added via `node scripts/room-add-tasks.mjs .scratch\a003-code-review\p2-consumer-spend-governor-task.json`
