# PR 183 Triage - 2026-05-07

Task: `task_1778135229172_z1fb` (`[capability][github] Triage stale human PR #183`)

## Decision

Close PR #183 as stale and superseded. Do not merge or rebase it as-is.

The branch remains a recoverable archive at `origin/pr-183` /
`wip/local-scaffolds-2026-04-16`, but any still-desired ideas should be
refiled as fresh subsystem tasks rather than salvaged by cherry-picking the
April 16 bundle.

## Live PR State

Observed with `gh pr view 183 --repo brianonbased-dev/HoloScript` on
2026-05-07:

- PR: <https://github.com/brianonbased-dev/HoloScript/pull/183>
- Title: `Local scaffolds bundle + graph play preview follow-up`
- Branch: `wip/local-scaffolds-2026-04-16` into `main`
- State: open, non-draft
- Created: 2026-04-16
- Last updated: 2026-04-17
- Commits: 3 (`34ff1c9b4`, `cc9d31f81`, `a8fdb83c9`)
- Diff size: 42 files, +1888 / -396
- Checks: 15 failing, 2 passing, 4 skipped on the April 16 run

## Comparison Against Current Main

Fetched the PR head read-only:

```text
git fetch origin pull/183/head:refs/remotes/origin/pr-183
```

`git cherry -v origin/main origin/pr-183` shows all three PR commits are still
unique patch IDs, but the useful themes are no longer cleanly mergeable:

- Node graph execution bridge: current `main` already has
  `packages/studio/src/lib/nodeGraphExecutionBridge.ts` and
  `packages/studio/src/components/node-graph/NodeGraphPanel.tsx`; latest
  touched by `c2eb1f488` after the April branch.
- Core node graph preview: `packages/core/src/editor/nodeGraphPlayPreview.ts`
  already exists on `main`.
- GitHub API shared helpers: `packages/studio/src/app/api/github/_shared.ts`
  and the repo/file/pr/search/tree routes already exist on `main`; the PR adds
  only an untested `gist/route.ts` wrapper.
- Remaining PR-only scaffolds (`WorldSimulationBridge`, `FoundationDAOTrait`,
  `X402PaywallTrait`, `PlayModeController`) are not present on `main`.

## Why No Salvage Was Applied

The remaining unique scaffolds fail current quality gates and should be rebuilt
fresh if still wanted:

- `git diff --check origin/main...origin/pr-183` reports trailing whitespace in
  docs, tests, editor code, world bridge tests, x402 service code, gist route,
  and UAAL tests.
- `FoundationDAOTrait.ts` emits proposal IDs with `Date.now()` plus
  `Math.random()` and uses `any` payloads/handler storage. That conflicts with
  current deterministic-compiler and strict-typing discipline.
- `X402PaywallTrait.ts` relies on repeated `as any` casts for material/node
  mutation and has no current test coverage.
- `WorldSimulationBridge.ts` is a thin event shim with no current consumer on
  `main`; it should be reintroduced only through the current world-generation
  architecture and tests.
- The PR branch predates weeks of mainline CI, vocabulary, mesh, studio, and
  docs changes. Rebase would be a new engineering task, not a hygiene merge.

## Disposition

Close PR #183 with evidence. Do not delete the branch. If the product still
wants any of the archived ideas, file new board tasks with narrow scopes:

- `studio/github`: tested gist publishing route using current GitHub helpers.
- `core/traits`: deterministic governance/paywall trait vocabulary and tests.
- `core/world`: world-generation bridge aligned to current `WorldGenerator*`
  modules and current event contracts.

## Verification Commands

```text
gh pr view 183 --repo brianonbased-dev/HoloScript --json number,title,state,author,headRefName,baseRefName,createdAt,updatedAt,commits,additions,deletions,changedFiles,url
gh pr checks 183 --repo brianonbased-dev/HoloScript
gh pr diff 183 --repo brianonbased-dev/HoloScript --name-only
git fetch origin pull/183/head:refs/remotes/origin/pr-183
git cherry -v origin/main origin/pr-183
git diff --stat origin/main...origin/pr-183
git diff --check origin/main...origin/pr-183
git grep -n "Math.random\|Date.now\|: any\| as any" origin/pr-183 -- packages/core/src/traits/economics packages/core/src/world packages/studio/src/app/api/github/gist/route.ts
```
