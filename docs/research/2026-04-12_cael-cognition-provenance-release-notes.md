# CAEL Cognition + Provenance Release Notes (2026-04-12)

## Scope

This note captures the April 12, 2026 release arc around CAEL cognition, provenance envelopes, and contracted sandbox execution.

## Grounded changes (from git history)

- `6df32b4a` — defaulted CAEL Phase 2 cognition path to `SNNCognitionEngine`.
- `2b8038d6` — aligned CAEL loop with async cognition (`think`/`tick` flow).
- `a1525e4a` — added MCP absorb provenance answer envelope routing.
- `0b905104` — added contracted sandbox execution with CAEL trace metadata.
- `74f24a3d` — added `CRDTCAELBridge` for spatial/world-state merges into CAEL provenance events.
- `5c0f988f` — introduced `SNNCognitionEngine` backed by `@holoscript/snn-webgpu` LIF simulation.

## Verification evidence captured in this session

- Simulation tests passed after async cognition/default-path migration:
  - `SNNCognitionEngine.test.ts`
  - `CAELPhase2Embodied.test.ts`
  - `CRDTCAELBridge.test.ts`
  - Aggregate run: [see NUMBERS.md]  passed.

- Targeted MCP + sandbox tests passed for provenance/contracted execution paths:
  - `absorb-provenance-tools.test.ts`
  - `contracted-sandbox.test.ts`

## Key technical outcome

The CAEL pipeline now records cognition and action loops using the engine-backed SNN path by default, while preserving deterministic CPU fallback behavior when WebGPU is unavailable. Provenance output for MCP answers and sandboxed execution is now explicitly envelope-backed and test-covered.

## Open follow-up

- Run release guard and publish checks from a clean working tree to produce final release go/no-go.
- Keep package-level changelogs generated by changesets in sync with top-level `CHANGELOG.md` unreleased entries.
