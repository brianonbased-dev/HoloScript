# Example Health Report — 2026-05-02

Routine: A-009 Example Freshness + Artist Stress-Test  
Fire: 2026-05-02T00:00:00Z  
Previous parser drift clusters (memory-atoms-showcase, nft-marketplace-basic/advanced, product-viewer, site, world-builder): **all now PASS** — drift fixed since 2026-05-01.

## Phase 1 — Health Check

- Core build: **PASS** (tsup + generate-types, 33.7s)
- Spot-check: **80 / 80** `.holo` files pass `HoloScriptPlusParser` (30 targeted + 50 random)
- Previously-failing examples (6): all clear — no new drift class introduced

## Phase 2 — Refresh

Git-based staleness detection is not effective (all examples share initial mega-commit `e8551327`); using A-009 refresh history as freshness proxy instead.

### Refreshed: `examples/interaction/physics-interaction-demo`

- Source: `physics-interaction-demo.holo` (5 traits, v1.0)
- Output: `physics-interaction-demo.refreshed.holo`
- New traits exercised:
  - `@vfx(trigger: "on_break", effect: "spark_burst", ...)` — particle burst on each shatter event
  - `@interactive_graph(metrics: [...], graph_style: "line", ...)` — live stat board showing throw velocity and accuracy curves
  - `@speech(events: {...}, tts_provider: "edge_tts", ...)` — spatial announcer voice calling milestones
- Composition improvements: zone-specific accent lights (orange/teal/crimson), PBR materials, chromatic ACES grading
- Parser: **PASS**

**Missing-trait wish captured for Phase 4:**  
While composing, needed `@vfx_particle_sparks` and `@vfx_particle_smoke` as independent composable traits (e.g., apply smoke to a grabbed ball without requiring `@breakable`). Constants `vfx-audio.ts` defines these as string identifiers but they have no `VRTraitName` entry — only the blanket `@vfx` exists. Filed as Phase 4 seed (dedup_key: `a009-trait-vfx_particle_subtypes`).

## Phase 3 — Stress-Test

### New: `examples/stress-tests/sunken-archive-holomap-speech-2026-05-02.holo`

Visual story: Underwater archaeological scan — a research drone orbits sunken marble tablets 12m below the Aegean surface, reconstructing them into a live Gaussian splat model while a researcher AI narrates discoveries.

**Zero-coverage traits exercised for the first time:**

| Trait | Test aspect |
|---|---|
| `@holomap_reconstruct` | `session_id`, `input_mode`, `reconstruction_method`, `output_format` fields |
| `@holomap_camera_trajectory` | `path_mode: "orbital"`, `orbit_radius`, `altitude_oscillation`, `overlap_pct` |
| `@holomap_anchor_context` | `anchor_role` enum, `position_accuracy_m`, `rtk_gps` nested config |
| `@holomap_drift_correction` | `correction_strategy: "incremental_icp"`, `correction_hz`, `reference_anchors` array |
| `@holomap_splat_output` | `output_mode`, `lod_levels` array, `confidence_color_map` |
| `@speech` | `events` map with variable substitution syntax, `reverb` nested config |

Well-exercised anchors: `@glowing` `@collidable` `@networked` `@grabbable` `@physics`  
Parser: **PASS** — all 6 zero-coverage traits compile in combination

## Phase 4 — Seeds Emitted

1 seed emitted (vfx particle sub-type feature request):

```json
{
  "dedup_key": "a009-trait-vfx_particle_subtypes",
  "title": "Add @vfx_particle_sparks / @vfx_particle_smoke as first-class VRTraitNames (A-009 example-driven request)",
  "priority": "low"
}
```

File: `research/audit-reports/gaps-pending/A-009-2026-05-02T000000Z.json`

## Summary

- `EXAMPLE_HEALTH=ok` — health pass, 1 refresh, stress-test pass
- `A009_SEEDS_EMITTED=1`
- Previously-reported drift clusters: resolved (no new class)
- New zero-coverage traits exercised: 6 (all 5 holomap_* + speech)
- Remaining zero-coverage traits: `draft`, `emoji`, `hot_reload`, `video`, `interactive_graph` (now partially covered in refresh, but not yet in a dedicated stress-test)
