# Examples Health Report — 2026-05-25

Generated by: A-009 Example Freshness + Artist Stress-Test

## Summary

| Status | Count |
|--------|-------|
| Parsed OK | ~20 |
| Parse failures | 5 |
| Net new stress-test scenes | 1 |
| Refreshed examples | 2 |

## Parse Failures (drift caught)

### examples/dashboard.hs

**Error:** `Line 399:7: Unexpected token in array: punctuation` / `Line 399:37: Unexpected token in array: newline`

**Cause:** Array literal on line 399 contains inline object literals with punctuation separators that the parser rejects. Likely a comma vs. newline separator style mismatch introduced by a recent parser grammar change.

**Minimal repro area:**
```
// Around line 399 — thresholds array with inline objects
thresholds: [
  { max: 1, color: "#00ff66" },
  { max: 5, color: "#ffaa00" },
  ...
]
```

**Action needed:** Investigate if `{ ... }` inline objects inside array literals require newline-only separation in current parser grammar. Fix or add regression test.

---

### examples/site.holo

**Error:** `Line 177:1: Expected RBRACE, got EOF (in composition)`

**Cause:** Brace balance is 37/37 (balanced), so no raw unclosed block — the parser is likely consuming more input than expected inside a nested construct before line 177. Could be an unmatched `@tailwind("...")` string or a nested construct that the parser's composition rule advances past the final `}`.

**Action needed:** Investigate if multi-trait inline composition (`@panel ... @tailwind(...)`) inside a top-level object causes the parser to miscount the nesting depth.

---

### examples/memory-atoms-showcase.holo

**Error:** `Line 145:20: Expected COLON, got COMMA (in composition)`

**Cause:** Inside a `state { ... }` block, `mode: "review",` at line 145 — the trailing comma after the string value triggers an unexpected COLON expectation.

**Action needed:** Clarify whether `state { }` blocks support comma-separated assignments or require newline termination.

---

### examples/ai-agent.hs

**Error:** 19 errors including `Line 0:0: Too many blocks in program`

**Cause:** Rich 370+ line example may be hitting internal block count limit introduced in a recent parser hardening pass. Also: string-quoted object names triggering identifier-vs-string parse regression.

**Action needed:** Investigate `MAX_BLOCKS` parser limit for large `.hs` files.

---

### examples/product-viewer.holo

**Error:** 9 errors at `Line 147:8: Expected COLON, got STRING (in composition > domain-dataviz)`

**Cause:** The `domain-dataviz` sub-composition uses `key = value` assignment style instead of `key: value` property format.

**Action needed:** Migrate `=` assignments to `:` property format in domain-dataviz block.

---

## Refreshed Examples (Phase 2)

- `examples/vr-meeting-room.refreshed.holo` — adds `@networked`, `@advanced_pbr` (walnut clearcoat table, parquet floor, velvet sheen chairs), `@spatial_audio_cue` (screen activation + HVAC tone), `@subtitle` (live captions). Missing-trait wish: `@gaze_focus_analytics`.
- `examples/multiplayer-game.refreshed.hsplus` — adds `@lobby` (skill matchmaking — first use), `@networked_avatar` (skeletal IK avatars at spawns — first use), `@haptic` (ball grab + goal rumble), `@state_machine` (game phase FSM), `@particle` (goal burst).

## Stress-Test Results (Phase 3)

`examples/stress-tests/cartographer-genesis-worldgen-sse-2026-05-25.holo`
- Exercised: `@world_generator` (sovereign-3d, first use in any example), `@screen_space_effects` (SSR+SSAO+TAA+DoF+film grain, first use)
- Combined with: `@glowing`, `@animated`, `@particle`, `@physics`, `@bloom_reactive`, `@collidable`, `@grabbable`
- Both new traits parsed clean — no compiler bug found
- Scene: obsidian chamber, floating generated volcanic islands, data-dust vortex, physics-shatterable basalt

## Seeds Emitted (Phase 4)

See `research/audit-reports/gaps-pending/A-009-2026-05-25T220000Z.json`
- `@gaze_focus_analytics`: XR per-object eye-tracking dwell heatmap for meeting analytics
