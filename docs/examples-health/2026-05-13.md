# Examples Health — 2026-05-13

Routine: A-009 Example freshness + artist stress-test  
Core build: **PASS** (after dependency install)  
Parse sweep: **31 pass / 9 fail**

---

## Failed files (parse drift)

| File | First error |
|------|-------------|
| `examples/ai-agent.hs` | Line 266:17 — Expected identifier, got punctuation |
| `examples/dashboard.hs` | Line 398:36 — Unexpected token in array: newline |
| `examples/neural-network.hs` | Line 707:17 — Expected identifier, got punctuation |
| `examples/memory-atoms-showcase.holo` | Line 145:20 — Expected COLON, got COMMA (in composition) |
| `examples/nft-marketplace-advanced.holo` | Line 116:4 — Expected identifier, got RBRACKET (in implicit-composition > domain-web3) |
| `examples/nft-marketplace-basic.holo` | Line 86:4 — Expected identifier, got RBRACKET (in implicit-composition > domain-web3) |
| `examples/product-viewer.holo` | Line 152:5 — Expected LBRACE, got IDENTIFIER (in composition > domain-dataviz) |
| `examples/site.holo` | Line 177:1 — Expected RBRACE, got EOF (in composition) |
| `examples/world-builder.holo` | Line 138:73 — Expected value, got NEWLINE (in composition > domain-dataviz) |

## Error patterns

- **Punctuation-as-identifier** (`ai-agent.hs`, `neural-network.hs`): parser rejects
  advanced expression syntax (likely arrow functions or spread operators in object
  values) that the examples use but the current grammar doesn't recognize.
- **Array newline sensitivity** (`dashboard.hs`, `world-builder.holo`): multi-line
  array literals with trailing newlines cause unexpected-token failures. Parser
  appears to require single-line arrays in some contexts.
- **RBRACKET in domain-web3 block** (`nft-marketplace-*.holo`): nested array close
  inside a `domain-web3` domain block confuses the parser.
- **EOF without close brace** (`site.holo`, `world-builder.holo`): file appears
  truncated or has an unclosed composition block.
- **COMMA in composition object** (`memory-atoms-showcase.holo`): key-value pair
  separation uses comma inside composition block where the grammar expects newlines.

## Phase 2 refreshed this session

- `examples/quantum-arena.refreshed.hs` — **PASS** (1 lint warning, no parse error)

## Phase 3 stress-test this session

- `examples/stress-tests/glass-oracle-holographic-sprite-screen-space-effects-2026-05-13.holo` — **PASS**

## Recommended fixes

1. Update parser grammar to accept multi-line array literals (addresses `dashboard.hs`,
   `world-builder.holo`).
2. Fix `site.holo` and `world-builder.holo` missing closing braces (content drift /
   truncation).
3. Investigate punctuation-as-identifier: likely `@hoverable: { label: "...", detail:
   "..." }` nested string values containing punctuation — parser may be mis-tokenizing
   the strings.
