# Examples Health Report — 2026-06-01

Generated by A-009 (Example freshness + artist stress-test routine).

## Summary

| Category | Count |
|---|---|
| Top-level examples (pass) | 35 |
| Top-level examples (fail) | 11 |
| AR Foundation examples (fail) | 7 |
| Stress-test examples (fail) | 4 |
| Cross-reality examples (fail / OOM) | 2 |
| **Total failures** | **24** |

Core build: **PASS** (`@holoscript/core` 8.0.0 builds cleanly after `pnpm install`).

## Failing Examples

### Top-Level (examples/*.hs / *.hsplus / *.holo)

| File | Errors | First Error |
|---|---|---|
| `examples/ai-agent.hs` | 19 | Line 75:10 — Expected identifier, got string |
| `examples/dashboard.hs` | 28 | Line 128:18 — Unexpected token in array: newline |
| `examples/neural-network.hs` | 6 | Line 84:13 — Expected identifier, got punctuation |
| `examples/brittney-avatar.holo` | 7 | Line 87:8 — Expected RBRACKET, got LBRACE (composition) |
| `examples/memory-atoms-showcase.holo` | 12 | Line 64:29 — Expected identifier, got COMMA (composition) |
| `examples/nft-marketplace-advanced.holo` | 16 | Line 18:4 — Expected RBRACKET, got LBRACE (implicit-composition > domain-web3) |
| `examples/nft-marketplace-basic.holo` | 10 | Line 18:4 — Expected RBRACKET, got LBRACE (implicit-composition > domain-web3) |
| `examples/platform-conditional-minimal.holo` | 18 | Line 1:20 — `@platform()`: empty platform list |
| `examples/product-viewer.holo` | 9 | Line N/A — Validation errors |
| `examples/site.holo` | 65 | Line 12:3 — Expected LBRACE, got OBJECT (composition) |
| `examples/world-builder.holo` | 10 | Line N/A — Validation errors |

### AR Foundation (examples/ar-foundation/*.holo)

All 7 AR Foundation examples fail with 2 errors each. Likely a shared `@ar_session` or
`@platform` declaration pattern changed in recent core.

| File | Errors |
|---|---|
| `examples/ar-foundation/face-tracking.holo` | 2 |
| `examples/ar-foundation/geospatial-ar.holo` | 2 |
| `examples/ar-foundation/image-tracking.holo` | 2 |
| `examples/ar-foundation/light-estimation.holo` | 2 |
| `examples/ar-foundation/mesh-scanning.holo` | 2 |
| `examples/ar-foundation/persistent-anchors.holo` | 2 |
| `examples/ar-foundation/plane-detection.holo` | 2 |

### Stress-Tests (parse regression)

| File | Errors | Notes |
|---|---|---|
| `gem-vault-crystal-enchantable-2026-04-30.holo` | 4 | Older stress-test, syntax drift |
| `murmuration-market-crowd-sim-navmesh-pose-2026-05-04.holo` | 35 | Heavy drift |
| `sunken-archive-holomap-speech-2026-05-02.holo` | 12 | HoloMap trait API changed |
| `tidal-pool-bioluminescent-buoyancy-2026-05-07.holo` | 24 | Significant drift |

### Cross-Reality (OOM during parse)

`examples/cross-reality/01-geospatial-anchoring.holo` and `02-mvc-state-sync.holo` cause
JavaScript heap exhaustion during parse — likely recursive AST expansion from large nested
compositions. Not a syntax error; a parser memory issue.

## Patterns

1. **`@platform()` empty list** — grammar now rejects `@platform()` with no arguments.
   Affected: `platform-conditional-minimal.holo`. Fix: add at least one platform name.

2. **`domain-web3` block syntax** — both NFT marketplace files use `domain-web3 { ... }`
   inside compositions; parser now expects `LBRACE` after `RBRACKET`, suggesting array-of-blocks
   syntax changed. The `implicit-composition > domain-web3` path needs review.

3. **`ai-agent.hs`** — 19 errors from string-where-identifier pattern, likely uses
   newer `.holo` shorthand syntax in a `.hs` file.

4. **AR Foundation** — consistent 2-error pattern across all 7 files suggests a common
   header/preamble changed (e.g., `@ar_session` schema, `@platform(ar)` syntax, or
   `environment { ar_mode: true }` key rename).

5. **Cross-Reality OOM** — parser infinite-loop or exponential AST growth on large nested
   CRDT compositions. Should be profiled with `--max-old-space-size` flag.

## No Action Required From This Report

A-009 health reports are read-only artifacts. Fixes should be tracked as separate board tasks.
The 35 passing top-level examples remain the healthy example corpus for today's refreshes and
stress-tests.
