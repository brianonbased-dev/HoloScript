# Example Health Report — 2026-04-28

**A-009 Routine Fire** | Triggered: 2026-04-28T22:31 UTC

## Summary

| Status | Count |
|--------|-------|
| PASS   | 193   |
| FAIL   | 200   |
| TOTAL  | 393   |
| Pass % | 49.1% |

## Root Cause: Parser Gaps (Pre-existing)

All 200 failures trace to parser features present in example files but not yet implemented in the HoloScript parser. These are **pre-existing** gaps — examples were authored against a forward-looking spec; the parser hasn't caught up.

### Error Code Breakdown

| Code   | Count | Pattern |
|--------|-------|---------|
| HSP101 | 104   | `->` arrow in composition body (event handler syntax: `on Node.event -> (handler) {}`) |
| HSP001 | 61    | Various: `PIPE \|` in interface body, `ON_ERROR` keyword, COLON in node body, NPC syntax |
| HSP300 | 25    | `->` arrow in expressions, PIPE in expressions |
| HSP201 | 6     | Other structural errors |
| HSP002 | 2     | Minor token errors |
| HSP003 | 2     | Minor token errors |

### Key Missing Parser Features

1. **Arrow event handler syntax** — `on Node.path.event -> (event) { ... }` — Used extensively in affordances/, autonomous-ecosystems/, cross-reality/, lighting/, weather/ examples
2. **Pipe `|` in type unions** — `interface Foo { state: "a" | "b" }` — Used in distributed state examples
3. **`on_error` keyword** — Used in autonomous-ecosystem agent pipelines
4. **String-keyed properties** — `"key": value` in object bodies

### Failing Example Directories (complete coverage gap)

- `examples/affordances/` — all 2 files fail (HSP101: arrow event syntax)
- `examples/ar-foundation/` — all 7 files fail (HSP101: EQUALS in properties)
- `examples/autonomous-ecosystems/` — 13+ files fail
- `examples/cross-reality/` — all 3 files fail
- `examples/lighting/` — all 4 files fail
- `examples/weather/` — all 5 files fail
- `examples/webgpu-compute/` — all 5 files fail

## Recommendation

File parser gap tasks (covered separately by A-009 Phase 4 if stress-test triggers). The primary action is to track these 200 failures as a baseline — improvement = fewer failures in subsequent daily fires. The failing files are NOT regressions introduced today; they represent the accumulated delta between the spec-aspirational examples and the implemented parser.

## Passing Examples (193)

These files compile cleanly and serve as the valid example corpus:

`examples/hello-world.hs`, `examples/solar_system.holo`, `examples/vr-scene.hs`,  
`examples/multiplayer-game.hsplus`, `examples/neural-network.hs`, `examples/quantum-arena.hs`,  
`examples/quantum-radar.hs`, `examples/product-viewer.holo`, and 185 more.

---
_Generated by A-009 Example Freshness + Artist Stress-test Routine_
