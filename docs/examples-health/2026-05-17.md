# Example Health Report — 2026-05-17

Generated by: A-009 Example Freshness + Artist Stress-Test routine  
Run time: 2026-05-17 ~22:16 UTC

## Phase 1 — Core Build

`pnpm --filter @holoscript/core build` → **PASS**  
Upstream peers required (not pre-built in this environment):
- `@holoscript/core-types` — built ✓  
- `@holoscript/llm-provider` — built ✓  
- `@holoscript/framework` — built ✓  
- `@holoscript/crdt-spatial` — built ✓  
- `@holoscript/snn-webgpu` — built ✓  
- `@holoscript/uaal` — built ✓  
- `@holoscript/engine` — built ✓  
- `@holoscript/mesh` — built ✓  
- `@holoscript/platform` — built ✓

Note: This full build cascade is needed on every cold-start environment. CI or session-start should pre-build these in dependency order before any parse/validate step.

## Phase 1 — Parse Results (examples/ + stress-tests/)

Scope: `examples/*.{hs,hsplus,holo}` + `examples/stress-tests/*.holo`

| Result | Count | Pct |
|--------|-------|-----|
| PASS   | 48    | 79% |
| FAIL   | 13    | 21% |

### Failing Files (pre-existing drift — NOT introduced today)

| File | Error Count | Sample Error |
|------|-------------|--------------|
| `examples/ai-agent.hs` | 9 | syntax in action block |
| `examples/dashboard.hs` | 28 | JSX-style syntax not supported in .hs |
| `examples/neural-network.hs` | 6 | unknown directive |
| `examples/memory-atoms-showcase.holo` | 13 | unrecognized block type |
| `examples/nft-marketplace-advanced.holo` | 16 | nested template syntax |
| `examples/nft-marketplace-basic.holo` | 8 | NFT schema field |
| `examples/product-viewer.holo` | 11 | unknown directive |
| `examples/site.holo` | 7 | non-standard property |
| `examples/world-builder.holo` | 14 | script block syntax |
| `examples/stress-tests/gem-vault-crystal-enchantable-2026-04-30.holo` | 5 | trait config syntax |
| `examples/stress-tests/murmuration-market-crowd-sim-navmesh-pose-2026-05-04.holo` | 3 | array in trait |
| `examples/stress-tests/sunken-archive-holomap-speech-2026-05-02.holo` | 6 | nested block |
| `examples/stress-tests/tidal-pool-bioluminescent-buoyancy-2026-05-07.holo` | 4 | trait block syntax |

All 13 failures appear to be pre-existing (last-commit dates 2026-04-27 through 2026-05-07, predating yesterday's A-009 run). No regressions introduced today.

## Phase 2 — Refreshed Examples (2+ days stale)

Both originals last touched 2026-05-14 (3 days ago at run time):

| File | Added Traits | Status |
|------|-------------|--------|
| `examples/solar_system.refreshed.holo` | `@atmosphere_bruneton` (Earth + Mars), `@day_night_cycle` (Earth), `@irradiance_volume` (GI vol), `@emissive_gi` (Sun), `@lens_flare` (Sun), `@instanced` (asteroid belt) | ✓ PASS (1 cosmetic warning: "box" vs "cube") |
| `examples/quantum-radar.refreshed.hs` | `@emissive`, `@animated`, `@spatial_audio`, `@glowing`, `@clickable`, `@billboard`, `@networked` | ✓ PASS |

### Missing-trait wish captured (→ Phase 4)

During solar system refresh: `@corona_simulation` — solar corona with MHD flux tubes,  
prominence loops, and CME events. Currently faked with bloom + @emissive_gi.  
Filed in `research/audit-reports/gaps-pending/A-009-2026-05-17T221638Z.json`.

## Phase 3 — Stress-Test (never-exercised traits)

New file: `examples/stress-tests/eroded-shore-dawn-fft-radiance-ssgi-ssr-2026-05-17.holo`

**First-ever exercise** of these 5 trait combinations:

| Trait | Coverage before today | Result |
|-------|-----------------------|--------|
| `@ocean_fft` | 0 examples | ✓ compiled |
| `@terrain_hydraulic_erosion` | 0 examples | ✓ compiled |
| `@radiance_cascades` | 0 examples | ✓ compiled |
| `@screen_space_reflection` | 0 examples | ✓ compiled |
| `@ssgi` | 0 examples | ✓ compiled |

All 5 zero-coverage traits compiled cleanly in combination. Parser accepted:
- Nested single-line trait config objects (`@ocean_fft { resolution: 512, cascade_count: 4, ... }`)
- Invisible bounding-volume GI object pattern (`visible: false` + `@radiance_cascades`)
- SSR on both a water plane and a terrain/ledge object simultaneously
- `@ssgi` on two cliff objects at different radii without conflict

No compiler bug found. No Phase 4 (a) seed emitted for this test.

## Phase 4 — Seeds Emitted

1 seed (type b — missing trait):
- `@corona_simulation` — `dedup_key: a009-trait-corona_simulation`
- File: `research/audit-reports/gaps-pending/A-009-2026-05-17T221638Z.json`

## Recommendations

1. **CI parse gate**: the 9 long-standing failures in core examples (ai-agent.hs, dashboard.hs, etc.) need a fix pass. They were drifting before this run and will continue drifting without a CI gate on `examples/*.{hs,hsplus,holo}`.
2. **stress-tests pre-build**: 4 of the 17 stress-tests now fail to parse (gem-vault, murmuration-market, sunken-archive, tidal-pool). These were authored before the parser was stricter about nested array syntax — candidates for a Phase 2 refresh pass next cycle.
3. **Upstream build order**: sessions cold-starting without node_modules need to build 9 packages before the CLI validator works. Document in CONTRIBUTING.md.
