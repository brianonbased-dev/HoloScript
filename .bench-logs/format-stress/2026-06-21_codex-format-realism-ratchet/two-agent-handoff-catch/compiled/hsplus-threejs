// Generated by HoloScript Compiler
// Target: Three.js
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a2e);
const camera = new THREE.PerspectiveCamera(60, window.innerWidth / window.innerHeight, 0.1, 1000);
const renderer = new THREE.WebGLRenderer({ antialias: true });

renderer.setSize(window.innerWidth, window.innerHeight);
renderer.setPixelRatio(window.devicePixelRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.shadowMap.enabled = true;
document.body.appendChild(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;

// Lighting
scene.add(new THREE.AmbientLight(0xffffff, 0.4));
const directionalLight = new THREE.DirectionalLight(0xffffff, 1.2);
directionalLight.position.set(10, 20, 10);
directionalLight.castShadow = true;
scene.add(directionalLight);

// AlignProbe
const AlignProbe_geometry = new THREE.PlaneGeometry(0.64, 0.18);
const AlignProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const AlignProbe = new THREE.Mesh(AlignProbe_geometry, AlignProbe_material);
AlignProbe.position.set(-2.4, 2.35, -1.1);
AlignProbe.castShadow = true;
AlignProbe.receiveShadow = true;
scene.add(AlignProbe);

// ReleaseProbe
const ReleaseProbe_geometry = new THREE.PlaneGeometry(0.64, 0.18);
const ReleaseProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const ReleaseProbe = new THREE.Mesh(ReleaseProbe_geometry, ReleaseProbe_material);
ReleaseProbe.position.set(-1.2, 2.45, -1.1);
ReleaseProbe.castShadow = true;
ReleaseProbe.receiveShadow = true;
scene.add(ReleaseProbe);

// BallisticProbe
const BallisticProbe_geometry = new THREE.PlaneGeometry(0.64, 0.18);
const BallisticProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const BallisticProbe = new THREE.Mesh(BallisticProbe_geometry, BallisticProbe_material);
BallisticProbe.position.set(0, 2.55, -1.1);
BallisticProbe.castShadow = true;
BallisticProbe.receiveShadow = true;
scene.add(BallisticProbe);

// CatchProbe
const CatchProbe_geometry = new THREE.PlaneGeometry(0.64, 0.18);
const CatchProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const CatchProbe = new THREE.Mesh(CatchProbe_geometry, CatchProbe_material);
CatchProbe.position.set(1.2, 2.45, -1.1);
CatchProbe.castShadow = true;
CatchProbe.receiveShadow = true;
scene.add(CatchProbe);

// ReceiptProbe
const ReceiptProbe_geometry = new THREE.PlaneGeometry(0.64, 0.18);
const ReceiptProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const ReceiptProbe = new THREE.Mesh(ReceiptProbe_geometry, ReceiptProbe_material);
ReceiptProbe.position.set(2.4, 2.35, -1.1);
ReceiptProbe.castShadow = true;
ReceiptProbe.receiveShadow = true;
scene.add(ReceiptProbe);

camera.position.set(0, 3, 10);
controls.target.set(0, 1, 0);

// Logic Handlers
function recordPhase(...args) {
  console.log('[HoloScript] Triggered logic: recordPhase', args);
}

function releaseTool(...args) {
  console.log('[HoloScript] Triggered logic: releaseTool', args);
}

function catchTool(...args) {
  console.log('[HoloScript] Triggered logic: catchTool', args);
}

function writeReceipt(...args) {
  console.log('[HoloScript] Triggered logic: writeReceipt', args);
}

function recordGap(...args) {
  console.log('[HoloScript] Triggered logic: recordGap', args);
}

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

function animate() {
  requestAnimationFrame(animate);
  controls.update();
  renderer.render(scene, camera);
}
animate();
