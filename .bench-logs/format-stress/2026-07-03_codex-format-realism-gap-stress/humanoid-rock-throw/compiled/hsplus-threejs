// Generated by HoloScript Compiler
// Target: Three.js
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a2e);
const camera = new THREE.PerspectiveCamera(60, window.innerWidth / window.innerHeight, 0.1, 1000);
const renderer = new THREE.WebGLRenderer({ antialias: true });

renderer.setSize(window.innerWidth, window.innerHeight);
renderer.setPixelRatio(window.devicePixelRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.shadowMap.enabled = true;
document.body.appendChild(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;

// Lighting
scene.add(new THREE.AmbientLight(0xffffff, 0.4));
const directionalLight = new THREE.DirectionalLight(0xffffff, 1.2);
directionalLight.position.set(10, 20, 10);
directionalLight.castShadow = true;
scene.add(directionalLight);

// RockContract
const RockContract_geometry = new THREE.SphereGeometry(0.14, 32, 32);
const RockContract_material = new THREE.MeshStandardMaterial({ color: '#7c6f64', roughness: 0.95 });
const RockContract = new THREE.Mesh(RockContract_geometry, RockContract_material);
RockContract.position.set(-1.2, 0.35, 0);
RockContract.castShadow = true;
RockContract.receiveShadow = true;
// Physics enabled for RockContract (@physics)
scene.add(RockContract);

// SceneLoadedProbe
const SceneLoadedProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const SceneLoadedProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const SceneLoadedProbe = new THREE.Mesh(SceneLoadedProbe_geometry, SceneLoadedProbe_material);
SceneLoadedProbe.position.set(-4.8, 2.7, -2.8);
SceneLoadedProbe.castShadow = true;
SceneLoadedProbe.receiveShadow = true;
scene.add(SceneLoadedProbe);

// ApproachProbe
const ApproachProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const ApproachProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const ApproachProbe = new THREE.Mesh(ApproachProbe_geometry, ApproachProbe_material);
ApproachProbe.position.set(-3.8, 2.5, -2.8);
ApproachProbe.castShadow = true;
ApproachProbe.receiveShadow = true;
scene.add(ApproachProbe);

// ReachProbe
const ReachProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const ReachProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const ReachProbe = new THREE.Mesh(ReachProbe_geometry, ReachProbe_material);
ReachProbe.position.set(-2.8, 2.3, -2.8);
ReachProbe.castShadow = true;
ReachProbe.receiveShadow = true;
scene.add(ReachProbe);

// GrabProbe
const GrabProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const GrabProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const GrabProbe = new THREE.Mesh(GrabProbe_geometry, GrabProbe_material);
GrabProbe.position.set(-1.8, 2.1, -2.8);
GrabProbe.castShadow = true;
GrabProbe.receiveShadow = true;
scene.add(GrabProbe);

// LiftProbe
const LiftProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const LiftProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const LiftProbe = new THREE.Mesh(LiftProbe_geometry, LiftProbe_material);
LiftProbe.position.set(-0.8, 2, -2.8);
LiftProbe.castShadow = true;
LiftProbe.receiveShadow = true;
scene.add(LiftProbe);

// WindupProbe
const WindupProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const WindupProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const WindupProbe = new THREE.Mesh(WindupProbe_geometry, WindupProbe_material);
WindupProbe.position.set(0.2, 2.1, -2.8);
WindupProbe.castShadow = true;
WindupProbe.receiveShadow = true;
scene.add(WindupProbe);

// ReleaseProbe
const ReleaseProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const ReleaseProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const ReleaseProbe = new THREE.Mesh(ReleaseProbe_geometry, ReleaseProbe_material);
ReleaseProbe.position.set(1.2, 2.2, -2.8);
ReleaseProbe.castShadow = true;
ReleaseProbe.receiveShadow = true;
scene.add(ReleaseProbe);

// ArcProbe
const ArcProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const ArcProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const ArcProbe = new THREE.Mesh(ArcProbe_geometry, ArcProbe_material);
ArcProbe.position.set(2.2, 2.25, -2.8);
ArcProbe.castShadow = true;
ArcProbe.receiveShadow = true;
scene.add(ArcProbe);

// ImpactProbe
const ImpactProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const ImpactProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const ImpactProbe = new THREE.Mesh(ImpactProbe_geometry, ImpactProbe_material);
ImpactProbe.position.set(3.2, 2.15, -2.8);
ImpactProbe.castShadow = true;
ImpactProbe.receiveShadow = true;
scene.add(ImpactProbe);

// AftermathProbe
const AftermathProbe_geometry = new THREE.PlaneGeometry(0.6, 0.18);
const AftermathProbe_material = new THREE.MeshStandardMaterial({ color: '#111827', roughness: 0.5, emissive: '#1f2937', emissiveIntensity: 0.2, opacity: 0.92, transparent: true });
const AftermathProbe = new THREE.Mesh(AftermathProbe_geometry, AftermathProbe_material);
AftermathProbe.position.set(4.2, 1.95, -2.8);
AftermathProbe.castShadow = true;
AftermathProbe.receiveShadow = true;
scene.add(AftermathProbe);

camera.position.set(0, 3, 10);
controls.target.set(0, 1, 0);

// Logic Handlers
function recordSegment(...args) {
  console.log('[HoloScript] Triggered logic: recordSegment', args);
}

function recordStill(...args) {
  console.log('[HoloScript] Triggered logic: recordStill', args);
}

function recordGap(...args) {
  console.log('[HoloScript] Triggered logic: recordGap', args);
}

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

function animate() {
  requestAnimationFrame(animate);
  controls.update();
  renderer.render(scene, camera);
}
animate();
