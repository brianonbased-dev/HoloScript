{
  "_comment": "Trait parity manifest — traits emitted by R3FCompiler that have NO registered BrowserRuntime handler.",
  "_doc": "A trait here is classified as a no-op in the native runtime: the compiler emits it (for schema / downstream targets) but BrowserRuntime does not apply it at runtime. CI gate: scripts/holo-ci/check-trait-parity.mjs. When a handler IS added to BrowserRuntime for one of these traits, REMOVE it from this list so the gate stays tight. When R3FCompiler emits a NEW trait, add a handler to BrowserRuntime OR add it here with a reason.",
  "_generated": "2026-06-17 — seeded from R3FCompiler.ts name=== cases vs TraitSystem.register calls in BrowserRuntime.ts",
  "_updated": "2026-06-17 — 15 Tier-1 behavioral traits promoted to registered handlers in BehavioralTraits.ts (follow, orbit, crowd_sim, chain, ai_companion, ai_npc_brain, llm_agent, gpu_particle, gpu_physics, deformable_terrain, soft_body_pro, compute, object_tracking, sensor, digital_twin)",
  "noopTraits": {
    "ai_inpainting":     "AI inpainting — GPU-compute trait, no BrowserRuntime handler",
    "ai_texture_gen":    "AI texture generation — GPU-compute trait, no BrowserRuntime handler",
    "ai_upscaling":      "AI upscaling — GPU-compute trait, no BrowserRuntime handler",
    "ai_vision":         "alias for vision — same as vision, no BrowserRuntime handler",
    "ambisonics":        "ambisonic audio — spatialised audio trait, BrowserRuntime omitted",
    "attach":            "attachment relationship — resolved at compile time, no runtime handler",
    "audio_occlusion":   "audio occlusion — XR audio, no BrowserRuntime handler",
    "audio_portal":      "audio portal — XR audio, no BrowserRuntime handler",
    "bloom":             "post-processing bloom — R3F post-processing prop, handled by R3F itself",
    "body":              "character body directive — resolved at compile time",
    "botanical_lotus":   "special botanical object — compiler-generated geometry, no trait handler",
    "co_located":        "shared-space co-location — XR feature, no BrowserRuntime handler",
    "controlnet":        "ControlNet diffusion — GPU-compute trait, no BrowserRuntime handler",
    "diffusion_realtime":"realtime diffusion — GPU-compute trait, no BrowserRuntime handler",
    "draft":             "draft/blockout geometry marker — asset pipeline stage, no runtime handler",
    "embedding_search":  "vector embedding search — server-side retrieval, no BrowserRuntime handler",
    "eye_hand_fusion":   "eye-hand fusion — VisionOS XR feature, no BrowserRuntime handler",
    "eye_tracked":       "eye tracking — XR hardware feature, no BrowserRuntime handler",
    "gaussian_splat":    "3DGS splat renderer — rendered via specialised component, no trait handler",
    "gem_resonance":     "gem resonance audio — set as R3F prop, no trait handler",
    "geospatial":        "geospatial anchor — ARCore/ARKit feature, no BrowserRuntime handler",
    "god_rays":          "god-rays VFX — post-processing, no BrowserRuntime handler",
    "head_tracked_audio":"head-tracked audio — XR hardware feature, no BrowserRuntime handler",
    "hrtf":              "HRTF audio — XR audio, no BrowserRuntime handler",
    "light_estimation":  "AR light estimation — ARCore/ARKit feature, no BrowserRuntime handler",
    "magnifiable":       "magnification accessibility — UI concern, no runtime handler",
    "material":          "material preset — set as R3F materialProps prop at compile time",
    "mesh_detection":    "AR mesh detection — ARCore/ARKit feature, no BrowserRuntime handler",
    "moldable":          "moldable surface — set as R3F prop, no trait handler",
    "nerf":              "NeRF renderer — rendered via specialised component, no trait handler",
    "neural_forge":      "neural forge — GPU-compute trait, no BrowserRuntime handler",
    "neural_link":       "neural link — server-side inference, no BrowserRuntime handler",
    "occlusion":         "AR occlusion — ARCore/ARKit feature, no BrowserRuntime handler",
    "persistent_anchor": "persistent AR anchor — ARCore/ARKit feature, no BrowserRuntime handler",
    "photogrammetry":    "photogrammetry asset — rendered via specialised component, no trait handler",
    "plane_detection":   "AR plane detection — ARCore/ARKit feature, no BrowserRuntime handler",
    "point_cloud":       "point cloud renderer — rendered via specialised component, no trait handler",
    "positional":        "positional audio alias — set as R3F spatial prop, no trait handler",
    "scene_reconstruction":"AR scene reconstruction — ARCore/ARKit feature, no BrowserRuntime handler",
    "shadow":            "shadow casting — set as R3F castShadow prop, no trait handler",
    "shared_anchor":     "shared AR anchor — ARCore/ARKit feature, no BrowserRuntime handler",
    "shareplay":         "SharePlay — visionOS feature, no BrowserRuntime handler",
    "skeleton":          "skeleton/rig binding — resolved at compile time, no runtime handler",
    "spatial_awareness": "spatial awareness — XR feature, no BrowserRuntime handler",
    "spatial_persona":   "spatial persona — visionOS feature, no BrowserRuntime handler",
    "spatial_voice":     "spatial voice — set as R3F prop, no standalone trait handler",
    "stretchable":       "stretchable surface — set as R3F prop, no trait handler",
    "string":            "string type artifact — parser artefact, should not appear in traits",
    "vector_db":         "vector DB binding — server-side retrieval, no BrowserRuntime handler",
    "vision":            "AI vision — server-side inference, no BrowserRuntime handler",
    "volumetric_clouds": "volumetric clouds VFX — post-processing, no BrowserRuntime handler",
    "volumetric_video":  "volumetric video — rendered via specialised component, no trait handler",
    "web_surface":       "embedded web surface — set as R3F prop, no trait handler"
  }
}
