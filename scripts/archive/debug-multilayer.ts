/**
 * Archived 2026-06-29: ARCompiler was retired with the apex-poison web bridge
 * compilers. Keep this file as a breadcrumb only; active debugging should use
 * AndroidXR/OpenXR/Quest native targets instead of generic WebXR AR output.
 */

console.error(
  'scripts/archive/debug-multilayer.ts is archived: ARCompiler is retired. ' +
    'Use AndroidXR/OpenXR/Quest native targets.'
);
process.exitCode = 1;
