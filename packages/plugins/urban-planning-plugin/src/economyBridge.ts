/**
 * Canonical plugin id for `CreatorRevenueAggregator.recordRevenue` attribution.
 *
 * Must match `URBAN_PLANNING_ECONOMY_PLUGIN_ID` in `@holoscript/framework` economy
 * (`urbanPlanningEconomyBridge.ts`).
 */
export const URBAN_PLANNING_ECONOMY_PLUGIN_ID = '@holoscript/plugin-urban-planning' as const;
