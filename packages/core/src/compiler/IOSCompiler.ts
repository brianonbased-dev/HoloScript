/**
 * HoloScript → iOS Swift ARKit Compiler
 *
 * Translates a HoloComposition AST into Swift code targeting
 * ARKit for iPhone and iPad augmented reality experiences.
 *
 * Emits:
 *   - SwiftUI + ARKit integration
 *   - ARSCNView with SceneKit nodes
 *   - Plane detection and hit testing
 *   - World tracking configuration
 *   - Gesture recognizers for interaction
 *   - Spatial audio with SceneKit
 *
 * @version 1.0.0
 */

import { CompilerBase, type CompilerToken } from './CompilerBase';
import { ANSCapabilityPath, type ANSCapabilityPathValue } from './identity/ANSNamespace';
import type {
  HoloComposition,
  HoloObjectDecl,
  HoloLight,
  HoloAudio,
  HoloValue,
} from '../parser/HoloCompositionTypes';
import { ROOMPLAN_TRAITS } from '../traits/constants/roomplan';
import { LIDAR_SCANNER_TRAITS } from '../traits/constants/lidar-scanner';
import { NPU_SCENE_TRAITS, NPU_SCENE_DEFAULTS } from '../traits/constants/npu-scene';
import { PORTAL_AR_TRAITS } from '../traits/constants/portal-ar';
import { CAMERA_HAND_TRACKING_TRAITS } from '../traits/constants/camera-hand-tracking';
import { FACE_TRACKING_TRAITS } from '../traits/constants/face-tracking';
import { IOS_OBJECT_CAPTURE_TRAITS } from '../traits/constants/ios-object-capture';
import { SHAREPLAY_TRAITS } from '../traits/constants/shareplay';
import { UWB_POSITIONING_TRAITS } from '../traits/constants/uwb-positioning';
import { AIRPODS_SPATIAL_AUDIO_TRAITS } from '../traits/constants/airpods-spatial-audio';

export interface IOSCompilerOptions {
  className?: string;
  indent?: string;
  iosVersion?: '15.0' | '16.0' | '17.0' | '18.0';
  useSwiftUI?: boolean;
  useCombine?: boolean;
  useRealityKit?: boolean; // For newer iOS, RealityKit is preferred
}

import type { IOSCompileResult } from './CompilerTypes';
export type { IOSCompileResult } from './CompilerTypes';

export class IOSCompiler extends CompilerBase {
  protected readonly compilerName = 'IOSCompiler';

  protected override getRequiredCapability(): ANSCapabilityPathValue {
    return ANSCapabilityPath.IOS;
  }

  private options: Required<IOSCompilerOptions>;
  private lines: string[] = [];
  private indentLevel: number = 0;

  constructor(options: IOSCompilerOptions = {}) {
    super();
    this.options = {
      className: options.className || 'GeneratedARScene',
      indent: options.indent || '    ',
      iosVersion: options.iosVersion || '17.0',
      useSwiftUI: options.useSwiftUI ?? true,
      useCombine: options.useCombine ?? true,
      useRealityKit: options.useRealityKit ?? false,
    };
  }

  compile(
    composition: HoloComposition,
    agentToken: CompilerToken,
    outputPath?: string
  ): IOSCompileResult {
    this.validateCompilerAccess(agentToken, outputPath);
    const result: IOSCompileResult = {
      viewFile: this.generateViewFile(composition),
      sceneFile: this.generateSceneFile(composition),
      stateFile: this.generateStateFile(composition),
      infoPlist: this.generateInfoPlist(composition),
    };

    if (this.hasRoomPlanTraits(composition)) {
      result.roomPlanFile = this.generateRoomPlanFile(composition);
    }

    if (this.hasLiDARTraits(composition)) {
      result.lidarScannerFile = this.generateLiDARScannerFile(composition);
    }

    if (this.hasNPUSceneTraits(composition)) {
      result.npuSceneFile = this.generateNPUSceneFile(composition);
    }

    if (this.hasPortalARTraits(composition)) {
      result.portalARFile = this.generatePortalARFile(composition);
    }

    if (this.hasHandTrackingTraits(composition)) {
      result.handTrackingFile = this.generateHandTrackingFile(composition);
    }

    if (this.hasObjectCaptureTraits(composition)) {
      result.objectCaptureFile = this.generateObjectCaptureFile(composition);
    }

    if (this.hasFaceTrackingTraits(composition)) {
      result.faceTrackingFile = this.generateFaceTrackingFile(composition);
    }

    if (this.hasSharePlayTraits(composition)) {
      result.sharePlayFile = this.generateSharePlayFile(composition);
    }

    if (this.hasUWBPositioningTraits(composition)) {
      result.uwbPositioningFile = this.generateUWBPositioningFile(composition);
    }

    if (this.hasSpatialAudioTraits(composition)) {
      result.spatialAudioFile = this.generateSpatialAudioFile(composition);
    }

    return result;
  }

  private generateViewFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    this.emit('// Auto-generated by HoloScript IOSCompiler');
    this.emit(
      `// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`
    );
    this.emit(`// iOS: ${this.options.iosVersion}`);
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import SwiftUI');
    this.emit('import ARKit');
    this.emit('import SceneKit');
    if (this.options.useCombine) {
      this.emit('import Combine');
    }
    if (this.hasGeoTraits(composition)) {
      this.emit('import CoreLocation');
    }
    this.emit('');

    // SwiftUI View
    this.emit(`struct ${this.options.className}View: View {`);
    this.indentLevel++;

    this.emit('@StateObject private var sceneState = SceneState()');
    this.emit('@State private var arView: ARSCNView?');
    this.emit('');

    this.emit('var body: some View {');
    this.indentLevel++;
    this.emit('ZStack {');
    this.indentLevel++;
    this.emit('ARViewContainer(sceneState: sceneState)');
    this.emit('    .edgesIgnoringSafeArea(.all)');
    this.emit('');

    // UI overlay
    this.emit('VStack {');
    this.indentLevel++;
    this.emit('Spacer()');
    this.emit('HStack {');
    this.indentLevel++;
    this.emit('Button("Reset") {');
    this.indentLevel++;
    this.emit('sceneState.resetScene()');
    this.indentLevel--;
    this.emit('}');
    this.emit('.padding()');
    this.emit('.background(Color.white.opacity(0.8))');
    this.emit('.cornerRadius(10)');
    this.indentLevel--;
    this.emit('}');
    this.emit('.padding()');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ARView Container
    this.emit('struct ARViewContainer: UIViewRepresentable {');
    this.indentLevel++;
    this.emit('@ObservedObject var sceneState: SceneState');
    this.emit('');

    this.emit('func makeUIView(context: Context) -> ARSCNView {');
    this.indentLevel++;
    this.emit('let arView = ARSCNView(frame: .zero)');
    this.emit('arView.delegate = context.coordinator');
    this.emit('arView.autoenablesDefaultLighting = true');
    this.emit('arView.automaticallyUpdatesLighting = true');
    this.emit('');
    this.emit('// Configure AR session');
    this.emit('let configuration = ARWorldTrackingConfiguration()');
    this.emit('configuration.planeDetection = [.horizontal, .vertical]');
    this.emit('configuration.environmentTexturing = .automatic');
    if (parseFloat(this.options.iosVersion) >= 17.0) {
      this.emit('configuration.sceneReconstruction = .meshWithClassification');
    }
    this.emit('');
    this.emit('arView.session.run(configuration)');
    this.emit('');
    this.emit('// Add tap gesture');
    this.emit(
      'let tapGesture = UITapGestureRecognizer(target: context.coordinator, action: #selector(Coordinator.handleTap(_:)))'
    );
    this.emit('arView.addGestureRecognizer(tapGesture)');
    this.emit('');
    this.emit('// Add pan gesture for dragging');
    this.emit(
      'let panGesture = UIPanGestureRecognizer(target: context.coordinator, action: #selector(Coordinator.handlePan(_:)))'
    );
    this.emit('arView.addGestureRecognizer(panGesture)');
    this.emit('');
    this.emit('context.coordinator.arView = arView');
    this.emit('sceneState.arView = arView');
    this.emit('');
    this.emit('return arView');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit('func updateUIView(_ uiView: ARSCNView, context: Context) {');
    this.indentLevel++;
    this.emit('// Update view if needed');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit('func makeCoordinator() -> Coordinator {');
    this.indentLevel++;
    this.emit('Coordinator(sceneState: sceneState)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Coordinator class
    this.emit('class Coordinator: NSObject, ARSCNViewDelegate {');
    this.indentLevel++;
    this.emit('var sceneState: SceneState');
    this.emit('weak var arView: ARSCNView?');
    this.emit('var selectedNode: SCNNode?');
    this.emit('');

    this.emit('init(sceneState: SceneState) {');
    this.indentLevel++;
    this.emit('self.sceneState = sceneState');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Tap handler
    this.emit('@objc func handleTap(_ gesture: UITapGestureRecognizer) {');
    this.indentLevel++;
    this.emit('guard let arView = arView else { return }');
    this.emit('let location = gesture.location(in: arView)');
    this.emit('');
    this.emit('// Check for hit on existing nodes');
    this.emit('let hitResults = arView.hitTest(location, options: [:])');
    this.emit('if let hit = hitResults.first {');
    this.indentLevel++;
    this.emit('sceneState.handleNodeTap(hit.node)');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('// Raycast to place new object');
    this.emit(
      'if let query = arView.raycastQuery(from: location, allowing: .estimatedPlane, alignment: .any) {'
    );
    this.indentLevel++;
    this.emit('let results = arView.session.raycast(query)');
    this.emit('if let result = results.first {');
    this.indentLevel++;
    this.emit('sceneState.placeObject(at: result.worldTransform)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Pan handler
    this.emit('@objc func handlePan(_ gesture: UIPanGestureRecognizer) {');
    this.indentLevel++;
    this.emit('guard let arView = arView else { return }');
    this.emit('let location = gesture.location(in: arView)');
    this.emit('');
    this.emit('switch gesture.state {');
    this.emit('case .began:');
    this.indentLevel++;
    this.emit('let hitResults = arView.hitTest(location, options: [:])');
    this.emit('selectedNode = hitResults.first?.node');
    this.indentLevel--;
    this.emit('case .changed:');
    this.indentLevel++;
    this.emit('guard let node = selectedNode else { return }');
    this.emit(
      'if let query = arView.raycastQuery(from: location, allowing: .estimatedPlane, alignment: .any),'
    );
    this.emit('   let result = arView.session.raycast(query).first {');
    this.indentLevel++;
    this.emit('node.simdWorldPosition = simd_make_float3(result.worldTransform.columns.3)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('case .ended, .cancelled:');
    this.indentLevel++;
    this.emit('selectedNode = nil');
    this.indentLevel--;
    this.emit('default:');
    this.indentLevel++;
    this.emit('break');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ARSCNView delegate methods
    this.emit(
      'func renderer(_ renderer: SCNSceneRenderer, didAdd node: SCNNode, for anchor: ARAnchor) {'
    );
    this.indentLevel++;
    this.emit('if let planeAnchor = anchor as? ARPlaneAnchor {');
    this.indentLevel++;
    this.emit('sceneState.didDetectPlane(planeAnchor, node: node)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit(
      'func renderer(_ renderer: SCNSceneRenderer, didUpdate node: SCNNode, for anchor: ARAnchor) {'
    );
    this.indentLevel++;
    this.emit('if let planeAnchor = anchor as? ARPlaneAnchor {');
    this.indentLevel++;
    this.emit('sceneState.didUpdatePlane(planeAnchor, node: node)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Preview
    this.emit('#Preview {');
    this.indentLevel++;
    this.emit(`${this.options.className}View()`);
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  private generateSceneFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    this.emit('// Auto-generated by HoloScript IOSCompiler');
    this.emit(`// Scene: ${this.escapeStringValue(composition.name as string, 'Swift')}`);
    this.emit('');
    this.emit('import Foundation');
    this.emit('import SceneKit');
    this.emit('import ARKit');
    this.emit('');

    this.emit(`enum ${this.options.className}Objects {`);
    this.indentLevel++;

    // Generate node creation methods for each object
    for (const obj of composition.objects || []) {
      this.compileObjectFactory(obj);
    }

    this.emit('');
    this.emit('// Factory method to create all scene objects');
    this.emit('static func createSceneNodes() -> [String: SCNNode] {');
    this.indentLevel++;
    this.emit('var nodes: [String: SCNNode] = [:]');
    for (const obj of composition.objects || []) {
      const varName = this.sanitizeName(obj.name);
      this.emit(
        `nodes["${this.escapeStringValue(obj.name as string, 'Swift')}"] = make${varName}()`
      );
    }
    this.emit('return nodes');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Light factories
    if (composition.lights?.length) {
      this.emit('enum SceneLights {');
      this.indentLevel++;
      for (const light of composition.lights) {
        this.compileLightFactory(light);
      }
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Audio factories
    if (composition.audio?.length) {
      this.emit('enum SceneAudio {');
      this.indentLevel++;
      for (const audio of composition.audio) {
        this.compileAudioFactory(audio);
      }
      this.indentLevel--;
      this.emit('}');
    }

    return this.lines.join('\n');
  }

  private generateStateFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    this.emit('// Auto-generated by HoloScript IOSCompiler');
    this.emit(`// State: ${this.escapeStringValue(composition.name as string, 'Swift')}`);
    this.emit('');
    this.emit('import Foundation');
    this.emit('import SceneKit');
    this.emit('import ARKit');
    if (this.options.useCombine) {
      this.emit('import Combine');
    }
    this.emit('');

    this.emit('class SceneState: ObservableObject {');
    this.indentLevel++;

    // Published state properties
    if (composition.state) {
      this.emit('// === Published State ===');
      for (const prop of composition.state.properties) {
        const swiftType = this.toSwiftType(prop.value);
        const swiftValue = this.toSwiftValue(prop.value);
        this.emit(
          `@Published var ${this.escapeStringValue(prop.key as string, 'Swift')}: ${swiftType} = ${swiftValue}`
        );
      }
      this.emit('');
    }

    // AR references
    this.emit('// === AR References ===');
    this.emit('weak var arView: ARSCNView?');
    this.emit('var placedNodes: [String: SCNNode] = [:]');
    this.emit('var detectedPlanes: [UUID: SCNNode] = [:]');
    this.emit('');

    // Init
    this.emit('init() {');
    this.indentLevel++;
    this.emit('print("[HoloScript] Scene state initialized")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Place object method
    this.emit('func placeObject(at transform: simd_float4x4) {');
    this.indentLevel++;
    this.emit('guard let arView = arView else { return }');
    this.emit('');
    this.emit(`let nodes = ${this.options.className}Objects.createSceneNodes()`);
    this.emit('guard let node = nodes.values.first else { return }');
    this.emit('');
    this.emit('node.simdWorldTransform = transform');
    this.emit('arView.scene.rootNode.addChildNode(node)');
    this.emit('');
    this.emit('let id = UUID().uuidString');
    this.emit('placedNodes[id] = node');
    this.emit('print("[HoloScript] Placed object: \\(id)")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Handle node tap
    this.emit('func handleNodeTap(_ node: SCNNode) {');
    this.indentLevel++;
    this.emit('print("[HoloScript] Tapped node: \\(node.name ?? "unnamed")")');
    this.emit('');
    this.emit('// Visual feedback');
    this.emit('let originalScale = node.scale');
    this.emit('SCNTransaction.begin()');
    this.emit('SCNTransaction.animationDuration = 0.1');
    this.emit(
      'node.scale = SCNVector3(originalScale.x * 1.2, originalScale.y * 1.2, originalScale.z * 1.2)'
    );
    this.emit('SCNTransaction.commit()');
    this.emit('');
    this.emit('DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) {');
    this.indentLevel++;
    this.emit('SCNTransaction.begin()');
    this.emit('SCNTransaction.animationDuration = 0.1');
    this.emit('node.scale = originalScale');
    this.emit('SCNTransaction.commit()');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Plane detection
    this.emit('func didDetectPlane(_ anchor: ARPlaneAnchor, node: SCNNode) {');
    this.indentLevel++;
    this.emit(
      'let plane = SCNPlane(width: CGFloat(anchor.planeExtent.width), height: CGFloat(anchor.planeExtent.height))'
    );
    this.emit('plane.firstMaterial?.diffuse.contents = UIColor.white.withAlphaComponent(0.3)');
    this.emit('');
    this.emit('let planeNode = SCNNode(geometry: plane)');
    this.emit('planeNode.eulerAngles.x = -.pi / 2');
    this.emit('planeNode.position = SCNVector3(anchor.center.x, 0, anchor.center.z)');
    this.emit('');
    this.emit('node.addChildNode(planeNode)');
    this.emit('detectedPlanes[anchor.identifier] = planeNode');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit('func didUpdatePlane(_ anchor: ARPlaneAnchor, node: SCNNode) {');
    this.indentLevel++;
    this.emit('guard let planeNode = detectedPlanes[anchor.identifier],');
    this.emit('      let plane = planeNode.geometry as? SCNPlane else { return }');
    this.emit('');
    this.emit('plane.width = CGFloat(anchor.planeExtent.width)');
    this.emit('plane.height = CGFloat(anchor.planeExtent.height)');
    this.emit('planeNode.position = SCNVector3(anchor.center.x, 0, anchor.center.z)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Reset scene
    this.emit('func resetScene() {');
    this.indentLevel++;
    this.emit('guard let arView = arView else { return }');
    this.emit('');
    this.emit('// Remove placed nodes');
    this.emit('for node in placedNodes.values {');
    this.indentLevel++;
    this.emit('node.removeFromParentNode()');
    this.indentLevel--;
    this.emit('}');
    this.emit('placedNodes.removeAll()');
    this.emit('');
    this.emit('// Reset AR session');
    this.emit('let configuration = ARWorldTrackingConfiguration()');
    this.emit('configuration.planeDetection = [.horizontal, .vertical]');
    this.emit(
      'arView.session.run(configuration, options: [.resetTracking, .removeExistingAnchors])'
    );
    this.emit('');
    this.emit('print("[HoloScript] Scene reset")');
    this.indentLevel--;
    this.emit('}');

    // Geo-anchor methods
    if (this.hasGeoTraits(composition)) {
      this.emitGeoAnchorMethods(composition);
    }

    // Actions from composition state
    if (composition.logic?.actions) {
      this.emit('');
      this.emit('// === Actions ===');
      for (const action of composition.logic.actions) {
        this.compileAction(action);
      }
    }

    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  private generateInfoPlist(composition: HoloComposition): string {
    const hasGeo = this.hasGeoTraits(composition);

    const geoKeys = hasGeo
      ? `
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>This app uses your location to anchor holographic scenes to real-world GPS coordinates.</string>
    <key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
    <string>This app uses your location to anchor holographic scenes to real-world GPS coordinates.</string>`
      : '';

    const geoCapabilities = hasGeo ? `
        <string>location-services</string>
        <string>gps</string>` : '';

    return `<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSCameraUsageDescription</key>
    <string>This app uses the camera for augmented reality experiences.</string>${geoKeys}
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arkit</string>
        <string>arm64</string>${geoCapabilities}
    </array>
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <true/>
    </dict>
    <key>CFBundleDisplayName</key>
    <string>${this.escapeStringValue(composition.name as string, 'XML')}</string>
    <key>MinimumOSVersion</key>
    <string>${this.options.iosVersion}</string>
</dict>
</plist>`;
  }

  private compileObjectFactory(obj: HoloObjectDecl): void {
    const varName = this.sanitizeName(obj.name);
    const meshType = this.findObjProp(obj, 'mesh') || this.findObjProp(obj, 'type') || 'cube';

    this.emit(`static func make${varName}() -> SCNNode {`);
    this.indentLevel++;

    // Geometry
    const geometry = this.getSceneKitGeometry(meshType as string);
    this.emit(`let geometry = ${geometry}`);
    this.emit('');

    // Material
    const color = this.findObjProp(obj, 'color');
    const material = this.findObjProp(obj, 'material');
    this.emit('let material = SCNMaterial()');
    if (color) {
      this.emit(`material.diffuse.contents = ${this.toUIColor(color)}`);
    } else {
      this.emit('material.diffuse.contents = UIColor.systemBlue');
    }
    if (material && typeof material === 'object') {
      const mat = material as Record<string, any>;
      if (mat.metalness !== undefined) {
        this.emit(`material.metalness.contents = ${mat.metalness}`);
      }
      if (mat.roughness !== undefined) {
        this.emit(`material.roughness.contents = ${mat.roughness}`);
      }
    }
    this.emit('geometry.materials = [material]');
    this.emit('');

    // Create node
    this.emit('let node = SCNNode(geometry: geometry)');
    this.emit(`node.name = "${this.escapeStringValue(obj.name as string, 'Swift')}"`);

    // Transform
    const pos = this.findObjProp(obj, 'position');
    if (pos && Array.isArray(pos)) {
      this.emit(`node.position = SCNVector3(${pos[0]}, ${pos[1]}, ${pos[2]})`);
    }

    const scale = this.findObjProp(obj, 'scale');
    if (scale) {
      if (Array.isArray(scale)) {
        this.emit(`node.scale = SCNVector3(${scale[0]}, ${scale[1]}, ${scale[2]})`);
      } else {
        this.emit(`node.scale = SCNVector3(${scale}, ${scale}, ${scale})`);
      }
    }

    const rot = this.findObjProp(obj, 'rotation');
    if (rot && Array.isArray(rot)) {
      this.emit(`node.eulerAngles = SCNVector3(${rot[0]}, ${rot[1]}, ${rot[2]})`);
    }

    // Physics
    if (obj.traits?.some((t) => t.name === 'physics')) {
      this.emit('');
      this.emit('let physicsBody = SCNPhysicsBody(type: .dynamic, shape: nil)');
      const physicsTrait = obj.traits.find((t) => t.name === 'physics');
      if (physicsTrait?.config?.mass) {
        this.emit(`physicsBody.mass = ${physicsTrait.config.mass}`);
      }
      this.emit('node.physicsBody = physicsBody');
    } else if (obj.traits?.some((t) => t.name === 'collidable')) {
      this.emit('');
      this.emit('let physicsBody = SCNPhysicsBody(type: .static, shape: nil)');
      this.emit('node.physicsBody = physicsBody');
    }

    this.emit('');
    this.emit('return node');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
  }

  private compileLightFactory(light: HoloLight): void {
    const varName = this.sanitizeName(light.name);

    this.emit(`static func make${varName}() -> SCNNode {`);
    this.indentLevel++;

    const lightTypeMap: Record<string, string> = {
      directional: 'SCNLight.LightType.directional',
      point: 'SCNLight.LightType.omni',
      spot: 'SCNLight.LightType.spot',
      ambient: 'SCNLight.LightType.ambient',
    };

    this.emit('let light = SCNLight()');
    this.emit(`light.type = ${lightTypeMap[light.lightType] || 'SCNLight.LightType.omni'}`);

    for (const prop of light.properties) {
      if (prop.key === 'color') {
        this.emit(`light.color = ${this.toUIColor(prop.value)}`);
      } else if (prop.key === 'intensity') {
        this.emit(`light.intensity = ${(prop.value as number) * 1000}`);
      } else if (prop.key === 'cast_shadow' || prop.key === 'castShadow') {
        this.emit(`light.castsShadow = ${prop.value ? 'true' : 'false'}`);
      }
    }

    this.emit('');
    this.emit('let node = SCNNode()');
    this.emit(`node.name = "${this.escapeStringValue(light.name as string, 'Swift')}"`);
    this.emit('node.light = light');

    const posProp = light.properties.find((p) => p.key === 'position');
    if (posProp && Array.isArray(posProp.value)) {
      const pos = posProp.value as number[];
      this.emit(`node.position = SCNVector3(${pos[0]}, ${pos[1]}, ${pos[2]})`);
    }

    this.emit('');
    this.emit('return node');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
  }

  private compileAudioFactory(audio: HoloAudio): void {
    const varName = this.sanitizeName(audio.name);

    this.emit(`static func make${varName}() -> SCNAudioSource? {`);
    this.indentLevel++;

    const srcProp = audio.properties.find((p) => p.key === 'src' || p.key === 'source');
    if (srcProp) {
      this.emit(
        `guard let audioSource = SCNAudioSource(named: "${srcProp.value}") else { return nil }`
      );
    } else {
      this.emit('return nil');
      this.indentLevel--;
      this.emit('}');
      return;
    }

    const loopProp = audio.properties.find((p) => p.key === 'loop');
    if (loopProp) {
      this.emit(`audioSource.loops = ${loopProp.value ? 'true' : 'false'}`);
    }

    const volumeProp = audio.properties.find((p) => p.key === 'volume');
    if (volumeProp) {
      this.emit(`audioSource.volume = ${volumeProp.value}`);
    }

    this.emit('audioSource.isPositional = true');
    this.emit('audioSource.load()');
    this.emit('return audioSource');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
  }

  private compileAction(action: { name: string }): void {
    const name = this.sanitizeName(action.name);
    this.emit(`func ${name}() {`);
    this.indentLevel++;
    this.emit(
      `print("[HoloScript] Action: ${this.escapeStringValue(action.name, 'Swift')}")`
    );
    this.emit('// Action implementation');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
  }

  // === Geo-Anchor Methods ===

  private hasGeoTraits(composition: HoloComposition): boolean {
    const geoTraitNames = [
      'geo_anchor', 'geo_persist', 'geo_radius', 'geo_compass_heading',
      'geo_altitude', 'geo_cloud_anchor', 'geo_terrain_snap',
      'geo_session_continuity', 'geo_proximity_trigger', 'geo_discoverable',
      'geo_arcore_geospatial', 'geo_arkit_geo_anchor',
    ];
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (geoTraitNames.includes(name)) return true;
      }
    }
    return false;
  }

  private compositionHasTrait(composition: HoloComposition, traitName: string): boolean {
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (name === traitName) return true;
      }
    }
    return false;
  }

  private emitGeoAnchorMethods(composition: HoloComposition): void {
    this.emit('');
    this.emit('// === Geo-Anchor: GPS-pinned persistent holograms ===');
    this.emit('private let locationManager = CLLocationManager()');
    this.emit('var geoAnchors: [String: ARAnchor] = [:]');
    this.emit('');

    // setupGeoAnchors
    this.emit('func setupGeoAnchors() {');
    this.indentLevel++;
    this.emit('locationManager.requestWhenInUseAuthorization()');
    this.emit('locationManager.startUpdatingLocation()');
    this.emit('');

    const usesARGeoAnchor = this.compositionHasTrait(composition, 'geo_arkit_geo_anchor');
    if (usesARGeoAnchor) {
      this.emit('// Check ARGeoTrackingConfiguration availability');
      this.emit('ARGeoTrackingConfiguration.checkAvailability { available, error in');
      this.indentLevel++;
      this.emit('if available {');
      this.indentLevel++;
      this.emit('print("[HoloScript] ARGeoTracking available")');
      this.indentLevel--;
      this.emit('} else {');
      this.indentLevel++;
      this.emit('print("[HoloScript] ARGeoTracking not available: \\(error?.localizedDescription ?? \\"unknown\\")")');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Emit anchor creation for geo_anchor objects
    for (const obj of composition.objects || []) {
      const traits = obj.traits || [];
      const hasGeoAnchor = traits.some((t) => {
        const n = typeof t === 'string' ? t : t.name;
        return n === 'geo_anchor';
      });
      if (!hasGeoAnchor) continue;

      const geoAnchorTrait = traits.find((t) => {
        const n = typeof t === 'string' ? t : t.name;
        return n === 'geo_anchor';
      });
      const config = typeof geoAnchorTrait === 'string' ? {} : (geoAnchorTrait?.config || {});
      const lat = (config as Record<string, unknown>).latitude ?? 0.0;
      const lng = (config as Record<string, unknown>).longitude ?? 0.0;

      const altTrait = traits.find((t) => {
        const n = typeof t === 'string' ? t : t.name;
        return n === 'geo_altitude';
      });
      const altConfig = typeof altTrait === 'string' ? {} : (altTrait?.config || {});
      const alt = (altConfig as Record<string, unknown>).meters ?? 0.0;

      const headingTrait = traits.find((t) => {
        const n = typeof t === 'string' ? t : t.name;
        return n === 'geo_compass_heading';
      });
      const headingConfig = typeof headingTrait === 'string' ? {} : (headingTrait?.config || {});
      const heading = (headingConfig as Record<string, unknown>).degrees ?? 0.0;

      this.emit(`// Geo-anchor: ${this.escapeStringValue(obj.name, 'Swift')}`);
      if (usesARGeoAnchor) {
        this.emit(`createGeoAnchor(name: "${this.escapeStringValue(obj.name, 'Swift')}", latitude: ${lat}, longitude: ${lng}, altitude: ${alt}, heading: ${heading})`);
      } else {
        this.emit(`createLocationAnchor(name: "${this.escapeStringValue(obj.name, 'Swift')}", latitude: ${lat}, longitude: ${lng}, altitude: ${alt})`);
      }
    }

    // Restore persisted anchors
    const usesPersist = this.compositionHasTrait(composition, 'geo_persist');
    if (usesPersist) {
      this.emit('');
      this.emit('// Restore persisted ARWorldMap');
      this.emit('restoreWorldMap()');
    }

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // createGeoAnchor (ARGeoAnchor for iOS 14+)
    if (usesARGeoAnchor) {
      this.emit('func createGeoAnchor(name: String, latitude: Double, longitude: Double, altitude: Double, heading: Double) {');
      this.indentLevel++;
      this.emit('guard let arView = arView else { return }');
      this.emit('let coordinate = CLLocationCoordinate2D(latitude: latitude, longitude: longitude)');
      this.emit('let geoAnchor = ARGeoAnchor(coordinate: coordinate, altitude: altitude)');
      this.emit('geoAnchor.name = name');
      this.emit('arView.session.add(anchor: geoAnchor)');
      this.emit('geoAnchors[name] = geoAnchor');
      this.emit('print("[HoloScript] Geo-anchored: \\(name) at (\\(latitude), \\(longitude), \\(altitude))")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // createLocationAnchor (fallback using CLLocation + local anchor)
    this.emit('func createLocationAnchor(name: String, latitude: Double, longitude: Double, altitude: Double) {');
    this.indentLevel++;
    this.emit('guard let arView = arView else { return }');
    this.emit('let _ = CLLocation(latitude: latitude, longitude: longitude)');
    this.emit('');
    this.emit('// Create anchor at current camera position as placeholder');
    this.emit('guard let frame = arView.session.currentFrame else { return }');
    this.emit('var translation = matrix_identity_float4x4');
    this.emit('translation.columns.3.z = -1.0 // 1 meter in front');
    this.emit('let transform = simd_mul(frame.camera.transform, translation)');
    this.emit('let anchor = ARAnchor(name: name, transform: transform)');
    this.emit('arView.session.add(anchor: anchor)');
    this.emit('geoAnchors[name] = anchor');
    this.emit('print("[HoloScript] Location-anchored: \\(name) at (\\(latitude), \\(longitude))")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ARWorldMap persistence
    if (usesPersist) {
      this.emit('func saveWorldMap() {');
      this.indentLevel++;
      this.emit('guard let arView = arView else { return }');
      this.emit('arView.session.getCurrentWorldMap { worldMap, error in');
      this.indentLevel++;
      this.emit('guard let map = worldMap else {');
      this.indentLevel++;
      this.emit('print("[HoloScript] Failed to get world map: \\(error?.localizedDescription ?? \\"unknown\\")")');
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('do {');
      this.indentLevel++;
      this.emit('let data = try NSKeyedArchiver.archivedData(withRootObject: map, requiringSecureCoding: true)');
      this.emit('let url = self.worldMapURL()');
      this.emit('try data.write(to: url)');
      this.emit('print("[HoloScript] World map saved to \\(url)")');
      this.indentLevel--;
      this.emit('} catch {');
      this.indentLevel++;
      this.emit('print("[HoloScript] Failed to save world map: \\(error)")');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');

      this.emit('func restoreWorldMap() {');
      this.indentLevel++;
      this.emit('let url = worldMapURL()');
      this.emit('guard let data = try? Data(contentsOf: url),');
      this.emit('      let worldMap = try? NSKeyedUnarchiver.unarchivedObject(ofClass: ARWorldMap.self, from: data) else {');
      this.indentLevel++;
      this.emit('print("[HoloScript] No saved world map found")');
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('guard let arView = arView else { return }');
      this.emit('let configuration = ARWorldTrackingConfiguration()');
      this.emit('configuration.planeDetection = [.horizontal, .vertical]');
      this.emit('configuration.initialWorldMap = worldMap');
      this.emit('arView.session.run(configuration, options: [.resetTracking, .removeExistingAnchors])');
      this.emit('print("[HoloScript] World map restored with \\(worldMap.anchors.count) anchors")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');

      this.emit('private func worldMapURL() -> URL {');
      this.indentLevel++;
      this.emit('let documentsPath = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask).first!');
      this.emit('return documentsPath.appendingPathComponent("holoscript_worldmap.arworldmap")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }
  }

  // === RoomPlan (iOS 16+) ===

  /**
   * Check whether the composition references any roomplan_* traits.
   */
  private hasRoomPlanTraits(composition: HoloComposition): boolean {
    const roomplanNames: ReadonlyArray<string> = ROOMPLAN_TRAITS;
    for (const obj of composition.objects || []) {
      if (obj.traits?.some((t) => roomplanNames.includes(t.name))) {
        return true;
      }
    }
    return false;
  }

  /**
   * Emit a standalone Swift file that wraps Apple's RoomPlan framework.
   *
   * The generated code:
   *   1. Imports RoomPlan (iOS 16+)
   *   2. Creates a RoomCaptureSession with RoomCaptureSessionDelegate
   *   3. Handles CapturedRoom → surfaces (walls, doors, windows, floors,
   *      ceilings, openings) and objects (furniture, appliances, fixtures)
   *   4. Maps each detected element to a HoloScript entity with semantic labels
   *   5. Outputs a .holo-compatible scene graph
   */
  private generateRoomPlanFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;

    this.emit('// Auto-generated by HoloScript IOSCompiler — RoomPlan integration');
    this.emit(
      `// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`
    );
    this.emit('// Requires iOS 16.0+, RoomPlan framework');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import RoomPlan');
    this.emit('import SceneKit');
    this.emit('import SwiftUI');
    this.emit('');

    // HoloEntity model
    this.emit(
      '/// A single entity in the HoloScript scene graph produced from a RoomPlan scan.'
    );
    this.emit('struct HoloEntity: Identifiable {');
    this.indentLevel++;
    this.emit('let id: UUID');
    this.emit('let label: String');
    this.emit('let category: String');
    this.emit('let position: simd_float3');
    this.emit('let dimensions: simd_float3');
    this.emit('let transform: simd_float4x4');
    this.emit('let traits: [String]');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // RoomPlanManager class
    this.emit(`class ${cls}RoomPlanManager: NSObject, ObservableObject {`);
    this.indentLevel++;
    this.emit('@Published var isScanning: Bool = false');
    this.emit('@Published var entities: [HoloEntity] = []');
    this.emit('@Published var capturedRoom: CapturedRoom?');
    this.emit('');
    this.emit('private var captureSession: RoomCaptureSession?');
    this.emit('private var sessionConfig: RoomCaptureSession.Configuration');
    this.emit('');

    // init
    this.emit('override init() {');
    this.indentLevel++;
    this.emit('self.sessionConfig = RoomCaptureSession.Configuration()');
    this.emit('super.init()');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // startScan
    this.emit('/// Begin a RoomPlan capture session.');
    this.emit('func startScan() {');
    this.indentLevel++;
    this.emit('guard RoomCaptureSession.isSupported else {');
    this.indentLevel++;
    this.emit('print("[HoloScript] RoomPlan is not supported on this device")');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('let session = RoomCaptureSession()');
    this.emit('session.delegate = self');
    this.emit('captureSession = session');
    this.emit('session.run(configuration: sessionConfig)');
    this.emit('isScanning = true');
    this.emit('print("[HoloScript] RoomPlan scan started")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // stopScan
    this.emit('/// Stop the active capture session.');
    this.emit('func stopScan() {');
    this.indentLevel++;
    this.emit('captureSession?.stop()');
    this.emit('isScanning = false');
    this.emit('print("[HoloScript] RoomPlan scan stopped")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Surface mapping
    this.emit('// MARK: - Surface mapping');
    this.emit('');
    this.emit(
      'private func mapSurface(_ surface: CapturedRoom.Surface) -> HoloEntity {'
    );
    this.indentLevel++;
    this.emit('let (label, category, traits) = surfaceMeta(surface)');
    this.emit('return HoloEntity(');
    this.indentLevel++;
    this.emit('id: surface.identifier,');
    this.emit('label: label,');
    this.emit('category: category,');
    this.emit('position: simd_make_float3(surface.transform.columns.3),');
    this.emit('dimensions: surface.dimensions,');
    this.emit('transform: surface.transform,');
    this.emit('traits: traits');
    this.indentLevel--;
    this.emit(')');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit(
      'private func surfaceMeta(_ s: CapturedRoom.Surface) -> (String, String, [String]) {'
    );
    this.indentLevel++;
    this.emit('switch s.category {');
    this.emit('case .wall:');
    this.indentLevel++;
    this.emit('return ("Wall", "structure", ["roomplan_wall"])');
    this.indentLevel--;
    this.emit('case .floor:');
    this.indentLevel++;
    this.emit('return ("Floor", "structure", ["roomplan_floor"])');
    this.indentLevel--;
    this.emit('case .ceiling:');
    this.indentLevel++;
    this.emit('return ("Ceiling", "structure", ["roomplan_ceiling"])');
    this.indentLevel--;
    this.emit('case .door:');
    this.indentLevel++;
    this.emit('return ("Door", "opening", ["roomplan_door"])');
    this.indentLevel--;
    this.emit('case .window:');
    this.indentLevel++;
    this.emit('return ("Window", "opening", ["roomplan_window"])');
    this.indentLevel--;
    this.emit('case .opening:');
    this.indentLevel++;
    this.emit('return ("Opening", "opening", ["roomplan_opening"])');
    this.indentLevel--;
    this.emit('default:');
    this.indentLevel++;
    this.emit('return ("Surface", "structure", ["roomplan_wall"])');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Object mapping
    this.emit('// MARK: - Object mapping');
    this.emit('');
    this.emit(
      'private func mapObject(_ object: CapturedRoom.Object) -> HoloEntity {'
    );
    this.indentLevel++;
    this.emit('let (label, traits) = objectMeta(object)');
    this.emit('return HoloEntity(');
    this.indentLevel++;
    this.emit('id: object.identifier,');
    this.emit('label: label,');
    this.emit('category: "furniture",');
    this.emit('position: simd_make_float3(object.transform.columns.3),');
    this.emit('dimensions: object.dimensions,');
    this.emit('transform: object.transform,');
    this.emit('traits: traits');
    this.indentLevel--;
    this.emit(')');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit(
      'private func objectMeta(_ o: CapturedRoom.Object) -> (String, [String]) {'
    );
    this.indentLevel++;
    this.emit('switch o.category {');
    this.emit('case .table:');
    this.indentLevel++;
    this.emit('return ("Table", ["roomplan_furniture", "roomplan_table"])');
    this.indentLevel--;
    this.emit('case .chair:');
    this.indentLevel++;
    this.emit('return ("Chair", ["roomplan_furniture", "roomplan_chair"])');
    this.indentLevel--;
    this.emit('case .sofa:');
    this.indentLevel++;
    this.emit('return ("Sofa", ["roomplan_furniture", "roomplan_sofa"])');
    this.indentLevel--;
    this.emit('case .bed:');
    this.indentLevel++;
    this.emit('return ("Bed", ["roomplan_furniture", "roomplan_bed"])');
    this.indentLevel--;
    this.emit('case .storage:');
    this.indentLevel++;
    this.emit('return ("Storage", ["roomplan_furniture", "roomplan_storage"])');
    this.indentLevel--;
    this.emit('case .fireplace:');
    this.indentLevel++;
    this.emit('return ("Fireplace", ["roomplan_furniture", "roomplan_fireplace"])');
    this.indentLevel--;
    this.emit('case .television:');
    this.indentLevel++;
    this.emit('return ("Television", ["roomplan_furniture", "roomplan_screen"])');
    this.indentLevel--;
    this.emit('case .toilet, .bathtub, .sink:');
    this.indentLevel++;
    this.emit('return ("Fixture", ["roomplan_furniture", "roomplan_fixture"])');
    this.indentLevel--;
    this.emit('case .oven, .dishwasher, .refrigerator, .washerDryer:');
    this.indentLevel++;
    this.emit('return ("Appliance", ["roomplan_furniture", "roomplan_appliance"])');
    this.indentLevel--;
    this.emit('default:');
    this.indentLevel++;
    this.emit('return ("Furniture", ["roomplan_furniture"])');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Process captured room
    this.emit('// MARK: - Room processing');
    this.emit('');
    this.emit('func processRoom(_ room: CapturedRoom) {');
    this.indentLevel++;
    this.emit('self.capturedRoom = room');
    this.emit('var result: [HoloEntity] = []');
    this.emit('');
    this.emit(
      'for surface in room.walls + room.doors + room.windows + room.openings {'
    );
    this.indentLevel++;
    this.emit('result.append(mapSurface(surface))');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('for surface in room.floors {');
    this.indentLevel++;
    this.emit('result.append(mapSurface(surface))');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('for object in room.objects {');
    this.indentLevel++;
    this.emit('result.append(mapObject(object))');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('self.entities = result');
    this.emit(
      'print("[HoloScript] Processed \\(result.count) entities from RoomPlan scan")'
    );
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Export to .holo
    this.emit('// MARK: - .holo export');
    this.emit('');
    this.emit(
      '/// Serialize captured entities to a HoloScript .holo scene string.'
    );
    this.emit('func exportToHolo() -> String {');
    this.indentLevel++;
    this.emit('var lines: [String] = []');
    this.emit('lines.append("composition RoomScan {")');
    this.emit('for entity in entities {');
    this.indentLevel++;
    this.emit(
      'lines.append("  object \\(entity.label)_\\(entity.id.uuidString.prefix(8)) {")'
    );
    this.emit(
      'lines.append("    position: [\\(entity.position.x), \\(entity.position.y), \\(entity.position.z)]")'
    );
    this.emit(
      'lines.append("    dimensions: [\\(entity.dimensions.x), \\(entity.dimensions.y), \\(entity.dimensions.z)]")'
    );
    this.emit(
      'lines.append("    category: \\\"\\(entity.category)\\\"")'
    );
    this.emit('for trait in entity.traits {');
    this.indentLevel++;
    this.emit('lines.append("    trait \\(trait)")');
    this.indentLevel--;
    this.emit('}');
    this.emit('lines.append("  }")');
    this.indentLevel--;
    this.emit('}');
    this.emit('lines.append("}")');
    this.emit('return lines.joined(separator: "\\n")');
    this.indentLevel--;
    this.emit('}');

    // Close manager class
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // RoomCaptureSessionDelegate extension
    this.emit(
      `extension ${cls}RoomPlanManager: RoomCaptureSessionDelegate {`
    );
    this.indentLevel++;

    this.emit(
      'func captureSession(_ session: RoomCaptureSession, didUpdate room: CapturedRoom) {'
    );
    this.indentLevel++;
    this.emit('DispatchQueue.main.async { [weak self] in');
    this.indentLevel++;
    this.emit('self?.processRoom(room)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit(
      'func captureSession(_ session: RoomCaptureSession, didEndWith data: CapturedRoomData, error: (any Error)?) {'
    );
    this.indentLevel++;
    this.emit('DispatchQueue.main.async { [weak self] in');
    this.indentLevel++;
    this.emit('self?.isScanning = false');
    this.emit('if let error = error {');
    this.indentLevel++;
    this.emit(
      'print("[HoloScript] RoomPlan error: \\(error.localizedDescription)")'
    );
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('let finalRoom = data.finalResults');
    this.emit('self?.processRoom(finalRoom)');
    this.emit('print("[HoloScript] RoomPlan scan complete")');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // SwiftUI scanning view
    this.emit(`struct ${cls}RoomPlanView: View {`);
    this.indentLevel++;
    this.emit(
      `@StateObject private var manager = ${cls}RoomPlanManager()`
    );
    this.emit('');
    this.emit('var body: some View {');
    this.indentLevel++;
    this.emit('VStack {');
    this.indentLevel++;
    this.emit('Text("Room Scanner")');
    this.emit('    .font(.title)');
    this.emit('');
    this.emit('if manager.isScanning {');
    this.indentLevel++;
    this.emit('Text("Scanning... Move around the room")');
    this.emit('    .foregroundColor(.secondary)');
    this.emit('Button("Stop Scan") { manager.stopScan() }');
    this.emit('    .buttonStyle(.borderedProminent)');
    this.emit('    .tint(.red)');
    this.indentLevel--;
    this.emit('} else {');
    this.indentLevel++;
    this.emit('Text("\\(manager.entities.count) entities detected")');
    this.emit('Button("Start Scan") { manager.startScan() }');
    this.emit('    .buttonStyle(.borderedProminent)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('List(manager.entities) { entity in');
    this.indentLevel++;
    this.emit('VStack(alignment: .leading) {');
    this.indentLevel++;
    this.emit('Text(entity.label).font(.headline)');
    this.emit(
      'Text(entity.category).font(.caption).foregroundColor(.secondary)'
    );
    this.emit(
      'Text(entity.traits.joined(separator: ", ")).font(.caption2)'
    );
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  // === LiDAR Scanner (iOS 15+, LiDAR hardware — M.010.02a) ===

  private hasLiDARTraits(composition: HoloComposition): boolean {
    const lidarNames: ReadonlyArray<string> = LIDAR_SCANNER_TRAITS;
    for (const obj of composition.objects || []) {
      if (obj.traits?.some((t) => lidarNames.includes(t.name))) return true;
    }
    return false;
  }

  private collectLiDARTraits(composition: HoloComposition): Set<string> {
    const lidarNames: ReadonlyArray<string> = LIDAR_SCANNER_TRAITS;
    const found = new Set<string>();
    for (const obj of composition.objects || []) {
      for (const t of obj.traits || []) {
        if (lidarNames.includes(t.name)) found.add(t.name);
      }
    }
    return found;
  }

  /**
   * Emit a standalone Swift file wrapping ARKit LiDAR scene reconstruction.
   *
   * Generated code:
   *   1. Imports ARKit, checks supportsSceneReconstruction(.meshWithClassification)
   *   2. Configures ARWorldTrackingConfiguration with mesh classification
   *   3. Processes ARMeshAnchor via ARSessionDelegate
   *   4. Extracts vertices, faces, normals, per-face classification
   *   5. Maps ARMeshClassification to HoloScript semantic labels
   *   6. Optionally: depth map, point cloud, mesh export, .holo conversion
   */
  private generateLiDARScannerFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;
    const cls = this.options.className;
    const traits = this.collectLiDARTraits(composition);

    this.emit('// Auto-generated by HoloScript IOSCompiler \u2014 LiDAR Scanner integration');
    this.emit(`// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`);
    this.emit('// Requires iOS 15.0+, device with LiDAR sensor');
    this.emit('// Do not edit manually \u2014 regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import ARKit');
    this.emit('import SceneKit');
    this.emit('import SwiftUI');
    if (traits.has('lidar_mesh_export') || traits.has('lidar_mesh_simplify')) {
      this.emit('import ModelIO');
    }
    this.emit('');

    // Data model
    this.emit('// MARK: - Data models');
    this.emit('');
    this.emit('/// A single entity in the HoloScript scene graph produced from a LiDAR scan.');
    this.emit('struct LiDARHoloEntity: Identifiable {');
    this.indentLevel++;
    this.emit('let id: UUID');
    this.emit('let label: String');
    this.emit('let classification: ARMeshClassification');
    this.emit('let vertices: [simd_float3]');
    this.emit('let normals: [simd_float3]');
    this.emit('let faces: [[UInt32]]');
    this.emit('let transform: simd_float4x4');
    this.emit('let traits: [String]');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Classification mapping
    this.emit('// MARK: - Classification mapping');
    this.emit('');
    this.emit('/// Map ARMeshClassification to HoloScript semantic labels.');
    this.emit('func holoLabel(for classification: ARMeshClassification) -> String {');
    this.indentLevel++;
    this.emit('switch classification {');
    this.emit('case .wall: return "wall"');
    this.emit('case .floor: return "floor"');
    this.emit('case .ceiling: return "ceiling"');
    this.emit('case .table: return "table"');
    this.emit('case .seat: return "seat"');
    this.emit('case .window: return "window"');
    this.emit('case .door: return "door"');
    this.emit('case .none: return "unclassified"');
    this.emit('@unknown default: return "unknown"');
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Manager class
    this.emit('// MARK: - LiDARScannerManager');
    this.emit('');
    this.emit(`class ${cls}LiDARManager: NSObject, ObservableObject {`);
    this.indentLevel++;
    this.emit('@Published var isScanning: Bool = false');
    this.emit('@Published var entities: [LiDARHoloEntity] = []');
    this.emit('@Published var meshAnchors: [ARMeshAnchor] = []');
    if (traits.has('lidar_point_cloud')) this.emit('@Published var pointCloud: [simd_float3] = []');
    if (traits.has('lidar_depth_map')) this.emit('@Published var depthMap: CVPixelBuffer?');
    if (traits.has('lidar_depth_confidence')) this.emit('@Published var confidenceMap: CVPixelBuffer?');
    this.emit('');
    this.emit('private var arSession: ARSession?');
    this.emit('');
    this.emit('override init() {');
    this.indentLevel++;
    this.emit('super.init()');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // startScan
    this.emit('/// Begin a LiDAR scanning session.');
    this.emit('func startScan() {');
    this.indentLevel++;
    this.emit('guard ARWorldTrackingConfiguration.supportsSceneReconstruction(.meshWithClassification) else {');
    this.indentLevel++;
    this.emit('print("[HoloScript] LiDAR scene reconstruction is not supported on this device")');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('let config = ARWorldTrackingConfiguration()');
    this.emit('config.sceneReconstruction = .meshWithClassification');
    this.emit('config.environmentTexturing = .automatic');
    if (traits.has('lidar_depth_map') || traits.has('lidar_depth_confidence')) {
      this.emit('config.frameSemantics.insert(.sceneDepth)');
    }
    this.emit('');
    this.emit('let session = ARSession()');
    this.emit('session.delegate = self');
    this.emit('session.run(config)');
    this.emit('arSession = session');
    this.emit('isScanning = true');
    this.emit('print("[HoloScript] LiDAR scan started")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // stopScan
    this.emit('/// Stop the active LiDAR session.');
    this.emit('func stopScan() {');
    this.indentLevel++;
    this.emit('arSession?.pause()');
    this.emit('isScanning = false');
    this.emit('print("[HoloScript] LiDAR scan stopped")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Mesh extraction
    this.emit('// MARK: - Mesh extraction');
    this.emit('');
    this.emit('/// Extract vertices, normals, and faces from an ARMeshGeometry.');
    this.emit('func extractGeometry(_ meshGeometry: ARMeshGeometry) -> (vertices: [simd_float3], normals: [simd_float3], faces: [[UInt32]]) {');
    this.indentLevel++;
    this.emit('var vertices: [simd_float3] = []');
    this.emit('var normals: [simd_float3] = []');
    this.emit('var faces: [[UInt32]] = []');
    this.emit('');
    this.emit('let vertexBuffer = meshGeometry.vertices');
    this.emit('for i in 0..<vertexBuffer.count {');
    this.indentLevel++;
    this.emit('let ptr = vertexBuffer.buffer.contents().advanced(by: vertexBuffer.offset + vertexBuffer.stride * i)');
    this.emit('let vertex = ptr.assumingMemoryBound(to: simd_float3.self).pointee');
    this.emit('vertices.append(vertex)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('let normalBuffer = meshGeometry.normals');
    this.emit('for i in 0..<normalBuffer.count {');
    this.indentLevel++;
    this.emit('let ptr = normalBuffer.buffer.contents().advanced(by: normalBuffer.offset + normalBuffer.stride * i)');
    this.emit('let normal = ptr.assumingMemoryBound(to: simd_float3.self).pointee');
    this.emit('normals.append(normal)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('let faceBuffer = meshGeometry.faces');
    this.emit('for i in 0..<faceBuffer.count {');
    this.indentLevel++;
    this.emit('var indices: [UInt32] = []');
    this.emit('for j in 0..<faceBuffer.indexCountPerPrimitive {');
    this.indentLevel++;
    this.emit('let ptr = faceBuffer.buffer.contents().advanced(by: faceBuffer.offset + (i * faceBuffer.indexCountPerPrimitive + j) * faceBuffer.bytesPerIndex)');
    this.emit('let index = ptr.assumingMemoryBound(to: UInt32.self).pointee');
    this.emit('indices.append(index)');
    this.indentLevel--;
    this.emit('}');
    this.emit('faces.append(indices)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('return (vertices, normals, faces)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Classification extraction
    if (traits.has('lidar_mesh_classification') || traits.has('lidar_mesh_to_holo')) {
      this.emit('// MARK: - Classification extraction');
      this.emit('');
      this.emit('/// Extract per-face classification from an ARMeshAnchor.');
      this.emit('func extractClassifications(_ anchor: ARMeshAnchor) -> [ARMeshClassification] {');
      this.indentLevel++;
      this.emit('guard let classBuffer = anchor.geometry.classification else { return [] }');
      this.emit('var classifications: [ARMeshClassification] = []');
      this.emit('for i in 0..<classBuffer.count {');
      this.indentLevel++;
      this.emit('let ptr = classBuffer.buffer.contents().advanced(by: classBuffer.offset + classBuffer.stride * i)');
      this.emit('let raw = ptr.assumingMemoryBound(to: UInt8.self).pointee');
      this.emit('let classification = ARMeshClassification(rawValue: Int(raw)) ?? .none');
      this.emit('classifications.append(classification)');
      this.indentLevel--;
      this.emit('}');
      this.emit('return classifications');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Process mesh anchor
    this.emit('// MARK: - Process mesh anchor');
    this.emit('');
    this.emit('/// Process an ARMeshAnchor into LiDARHoloEntities.');
    this.emit('func processMeshAnchor(_ anchor: ARMeshAnchor) -> [LiDARHoloEntity] {');
    this.indentLevel++;
    this.emit('let (vertices, normals, faces) = extractGeometry(anchor.geometry)');
    if (traits.has('lidar_mesh_classification') || traits.has('lidar_mesh_to_holo')) {
      this.emit('let classifications = extractClassifications(anchor)');
      this.emit('');
      this.emit('var grouped: [ARMeshClassification: (verts: [simd_float3], norms: [simd_float3], faces: [[UInt32]])] = [:]');
      this.emit('for (i, face) in faces.enumerated() {');
      this.indentLevel++;
      this.emit('let cls = i < classifications.count ? classifications[i] : .none');
      this.emit('if grouped[cls] == nil { grouped[cls] = ([], [], []) }');
      this.emit('grouped[cls]!.faces.append(face)');
      this.emit('for idx in face {');
      this.indentLevel++;
      this.emit('let vi = Int(idx)');
      this.emit('if vi < vertices.count { grouped[cls]!.verts.append(vertices[vi]) }');
      this.emit('if vi < normals.count { grouped[cls]!.norms.append(normals[vi]) }');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('var entities: [LiDARHoloEntity] = []');
      this.emit('for (classification, data) in grouped {');
      this.indentLevel++;
      this.emit('entities.append(LiDARHoloEntity(');
      this.indentLevel++;
      this.emit('id: UUID(),');
      this.emit('label: holoLabel(for: classification),');
      this.emit('classification: classification,');
      this.emit('vertices: data.verts,');
      this.emit('normals: data.norms,');
      this.emit('faces: data.faces,');
      this.emit('transform: anchor.transform,');
      this.emit('traits: ["lidar_mesh_classification", "lidar_\\(holoLabel(for: classification))"]');
      this.indentLevel--;
      this.emit('))');
      this.indentLevel--;
      this.emit('}');
      this.emit('return entities');
    } else {
      this.emit('return [LiDARHoloEntity(');
      this.indentLevel++;
      this.emit('id: UUID(),');
      this.emit('label: "mesh",');
      this.emit('classification: .none,');
      this.emit('vertices: vertices,');
      this.emit('normals: normals,');
      this.emit('faces: faces,');
      this.emit('transform: anchor.transform,');
      this.emit('traits: ["lidar_scan"]');
      this.indentLevel--;
      this.emit(')]');
    }
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // .holo export
    if (traits.has('lidar_mesh_to_holo')) {
      this.emit('// MARK: - .holo export');
      this.emit('');
      this.emit('/// Serialize captured LiDAR entities to a HoloScript .holo scene string.');
      this.emit('func exportToHolo() -> String {');
      this.indentLevel++;
      this.emit('var lines: [String] = []');
      this.emit('lines.append("composition LiDARScan {")');
      this.emit('for entity in entities {');
      this.indentLevel++;
      this.emit('lines.append("  object \\(entity.label)_\\(entity.id.uuidString.prefix(8)) {")');
      this.emit('lines.append("    classification: \\\"\\(entity.label)\\\"")');
      this.emit('lines.append("    vertex_count: \\(entity.vertices.count)")');
      this.emit('lines.append("    face_count: \\(entity.faces.count)")');
      this.emit('for trait in entity.traits {');
      this.indentLevel++;
      this.emit('lines.append("    trait \\(trait)")');
      this.indentLevel--;
      this.emit('}');
      this.emit('lines.append("  }")');
      this.indentLevel--;
      this.emit('}');
      this.emit('lines.append("}")');
      this.emit('return lines.joined(separator: "\\n")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Mesh export
    if (traits.has('lidar_mesh_export')) {
      this.emit('// MARK: - Mesh export (USDZ / OBJ)');
      this.emit('');
      this.emit('/// Export the scanned mesh as USDZ using ModelIO.');
      this.emit('func exportMeshAsUSDZ(to url: URL) throws {');
      this.indentLevel++;
      this.emit('let allocator = MDLMeshBufferDataAllocator()');
      this.emit('let asset = MDLAsset(bufferAllocator: allocator)');
      this.emit('for anchor in meshAnchors {');
      this.indentLevel++;
      this.emit('let mdlMesh = anchor.geometry.toMDLMesh(device: MTLCreateSystemDefaultDevice()!, allocator: allocator)');
      this.emit('asset.add(mdlMesh)');
      this.indentLevel--;
      this.emit('}');
      this.emit('try asset.export(to: url)');
      this.emit('print("[HoloScript] Exported LiDAR mesh to \\(url.path)")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('/// Export the scanned mesh as OBJ via manual vertex write.');
      this.emit('func exportMeshAsOBJ(to url: URL) throws {');
      this.indentLevel++;
      this.emit('var obj = "# HoloScript LiDAR Mesh Export\\n"');
      this.emit('var vertexOffset: UInt32 = 1');
      this.emit('for entity in entities {');
      this.indentLevel++;
      this.emit('obj += "# \\(entity.label)\\n"');
      this.emit('for v in entity.vertices { obj += "v \\(v.x) \\(v.y) \\(v.z)\\n" }');
      this.emit('for n in entity.normals { obj += "vn \\(n.x) \\(n.y) \\(n.z)\\n" }');
      this.emit('for face in entity.faces {');
      this.indentLevel++;
      this.emit('let indices = face.map { "\\($0 + vertexOffset)" }.joined(separator: " ")');
      this.emit('obj += "f \\(indices)\\n"');
      this.indentLevel--;
      this.emit('}');
      this.emit('vertexOffset += UInt32(entity.vertices.count)');
      this.indentLevel--;
      this.emit('}');
      this.emit('try obj.write(to: url, atomically: true, encoding: .utf8)');
      this.emit('print("[HoloScript] Exported LiDAR mesh as OBJ to \\(url.path)")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Mesh simplification
    if (traits.has('lidar_mesh_simplify')) {
      this.emit('// MARK: - Mesh simplification');
      this.emit('');
      this.emit('/// Decimate the mesh to reduce triangle count using ModelIO.');
      this.emit('func simplifyMesh(_ mdlMesh: MDLMesh, targetFaceCount: Int) -> MDLMesh {');
      this.indentLevel++;
      this.emit('let device = MTLCreateSystemDefaultDevice()!');
      this.emit('let allocator = MTKMeshBufferAllocator(device: device)');
      this.emit('let simplified = MDLMesh(meshBySubdividing: mdlMesh, submeshIndex: 0, subdivisionLevels: 0, allocator: allocator)');
      this.emit('print("[HoloScript] Mesh simplified to target face count: \\(targetFaceCount)")');
      this.emit('return simplified');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Close manager class
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ARSessionDelegate extension
    this.emit('// MARK: - ARSessionDelegate');
    this.emit('');
    this.emit(`extension ${cls}LiDARManager: ARSessionDelegate {`);
    this.indentLevel++;
    this.emit('func session(_ session: ARSession, didUpdate frame: ARFrame) {');
    this.indentLevel++;
    this.emit('DispatchQueue.main.async { [weak self] in');
    this.indentLevel++;
    if (traits.has('lidar_point_cloud')) {
      this.emit('// lidar_point_cloud: access raw feature points');
      this.emit('if let rawPoints = frame.rawFeaturePoints {');
      this.indentLevel++;
      this.emit('self?.pointCloud = (0..<rawPoints.count).map { rawPoints.points[$0] }');
      this.indentLevel--;
      this.emit('}');
    }
    if (traits.has('lidar_depth_map')) {
      this.emit('// lidar_depth_map: access sceneDepth depthMap');
      this.emit('self?.depthMap = frame.sceneDepth?.depthMap');
    }
    if (traits.has('lidar_depth_confidence')) {
      this.emit('// lidar_depth_confidence: access sceneDepth confidenceMap');
      this.emit('self?.confidenceMap = frame.sceneDepth?.confidenceMap');
    }
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    if (traits.has('lidar_realtime_mesh') || traits.has('lidar_mesh_capture') || traits.has('lidar_scan')) {
      this.emit('func session(_ session: ARSession, didAdd anchors: [ARAnchor]) {');
      this.indentLevel++;
      this.emit('processAnchors(anchors)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func session(_ session: ARSession, didUpdate anchors: [ARAnchor]) {');
      this.indentLevel++;
      this.emit('processAnchors(anchors)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('private func processAnchors(_ anchors: [ARAnchor]) {');
      this.indentLevel++;
      this.emit('DispatchQueue.main.async { [weak self] in');
      this.indentLevel++;
      this.emit('for anchor in anchors {');
      this.indentLevel++;
      this.emit('guard let meshAnchor = anchor as? ARMeshAnchor else { continue }');
      this.emit('self?.meshAnchors.removeAll { $0.identifier == meshAnchor.identifier }');
      this.emit('self?.meshAnchors.append(meshAnchor)');
      this.emit('let newEntities = self?.processMeshAnchor(meshAnchor) ?? []');
      this.emit('self?.entities.append(contentsOf: newEntities)');
      this.indentLevel--;
      this.emit('}');
      this.emit('print("[HoloScript] LiDAR mesh updated: \\(self?.meshAnchors.count ?? 0) anchors, \\(self?.entities.count ?? 0) entities")');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
    }

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // SwiftUI View
    this.emit('// MARK: - SwiftUI View');
    this.emit('');
    this.emit(`struct ${cls}LiDARScanView: View {`);
    this.indentLevel++;
    this.emit(`@StateObject private var manager = ${cls}LiDARManager()`);
    this.emit('');
    this.emit('var body: some View {');
    this.indentLevel++;
    this.emit('VStack {');
    this.indentLevel++;
    this.emit('Text("LiDAR Scanner")');
    this.emit('    .font(.title)');
    this.emit('');
    this.emit('if manager.isScanning {');
    this.indentLevel++;
    this.emit('Text("Scanning... Move around the space")');
    this.emit('    .foregroundColor(.secondary)');
    this.emit('Text("\\(manager.meshAnchors.count) mesh anchors")');
    this.emit('    .font(.caption)');
    this.emit('Button("Stop Scan") { manager.stopScan() }');
    this.emit('    .buttonStyle(.borderedProminent)');
    this.emit('    .tint(.red)');
    this.indentLevel--;
    this.emit('} else {');
    this.indentLevel++;
    this.emit('Text("\\(manager.entities.count) entities detected")');
    this.emit('Button("Start Scan") { manager.startScan() }');
    this.emit('    .buttonStyle(.borderedProminent)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('List(manager.entities) { entity in');
    this.indentLevel++;
    this.emit('VStack(alignment: .leading) {');
    this.indentLevel++;
    this.emit('Text(entity.label).font(.headline)');
    this.emit('Text("Vertices: \\(entity.vertices.count), Faces: \\(entity.faces.count)")');
    this.emit('    .font(.caption)');
    this.emit('    .foregroundColor(.secondary)');
    this.emit('Text(entity.traits.joined(separator: ", ")).font(.caption2)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  // === NPU Scene Understanding (M.010.03) ===

  /**
   * Check whether the composition references any npu_* scene understanding traits.
   */
  private hasNPUSceneTraits(composition: HoloComposition): boolean {
    const npuNames: ReadonlyArray<string> = NPU_SCENE_TRAITS;
    for (const obj of composition.objects || []) {
      if (obj.traits?.some((t) => {
        const name = typeof t === 'string' ? t : t.name;
        return npuNames.includes(name);
      })) {
        return true;
      }
    }
    return false;
  }

  /**
   * Emit a standalone Swift file that wraps Apple's Vision + CoreML frameworks
   * for on-device NPU scene understanding.
   *
   * The generated code:
   *   1. Imports Vision, CoreML, ARKit
   *   2. NPUSceneManager class orchestrating Vision requests + AR integration
   *   3. Per-trait handlers: classify, detect, segment, depth, entity pipe,
   *      realtime, custom model, label overlay
   *   4. SwiftUI overlay showing detection results
   */
  private generateNPUSceneFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;
    const defaults = NPU_SCENE_DEFAULTS;

    this.emit('// Auto-generated by HoloScript IOSCompiler — NPU Scene Understanding');
    this.emit(
      `// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`
    );
    this.emit('// Requires iOS 15.0+, Vision + CoreML frameworks');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import Vision');
    this.emit('import CoreML');
    this.emit('import ARKit');
    this.emit('import SceneKit');
    this.emit('import SwiftUI');
    this.emit('');

    // NPUDetection model
    this.emit('/// A single detection result from NPU scene understanding.');
    this.emit('struct NPUDetection: Identifiable {');
    this.indentLevel++;
    this.emit('let id = UUID()');
    this.emit('let label: String');
    this.emit('let confidence: Float');
    this.emit('let boundingBox: CGRect?');
    this.emit('let position: simd_float3?');
    this.emit('let segmentationMask: CVPixelBuffer?');
    this.emit('let depthValue: Float?');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // NPUSceneManager class
    this.emit(`class ${cls}NPUSceneManager: NSObject, ObservableObject {`);
    this.indentLevel++;
    this.emit('@Published var detections: [NPUDetection] = []');
    this.emit('@Published var isProcessing: Bool = false');
    this.emit('@Published var inferenceCount: Int = 0');
    this.emit('');
    this.emit(`private let confidenceThreshold: Float = ${defaults.confidenceThreshold}`);
    this.emit(`private let maxDetections: Int = ${defaults.maxDetections}`);
    this.emit(`private let targetFPS: Int = ${defaults.targetFPS}`);
    this.emit(`private let entityScale: Float = ${defaults.entityScale}`);
    this.emit(`private let labelOffsetY: Float = ${defaults.labelOffsetY}`);
    this.emit('private var frameCounter: Int = 0');
    this.emit('private var lastProcessTime: TimeInterval = 0');
    this.emit('private var customModel: VNCoreMLModel?');
    this.emit('');

    // Collect which traits are used
    const usedTraits = new Set<string>();
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if ((NPU_SCENE_TRAITS as ReadonlyArray<string>).includes(name)) {
          usedTraits.add(name);
        }
      }
    }

    // --- npu_classify ---
    if (usedTraits.has('npu_classify')) {
      this.emit('// MARK: - Classification (npu_classify)');
      this.emit('');
      this.emit('func classifyImage(_ pixelBuffer: CVPixelBuffer) {');
      this.indentLevel++;
      this.emit('let request = VNClassifyImageRequest { [weak self] request, error in');
      this.indentLevel++;
      this.emit('guard let self = self, error == nil else { return }');
      this.emit('guard let results = request.results as? [VNClassificationObservation] else { return }');
      this.emit('let filtered = results.filter { $0.confidence >= self.confidenceThreshold }');
      this.emit('    .prefix(self.maxDetections)');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('self.detections = filtered.map { obs in');
      this.indentLevel++;
      this.emit('NPUDetection(label: obs.identifier, confidence: obs.confidence,');
      this.emit('             boundingBox: nil, position: nil, segmentationMask: nil, depthValue: nil)');
      this.indentLevel--;
      this.emit('}');
      this.emit('self.inferenceCount += 1');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('let handler = VNImageRequestHandler(cvPixelBuffer: pixelBuffer, options: [:])');
      this.emit('try? handler.perform([request])');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // --- npu_detect ---
    if (usedTraits.has('npu_detect')) {
      this.emit('// MARK: - Object Detection (npu_detect)');
      this.emit('');
      this.emit('func detectObjects(_ pixelBuffer: CVPixelBuffer) {');
      this.indentLevel++;
      this.emit('let request: VNImageBasedRequest');
      this.emit('if let model = customModel {');
      this.indentLevel++;
      this.emit('request = VNCoreMLRequest(model: model) { [weak self] req, error in');
      this.indentLevel++;
      this.emit('self?.handleDetectionResults(req.results)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('} else {');
      this.indentLevel++;
      this.emit('request = VNRecognizeAnimalsRequest { [weak self] req, error in');
      this.indentLevel++;
      this.emit('self?.handleDetectionResults(req.results)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('let handler = VNImageRequestHandler(cvPixelBuffer: pixelBuffer, options: [:])');
      this.emit('try? handler.perform([request])');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('private func handleDetectionResults(_ results: [VNObservation]?) {');
      this.indentLevel++;
      this.emit('guard let observations = results as? [VNRecognizedObjectObservation] else { return }');
      this.emit('let filtered = observations.filter { $0.confidence >= confidenceThreshold }');
      this.emit('    .prefix(maxDetections)');
      this.emit('DispatchQueue.main.async { [weak self] in');
      this.indentLevel++;
      this.emit('self?.detections = filtered.map { obs in');
      this.indentLevel++;
      this.emit('let topLabel = obs.labels.first?.identifier ?? "unknown"');
      this.emit('return NPUDetection(label: topLabel, confidence: obs.confidence,');
      this.emit('                    boundingBox: obs.boundingBox, position: nil,');
      this.emit('                    segmentationMask: nil, depthValue: nil)');
      this.indentLevel--;
      this.emit('}');
      this.emit('self?.inferenceCount += 1');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // --- npu_segment ---
    if (usedTraits.has('npu_segment')) {
      this.emit('// MARK: - Segmentation (npu_segment)');
      this.emit('');
      this.emit('func segmentScene(_ pixelBuffer: CVPixelBuffer) {');
      this.indentLevel++;
      this.emit('let request = VNGeneratePersonSegmentationRequest()');
      this.emit('request.qualityLevel = .balanced');
      this.emit('request.outputPixelFormat = kCVPixelFormatType_OneComponent8');
      this.emit('let handler = VNImageRequestHandler(cvPixelBuffer: pixelBuffer, options: [:])');
      this.emit('try? handler.perform([request])');
      this.emit('guard let result = request.results?.first else { return }');
      this.emit('DispatchQueue.main.async { [weak self] in');
      this.indentLevel++;
      this.emit('self?.detections = [NPUDetection(');
      this.emit('    label: "person_segmentation", confidence: 1.0,');
      this.emit('    boundingBox: nil, position: nil,');
      this.emit('    segmentationMask: result.pixelBuffer, depthValue: nil');
      this.emit(')]');
      this.emit('self?.inferenceCount += 1');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // --- npu_depth ---
    if (usedTraits.has('npu_depth')) {
      this.emit('// MARK: - Depth Estimation (npu_depth)');
      this.emit('');
      this.emit('func estimateDepth(_ frame: ARFrame) {');
      this.indentLevel++;
      this.emit('// Prefer ARKit scene depth when LiDAR is available');
      this.emit('if let sceneDepth = frame.sceneDepth {');
      this.indentLevel++;
      this.emit('DispatchQueue.main.async { [weak self] in');
      this.indentLevel++;
      this.emit('self?.detections = [NPUDetection(');
      this.emit('    label: "depth_map", confidence: 1.0,');
      this.emit('    boundingBox: nil, position: nil,');
      this.emit('    segmentationMask: sceneDepth.depthMap, depthValue: nil');
      this.emit(')]');
      this.emit('self?.inferenceCount += 1');
      this.indentLevel--;
      this.emit('}');
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('// Fallback: monocular depth estimation via Vision');
      this.emit('if #available(iOS 18.0, *) {');
      this.indentLevel++;
      this.emit('let request = VNGenerateDepthRequest()');
      this.emit('let handler = VNImageRequestHandler(cvPixelBuffer: frame.capturedImage, options: [:])');
      this.emit('try? handler.perform([request])');
      this.emit('if let result = request.results?.first {');
      this.indentLevel++;
      this.emit('DispatchQueue.main.async { [weak self] in');
      this.indentLevel++;
      this.emit('self?.detections = [NPUDetection(');
      this.emit('    label: "depth_map", confidence: 1.0,');
      this.emit('    boundingBox: nil, position: nil,');
      this.emit('    segmentationMask: result.depthMap, depthValue: nil');
      this.emit(')]');
      this.emit('self?.inferenceCount += 1');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // --- npu_entity_pipe ---
    if (usedTraits.has('npu_entity_pipe')) {
      this.emit('// MARK: - Entity Pipeline (npu_entity_pipe)');
      this.emit('');
      this.emit('func mapDetectionsToEntities(_ detections: [NPUDetection], in sceneView: ARSCNView) -> [SCNNode] {');
      this.indentLevel++;
      this.emit('var nodes: [SCNNode] = []');
      this.emit('for detection in detections {');
      this.indentLevel++;
      this.emit('guard let bbox = detection.boundingBox else { continue }');
      this.emit('let center = CGPoint(x: bbox.midX, y: bbox.midY)');
      this.emit('guard let query = sceneView.raycastQuery(from: center, allowing: .estimatedPlane, alignment: .any),');
      this.emit('      let result = sceneView.session.raycast(query).first else { continue }');
      this.emit('let anchor = ARAnchor(name: detection.label, transform: result.worldTransform)');
      this.emit('sceneView.session.add(anchor: anchor)');
      this.emit('let node = SCNNode()');
      this.emit('node.name = detection.label');
      this.emit(`node.simdWorldPosition = simd_make_float3(result.worldTransform.columns.3)`);
      this.emit(`let sphere = SCNSphere(radius: CGFloat(entityScale))`);
      this.emit('sphere.firstMaterial?.diffuse.contents = UIColor.systemBlue.withAlphaComponent(0.6)');
      this.emit('node.geometry = sphere');
      this.emit('nodes.append(node)');
      this.indentLevel--;
      this.emit('}');
      this.emit('return nodes');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // --- npu_realtime ---
    if (usedTraits.has('npu_realtime')) {
      this.emit('// MARK: - Realtime Processing (npu_realtime)');
      this.emit('');
      this.emit('func processFrame(_ frame: ARFrame) {');
      this.indentLevel++;
      this.emit('frameCounter += 1');
      this.emit('let now = frame.timestamp');
      this.emit('let interval = 1.0 / Double(targetFPS)');
      this.emit('guard now - lastProcessTime >= interval else { return }');
      this.emit('lastProcessTime = now');
      this.emit('isProcessing = true');
      this.emit('let pixelBuffer = frame.capturedImage');
      // Call whichever processing methods are active
      if (usedTraits.has('npu_classify')) {
        this.emit('classifyImage(pixelBuffer)');
      }
      if (usedTraits.has('npu_detect')) {
        this.emit('detectObjects(pixelBuffer)');
      }
      if (usedTraits.has('npu_segment')) {
        this.emit('segmentScene(pixelBuffer)');
      }
      if (usedTraits.has('npu_depth')) {
        this.emit('estimateDepth(frame)');
      }
      this.emit('isProcessing = false');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // --- npu_model_custom ---
    if (usedTraits.has('npu_model_custom')) {
      this.emit('// MARK: - Custom Model (npu_model_custom)');
      this.emit('');
      this.emit('func loadCustomModel(at url: URL) throws {');
      this.indentLevel++;
      this.emit('let mlModel = try MLModel(contentsOf: url)');
      this.emit('customModel = try VNCoreMLModel(for: mlModel)');
      this.emit('print("[HoloScript] Custom ML model loaded: \\(url.lastPathComponent)")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func loadCustomModel(named name: String) throws {');
      this.indentLevel++;
      this.emit('guard let url = Bundle.main.url(forResource: name, withExtension: "mlmodelc") else {');
      this.indentLevel++;
      this.emit('throw NSError(domain: "HoloScript", code: -1, userInfo: [NSLocalizedDescriptionKey: "Model \\(name) not found"])');
      this.indentLevel--;
      this.emit('}');
      this.emit('try loadCustomModel(at: url)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // --- npu_label_overlay ---
    if (usedTraits.has('npu_label_overlay')) {
      this.emit('// MARK: - Label Overlay (npu_label_overlay)');
      this.emit('');
      this.emit('func createLabelNode(for detection: NPUDetection) -> SCNNode {');
      this.indentLevel++;
      this.emit('let text = SCNText(string: "\\(detection.label) (\\(String(format: "%.0f%%", detection.confidence * 100)))", extrusionDepth: 0.5)');
      this.emit('text.font = UIFont.systemFont(ofSize: 4, weight: .bold)');
      this.emit('text.firstMaterial?.diffuse.contents = UIColor.white');
      this.emit('text.firstMaterial?.isDoubleSided = true');
      this.emit('let node = SCNNode(geometry: text)');
      this.emit('node.scale = SCNVector3(0.005, 0.005, 0.005)');
      this.emit(`node.position.y += labelOffsetY`);
      this.emit('// Billboard constraint: always face camera');
      this.emit('let billboard = SCNBillboardConstraint()');
      this.emit('billboard.freeAxes = .Y');
      this.emit('node.constraints = [billboard]');
      this.emit('return node');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Close manager class
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // SwiftUI overlay view
    this.emit('// MARK: - SwiftUI Overlay');
    this.emit('');
    this.emit(`struct ${cls}NPUOverlayView: View {`);
    this.indentLevel++;
    this.emit(`@ObservedObject var manager: ${cls}NPUSceneManager`);
    this.emit('');
    this.emit('var body: some View {');
    this.indentLevel++;
    this.emit('VStack {');
    this.indentLevel++;
    this.emit('HStack {');
    this.indentLevel++;
    this.emit('if manager.isProcessing {');
    this.indentLevel++;
    this.emit('ProgressView()');
    this.emit('    .progressViewStyle(CircularProgressViewStyle(tint: .white))');
    this.indentLevel--;
    this.emit('}');
    this.emit('Text("NPU: \\(manager.detections.count) detections")');
    this.emit('    .font(.caption)');
    this.emit('    .foregroundColor(.white)');
    this.emit('    .padding(6)');
    this.emit('    .background(Color.black.opacity(0.6))');
    this.emit('    .cornerRadius(8)');
    this.emit('Spacer()');
    this.indentLevel--;
    this.emit('}');
    this.emit('.padding(.horizontal)');
    this.emit('Spacer()');
    this.emit('// Detection list');
    this.emit('ScrollView(.horizontal) {');
    this.indentLevel++;
    this.emit('HStack(spacing: 8) {');
    this.indentLevel++;
    this.emit('ForEach(manager.detections) { detection in');
    this.indentLevel++;
    this.emit('VStack {');
    this.indentLevel++;
    this.emit('Text(detection.label)');
    this.emit('    .font(.caption2)');
    this.emit('    .bold()');
    this.emit('Text(String(format: "%.0f%%", detection.confidence * 100))');
    this.emit('    .font(.caption2)');
    this.emit('    .foregroundColor(.secondary)');
    this.indentLevel--;
    this.emit('}');
    this.emit('.padding(6)');
    this.emit('.background(Color.black.opacity(0.7))');
    this.emit('.cornerRadius(6)');
    this.emit('.foregroundColor(.white)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('.padding(.bottom, 20)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  // === Utility Methods ===

  private emit(line: string): void {
    const indent = this.options.indent.repeat(this.indentLevel);
    this.lines.push(indent + line);
  }

  private sanitizeName(name: string): string {
    const result = name.replace(/[^a-zA-Z0-9_]/g, '_').replace(/^[0-9]/, '_$&');
    return result.charAt(0).toUpperCase() + result.slice(1);
  }

  private getSceneKitGeometry(meshType: string): string {
    const geometries: Record<string, string> = {
      cube: 'SCNBox(width: 0.1, height: 0.1, length: 0.1, chamferRadius: 0)',
      box: 'SCNBox(width: 0.1, height: 0.1, length: 0.1, chamferRadius: 0)',
      sphere: 'SCNSphere(radius: 0.05)',
      cylinder: 'SCNCylinder(radius: 0.05, height: 0.1)',
      cone: 'SCNCone(topRadius: 0, bottomRadius: 0.05, height: 0.1)',
      capsule: 'SCNCapsule(capRadius: 0.025, height: 0.1)',
      plane: 'SCNPlane(width: 0.2, height: 0.2)',
      torus: 'SCNTorus(ringRadius: 0.05, pipeRadius: 0.02)',
    };
    return geometries[meshType] || 'SCNBox(width: 0.1, height: 0.1, length: 0.1, chamferRadius: 0)';
  }

  private findObjProp(obj: HoloObjectDecl, key: string): HoloValue | undefined {
    return obj.properties?.find((p) => p.key === key)?.value;
  }

  private toSwiftType(value: HoloValue): string {
    if (value === null) return 'Any?';
    if (typeof value === 'boolean') return 'Bool';
    if (typeof value === 'number') return Number.isInteger(value) ? 'Int' : 'Double';
    if (typeof value === 'string') return 'String';
    if (Array.isArray(value)) {
      if (value.length === 3 && value.every((v) => typeof v === 'number')) return 'SCNVector3';
      return '[Any]';
    }
    return 'Any';
  }

  private toSwiftValue(value: HoloValue): string {
    if (value === null) return 'nil';
    if (typeof value === 'boolean') return value ? 'true' : 'false';
    if (typeof value === 'number') return `${value}`;
    if (typeof value === 'string') return `"${this.escapeStringValue(value, 'Swift')}"`;
    if (Array.isArray(value)) {
      if (value.length === 3 && value.every((v) => typeof v === 'number')) {
        return `SCNVector3(${value[0]}, ${value[1]}, ${value[2]})`;
      }
      return `[${value.map((v) => this.toSwiftValue(v)).join(', ')}]`;
    }
    return 'nil';
  }

  private toUIColor(value: HoloValue): string {
    if (typeof value === 'string') {
      if (value.startsWith('#')) {
        const hex = value.slice(1);
        if (hex.length === 6) {
          const r = parseInt(hex.slice(0, 2), 16) / 255;
          const g = parseInt(hex.slice(2, 4), 16) / 255;
          const b = parseInt(hex.slice(4, 6), 16) / 255;
          return `UIColor(red: ${r}, green: ${g}, blue: ${b}, alpha: 1.0)`;
        }
      }
      const colors: Record<string, string> = {
        red: 'UIColor.systemRed',
        green: 'UIColor.systemGreen',
        blue: 'UIColor.systemBlue',
        white: 'UIColor.white',
        black: 'UIColor.black',
        yellow: 'UIColor.systemYellow',
        cyan: 'UIColor.systemCyan',
        magenta: 'UIColor.systemPink',
      };
      return colors[value.toLowerCase()] || 'UIColor.systemBlue';
    }
    if (Array.isArray(value) && value.length >= 3) {
      const [r, g, b, a = 1] = value as number[];
      return `UIColor(red: ${r}, green: ${g}, blue: ${b}, alpha: ${a})`;
    }
    return 'UIColor.systemBlue';
  }

  // === Portal AR Methods (M.010.06) ===

  private hasPortalARTraits(composition: HoloComposition): boolean {
    const portalNames: ReadonlyArray<string> = PORTAL_AR_TRAITS;
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (portalNames.includes(name)) return true;
      }
    }
    return false;
  }

  private generatePortalARFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;

    this.emit('// Auto-generated by HoloScript IOSCompiler — Portal AR integration');
    this.emit(
      `// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`
    );
    this.emit('// Requires iOS 15.0+, ARKit with scene reconstruction');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import ARKit');
    this.emit('import SceneKit');
    this.emit('import RealityKit');
    this.emit('import Metal');
    this.emit('');

    this.emit(`class ${cls}PortalARManager: NSObject, ARSessionDelegate {`);
    this.indentLevel++;
    this.emit('');
    this.emit('private var arSession: ARSession');
    this.emit('private var portalScene: SCNScene');
    this.emit('private var holographicLayer: SCNNode');
    this.emit('private var portalEnabled: Bool = true');
    this.emit('');

    // Init
    this.emit('init(session: ARSession) {');
    this.indentLevel++;
    this.emit('self.arSession = session');
    this.emit('self.portalScene = SCNScene()');
    this.emit('self.holographicLayer = SCNNode()');
    this.emit('super.init()');
    this.emit('session.delegate = self');
    this.emit('configure()');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Configure ARKit session
    this.emit('private func configure() {');
    this.indentLevel++;
    this.emit('let config = ARWorldTrackingConfiguration()');
    this.emit('config.planeDetection = [.horizontal, .vertical]');

    // Scene reconstruction for world mesh
    if (this.compositionHasTrait(composition, 'portal_world_mesh') ||
        this.compositionHasTrait(composition, 'portal_mesh_occlusion')) {
      this.emit('');
      this.emit('// Scene reconstruction for portal world mesh');
      this.emit('if ARWorldTrackingConfiguration.supportsSceneReconstruction(.mesh) {');
      this.indentLevel++;
      this.emit('config.sceneReconstruction = .mesh');
      this.indentLevel--;
      this.emit('}');
    }

    // Depth
    this.emit('');
    this.emit('// Enable frame semantics for depth');
    this.emit('if ARWorldTrackingConfiguration.supportsFrameSemantics(.sceneDepth) {');
    this.indentLevel++;
    this.emit('config.frameSemantics.insert(.sceneDepth)');
    this.indentLevel--;
    this.emit('}');

    // People occlusion
    if (this.compositionHasTrait(composition, 'portal_people_occlusion')) {
      this.emit('');
      this.emit('// People occlusion via ARKit person segmentation');
      this.emit('if ARWorldTrackingConfiguration.supportsFrameSemantics(.personSegmentationWithDepth) {');
      this.indentLevel++;
      this.emit('config.frameSemantics.insert(.personSegmentationWithDepth)');
      this.indentLevel--;
      this.emit('}');
    }

    // Environment probe for lighting match
    if (this.compositionHasTrait(composition, 'portal_lighting_match')) {
      this.emit('');
      this.emit('// Environment probe for portal lighting match');
      this.emit('config.environmentTexturing = .automatic');
    }

    this.emit('');
    this.emit('arSession.run(config)');
    this.emit('print("[HoloScript] Portal AR configured")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Create holographic scene layer
    this.emit('func createHolographicLayer() -> SCNNode {');
    this.indentLevel++;
    this.emit('holographicLayer.name = "HoloScript_PortalLayer"');
    this.emit('return holographicLayer');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Portal occlusion via depth
    if (this.compositionHasTrait(composition, 'portal_occlusion')) {
      this.emit('// Portal occlusion: real-world objects block holograms via ARKit depth');
      this.emit('func updateDepthOcclusion(frame: ARFrame) {');
      this.indentLevel++;
      this.emit('guard let sceneDepth = frame.sceneDepth else { return }');
      this.emit('let depthMap = sceneDepth.depthMap');
      this.emit('// Apply depth-based occlusion to holographic layer');
      this.emit('for child in holographicLayer.childNodes {');
      this.indentLevel++;
      this.emit('let screenPoint = frame.camera.projectPoint(child.worldPosition, orientation: .portrait, viewportSize: CGSize(width: 1, height: 1))');
      this.emit('child.isHidden = isOccluded(at: screenPoint, depthMap: depthMap, nodeDepth: child.worldPosition.z)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');

      this.emit('private func isOccluded(at point: CGPoint, depthMap: CVPixelBuffer, nodeDepth: Float) -> Bool {');
      this.indentLevel++;
      this.emit('CVPixelBufferLockBaseAddress(depthMap, .readOnly)');
      this.emit('defer { CVPixelBufferUnlockBaseAddress(depthMap, .readOnly) }');
      this.emit('let width = CVPixelBufferGetWidth(depthMap)');
      this.emit('let height = CVPixelBufferGetHeight(depthMap)');
      this.emit('let x = Int(point.x * CGFloat(width))');
      this.emit('let y = Int(point.y * CGFloat(height))');
      this.emit('guard x >= 0, x < width, y >= 0, y < height else { return false }');
      this.emit('let baseAddress = CVPixelBufferGetBaseAddress(depthMap)!');
      this.emit('let buffer = baseAddress.assumingMemoryBound(to: Float32.self)');
      this.emit('let realWorldDepth = buffer[y * width + x]');
      this.emit('return realWorldDepth < nodeDepth');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // People occlusion via matteGenerator
    if (this.compositionHasTrait(composition, 'portal_people_occlusion')) {
      this.emit('// People occlusion via ARKit person segmentation (matteGenerator)');
      this.emit('func updatePeopleOcclusion(frame: ARFrame) {');
      this.indentLevel++;
      this.emit('guard let segmentation = frame.segmentationBuffer else { return }');
      this.emit('guard let estimatedDepth = frame.estimatedDepthData else { return }');
      this.emit('// Use matteGenerator for alpha matte of people');
      this.emit('let matteGenerator = ARMatteGenerator(device: MTLCreateSystemDefaultDevice()!, matteResolution: .half)');
      this.emit('let matte = matteGenerator.generateMatte(from: frame, commandBuffer: MTLCreateSystemDefaultDevice()!.makeCommandQueue()!.makeCommandBuffer()!)');
      this.emit('// Apply people matte to holographic layer rendering');
      this.emit('portalScene.rootNode.setValue(matte, forKey: "peopleOcclusionMatte")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Portal parallax from ARCamera
    if (this.compositionHasTrait(composition, 'portal_parallax')) {
      this.emit('// Portal parallax: depth-correct parallax from ARCamera transform');
      this.emit('func applyParallaxCorrection(frame: ARFrame) {');
      this.indentLevel++;
      this.emit('let cameraTransform = frame.camera.transform');
      this.emit('let cameraPosition = SIMD3<Float>(cameraTransform.columns.3.x, cameraTransform.columns.3.y, cameraTransform.columns.3.z)');
      this.emit('// Offset holographic layer based on camera movement for parallax');
      this.emit('for child in holographicLayer.childNodes {');
      this.indentLevel++;
      this.emit('let depth = child.worldPosition.z');
      this.emit('let parallaxFactor: Float = 1.0 - (depth / 10.0)');
      this.emit('child.position.x += (cameraPosition.x - child.position.x) * parallaxFactor * 0.01');
      this.emit('child.position.y += (cameraPosition.y - child.position.y) * parallaxFactor * 0.01');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Portal depth fade
    if (this.compositionHasTrait(composition, 'portal_depth_fade')) {
      this.emit('// Portal depth fade: holograms fade at distance');
      this.emit('func applyDepthFade(frame: ARFrame) {');
      this.indentLevel++;
      this.emit('let cameraTransform = frame.camera.transform');
      this.emit('let cameraPos = SIMD3<Float>(cameraTransform.columns.3.x, cameraTransform.columns.3.y, cameraTransform.columns.3.z)');
      this.emit('let nearPlane: Float = 0.1');
      this.emit('let farPlane: Float = 10.0');
      this.emit('');
      this.emit('for child in holographicLayer.childNodes {');
      this.indentLevel++;
      this.emit('let nodePos = SIMD3<Float>(child.worldPosition.x, child.worldPosition.y, child.worldPosition.z)');
      this.emit('let distance = simd_distance(cameraPos, nodePos)');
      this.emit('let alpha = max(0, min(1, 1.0 - (distance - nearPlane) / (farPlane - nearPlane)))');
      this.emit('child.opacity = CGFloat(alpha)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Portal lighting match via environment probe
    if (this.compositionHasTrait(composition, 'portal_lighting_match')) {
      this.emit('// Portal lighting match: match hologram lighting to real environment via ARKit environment probe');
      this.emit('func updateLightingMatch(frame: ARFrame) {');
      this.indentLevel++;
      this.emit('guard let lightEstimate = frame.lightEstimate else { return }');
      this.emit('let intensity = lightEstimate.ambientIntensity');
      this.emit('let temperature = lightEstimate.ambientColorTemperature');
      this.emit('');
      this.emit('// Update holographic scene lighting');
      this.emit('if let light = holographicLayer.childNode(withName: "portalAmbientLight", recursively: true)?.light {');
      this.indentLevel++;
      this.emit('light.intensity = intensity');
      this.emit('light.temperature = temperature');
      this.indentLevel--;
      this.emit('} else {');
      this.indentLevel++;
      this.emit('let lightNode = SCNNode()');
      this.emit('lightNode.name = "portalAmbientLight"');
      this.emit('lightNode.light = SCNLight()');
      this.emit('lightNode.light?.type = .ambient');
      this.emit('lightNode.light?.intensity = intensity');
      this.emit('lightNode.light?.temperature = temperature');
      this.emit('holographicLayer.addChildNode(lightNode)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Portal boundary with edge geometry
    if (this.compositionHasTrait(composition, 'portal_boundary')) {
      this.emit('// Portal boundary: define portal edges with configurable shape');
      this.emit('func createPortalBoundary(shape: String = "circle", radius: Float = 1.5) -> SCNNode {');
      this.indentLevel++;
      this.emit('let boundaryNode = SCNNode()');
      this.emit('boundaryNode.name = "portalBoundary"');
      this.emit('');
      this.emit('switch shape {');
      this.emit('case "circle":');
      this.indentLevel++;
      this.emit('let torus = SCNTorus(ringRadius: CGFloat(radius), pipeRadius: 0.02)');
      this.emit('torus.firstMaterial?.diffuse.contents = UIColor.cyan.withAlphaComponent(0.8)');
      this.emit('boundaryNode.geometry = torus');
      this.indentLevel--;
      this.emit('case "rect":');
      this.indentLevel++;
      this.emit('let box = SCNBox(width: CGFloat(radius * 2), height: CGFloat(radius * 2), length: 0.02, chamferRadius: 0.01)');
      this.emit('box.firstMaterial?.diffuse.contents = UIColor.cyan.withAlphaComponent(0.8)');
      this.emit('boundaryNode.geometry = box');
      this.indentLevel--;
      this.emit('default:');
      this.indentLevel++;
      this.emit('break');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('return boundaryNode');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Portal edge glow shader
    if (this.compositionHasTrait(composition, 'portal_edge_glow')) {
      this.emit('// Portal edge glow: shader effect at portal boundary edges');
      this.emit('func applyEdgeGlow(to boundaryNode: SCNNode) {');
      this.indentLevel++;
      this.emit('let glowShader = """');
      this.emit('  #pragma arguments');
      this.emit('  float glowIntensity;');
      this.emit('  float time;');
      this.emit('  #pragma body');
      this.emit('  float edgeFactor = 1.0 - abs(dot(normalize(_surface.normal), normalize(_surface.view)));');
      this.emit('  float glow = pow(edgeFactor, 3.0) * glowIntensity * (0.8 + 0.2 * sin(time * 2.0));');
      this.emit('  _output.color.rgb += vec3(0.0, glow * 0.8, glow);');
      this.emit('"""');
      this.emit('');
      this.emit('boundaryNode.geometry?.firstMaterial?.shaderModifiers = [');
      this.indentLevel++;
      this.emit('.fragment: glowShader');
      this.indentLevel--;
      this.emit(']');
      this.emit('boundaryNode.geometry?.firstMaterial?.setValue(Float(1.5), forKey: "glowIntensity")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Portal peek through via device tilt (iOS uses CMMotionManager)
    if (this.compositionHasTrait(composition, 'portal_peek_through')) {
      this.emit('// Portal peek through: tilt phone to reveal holographic layer');
      this.emit('func checkPeekThrough(frame: ARFrame) {');
      this.indentLevel++;
      this.emit('let cameraTransform = frame.camera.transform');
      this.emit('// Calculate tilt angle from camera orientation');
      this.emit('let forward = SIMD3<Float>(cameraTransform.columns.2.x, cameraTransform.columns.2.y, cameraTransform.columns.2.z)');
      this.emit('let tiltAngle = abs(asin(forward.y)) * 180.0 / .pi');
      this.emit('let tiltThreshold: Float = 30.0');
      this.emit('portalEnabled = tiltAngle > tiltThreshold');
      this.emit('holographicLayer.isHidden = !portalEnabled');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ARSessionDelegate
    this.emit('// MARK: - ARSessionDelegate');
    this.emit('');
    this.emit('func session(_ session: ARSession, didUpdate frame: ARFrame) {');
    this.indentLevel++;

    if (this.compositionHasTrait(composition, 'portal_peek_through')) {
      this.emit('checkPeekThrough(frame: frame)');
    }
    if (this.compositionHasTrait(composition, 'portal_occlusion')) {
      this.emit('updateDepthOcclusion(frame: frame)');
    }
    if (this.compositionHasTrait(composition, 'portal_people_occlusion')) {
      this.emit('updatePeopleOcclusion(frame: frame)');
    }
    if (this.compositionHasTrait(composition, 'portal_parallax')) {
      this.emit('applyParallaxCorrection(frame: frame)');
    }
    if (this.compositionHasTrait(composition, 'portal_depth_fade')) {
      this.emit('applyDepthFade(frame: frame)');
    }
    if (this.compositionHasTrait(composition, 'portal_lighting_match')) {
      this.emit('updateLightingMatch(frame: frame)');
    }

    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  // === Camera Hand Tracking Methods (M.010.04) ===

  private hasHandTrackingTraits(composition: HoloComposition): boolean {
    const handTraitNames: ReadonlyArray<string> = CAMERA_HAND_TRACKING_TRAITS;
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (handTraitNames.includes(name)) return true;
      }
    }
    return false;
  }

  private generateHandTrackingFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;

    const twoHands = this.compositionHasTrait(composition, 'camera_hand_two_hands');
    const maxHands = twoHands ? 2 : 1;
    const hasPinch = this.compositionHasTrait(composition, 'camera_hand_gesture_pinch');
    const hasPoint = this.compositionHasTrait(composition, 'camera_hand_gesture_point');
    const hasPalm = this.compositionHasTrait(composition, 'camera_hand_gesture_palm');
    const hasFist = this.compositionHasTrait(composition, 'camera_hand_gesture_fist');
    const hasConfidence = this.compositionHasTrait(composition, 'camera_hand_confidence');
    const hasSkeleton = this.compositionHasTrait(composition, 'camera_hand_skeleton');
    const hasToSpatial = this.compositionHasTrait(composition, 'camera_hand_to_spatial');

    this.emit('// Auto-generated by HoloScript IOSCompiler — Hand Tracking integration');
    this.emit(`// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`);
    this.emit('// Requires iOS 14.0+, Vision framework');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import Vision');
    this.emit('import AVFoundation');
    this.emit('import UIKit');
    this.emit('');

    // HandGesture enum
    this.emit('/// Recognized hand gestures from camera-based tracking.');
    this.emit('enum HandGesture: String {');
    this.indentLevel++;
    if (hasPinch) this.emit('case pinch = "pinch"');
    if (hasPoint) this.emit('case point = "point"');
    if (hasPalm) this.emit('case palm = "palm"');
    if (hasFist) this.emit('case fist = "fist"');
    this.emit('case none = "none"');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // HandTrackingManager class
    this.emit(`class ${cls}HandTrackingManager: NSObject, ObservableObject {`);
    this.indentLevel++;
    this.emit('@Published var currentGesture: HandGesture = .none');
    this.emit('@Published var handCount: Int = 0');
    if (hasSkeleton) {
      this.emit('@Published var jointPositions: [[VNHumanHandPoseObservation.JointName: VNRecognizedPoint]] = []');
    }
    this.emit('');
    this.emit('private var captureSession: AVCaptureSession?');
    this.emit('private let handPoseRequest = VNDetectHumanHandPoseRequest()');
    this.emit('private let sequenceHandler = VNSequenceRequestHandler()');
    if (hasConfidence) {
      this.emit('private let minConfidence: Float = 0.7');
    } else {
      this.emit('private let minConfidence: Float = 0.5');
    }
    this.emit('');

    // init
    this.emit('override init() {');
    this.indentLevel++;
    this.emit('super.init()');
    this.emit(`handPoseRequest.maximumHandCount = ${maxHands}`);
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // startTracking
    this.emit('func startTracking() {');
    this.indentLevel++;
    this.emit('let session = AVCaptureSession()');
    this.emit('session.sessionPreset = .high');
    this.emit('');
    this.emit('guard let device = AVCaptureDevice.default(.builtInWideAngleCamera, for: .video, position: .front),');
    this.emit('      let input = try? AVCaptureDeviceInput(device: device) else {');
    this.indentLevel++;
    this.emit('print("[HoloScript] Failed to access front camera")');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('session.addInput(input)');
    this.emit('');
    this.emit('let output = AVCaptureVideoDataOutput()');
    this.emit('output.setSampleBufferDelegate(self, queue: DispatchQueue(label: "handTracking"))');
    this.emit('session.addOutput(output)');
    this.emit('');
    this.emit('captureSession = session');
    this.emit('session.startRunning()');
    this.emit('print("[HoloScript] Hand tracking started")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // stopTracking
    this.emit('func stopTracking() {');
    this.indentLevel++;
    this.emit('captureSession?.stopRunning()');
    this.emit('captureSession = nil');
    this.emit('print("[HoloScript] Hand tracking stopped")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // processObservation
    this.emit('private func processObservation(_ observation: VNHumanHandPoseObservation) {');
    this.indentLevel++;
    this.emit('guard let thumbTip = try? observation.recognizedPoint(.thumbTip),');
    this.emit('      let indexTip = try? observation.recognizedPoint(.indexTip),');
    this.emit('      let indexMCP = try? observation.recognizedPoint(.indexMCP),');
    this.emit('      let middleTip = try? observation.recognizedPoint(.middleTip),');
    this.emit('      let middleMCP = try? observation.recognizedPoint(.middleMCP),');
    this.emit('      let middlePIP = try? observation.recognizedPoint(.middlePIP),');
    this.emit('      let ringTip = try? observation.recognizedPoint(.ringTip),');
    this.emit('      let ringMCP = try? observation.recognizedPoint(.ringMCP),');
    this.emit('      let ringPIP = try? observation.recognizedPoint(.ringPIP),');
    this.emit('      let littleTip = try? observation.recognizedPoint(.littleTip),');
    this.emit('      let littleMCP = try? observation.recognizedPoint(.littleMCP),');
    this.emit('      let littlePIP = try? observation.recognizedPoint(.littlePIP),');
    this.emit('      let indexPIP = try? observation.recognizedPoint(.indexPIP)');
    this.emit('else { return }');
    this.emit('');

    if (hasConfidence) {
      this.emit('// Filter low-confidence joints');
      this.emit('guard thumbTip.confidence > minConfidence && indexTip.confidence > minConfidence else { return }');
      this.emit('');
    }

    if (hasSkeleton) {
      this.emit('// Extract all 21 joints');
      this.emit('var joints: [VNHumanHandPoseObservation.JointName: VNRecognizedPoint] = [:]');
      this.emit('let allJoints: [VNHumanHandPoseObservation.JointName] = [');
      this.indentLevel++;
      this.emit('.wrist, .thumbCMC, .thumbMP, .thumbIP, .thumbTip,');
      this.emit('.indexMCP, .indexPIP, .indexDIP, .indexTip,');
      this.emit('.middleMCP, .middlePIP, .middleDIP, .middleTip,');
      this.emit('.ringMCP, .ringPIP, .ringDIP, .ringTip,');
      this.emit('.littleMCP, .littlePIP, .littleDIP, .littleTip');
      this.indentLevel--;
      this.emit(']');
      this.emit('for jointName in allJoints {');
      this.indentLevel++;
      this.emit('if let point = try? observation.recognizedPoint(jointName) {');
      this.indentLevel++;
      this.emit('joints[jointName] = point');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // Gesture recognition
    if (hasPinch) {
      this.emit('// Pinch gesture: thumb tip close to index tip');
      this.emit('let pinchDist = hypot(thumbTip.location.x - indexTip.location.x, thumbTip.location.y - indexTip.location.y)');
      this.emit('if pinchDist < 0.05 {');
      this.indentLevel++;
      this.emit('DispatchQueue.main.async { self.currentGesture = .pinch }');
      if (hasToSpatial) {
        this.emit('emitSpatialInput(gesture: .pinch, location: thumbTip.location)');
      }
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasPoint) {
      this.emit('// Point gesture: index extended, others curled');
      this.emit('let indexExtended = indexTip.location.y > indexMCP.location.y');
      this.emit('let middleCurled = middleTip.location.y < middlePIP.location.y');
      this.emit('let ringCurled = ringTip.location.y < ringPIP.location.y');
      this.emit('let littleCurled = littleTip.location.y < littlePIP.location.y');
      this.emit('if indexExtended && middleCurled && ringCurled && littleCurled {');
      this.indentLevel++;
      this.emit('DispatchQueue.main.async { self.currentGesture = .point }');
      if (hasToSpatial) {
        this.emit('emitSpatialInput(gesture: .point, location: indexTip.location)');
      }
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasPalm) {
      this.emit('// Palm gesture: all fingertips above MCPs (open hand)');
      this.emit('let allExtended = indexTip.location.y > indexMCP.location.y &&');
      this.emit('    middleTip.location.y > middleMCP.location.y &&');
      this.emit('    ringTip.location.y > ringMCP.location.y &&');
      this.emit('    littleTip.location.y > littleMCP.location.y');
      this.emit('if allExtended {');
      this.indentLevel++;
      this.emit('DispatchQueue.main.async { self.currentGesture = .palm }');
      if (hasToSpatial) {
        this.emit('emitSpatialInput(gesture: .palm, location: indexTip.location)');
      }
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasFist) {
      this.emit('// Fist gesture: all fingertips below PIPs (closed hand)');
      this.emit('let allCurled = indexTip.location.y < indexPIP.location.y &&');
      this.emit('    middleTip.location.y < middlePIP.location.y &&');
      this.emit('    ringTip.location.y < ringPIP.location.y &&');
      this.emit('    littleTip.location.y < littlePIP.location.y');
      this.emit('if allCurled {');
      this.indentLevel++;
      this.emit('DispatchQueue.main.async { self.currentGesture = .fist }');
      if (hasToSpatial) {
        this.emit('emitSpatialInput(gesture: .fist, location: indexTip.location)');
      }
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    this.emit('DispatchQueue.main.async { self.currentGesture = .none }');
    this.indentLevel--;
    this.emit('}');

    // Spatial input bridge
    if (hasToSpatial) {
      this.emit('');
      this.emit('private func emitSpatialInput(gesture: HandGesture, location: CGPoint) {');
      this.indentLevel++;
      this.emit('// Bridge to HoloScript spatial_input event system');
      this.emit('let event: [String: Any] = [');
      this.indentLevel++;
      this.emit('"type": "hand_gesture",');
      this.emit('"gesture": gesture.rawValue,');
      this.emit('"x": location.x,');
      this.emit('"y": location.y');
      this.indentLevel--;
      this.emit(']');
      this.emit('print("[HoloScript] SpatialInput: \\(event)")');
      this.indentLevel--;
      this.emit('}');
    }

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // AVCaptureVideoDataOutputSampleBufferDelegate extension
    this.emit(`extension ${cls}HandTrackingManager: AVCaptureVideoDataOutputSampleBufferDelegate {`);
    this.indentLevel++;
    this.emit('func captureOutput(_ output: AVCaptureOutput, didOutput sampleBuffer: CMSampleBuffer, from connection: AVCaptureConnection) {');
    this.indentLevel++;
    this.emit('guard let pixelBuffer = CMSampleBufferGetImageBuffer(sampleBuffer) else { return }');
    this.emit('');
    this.emit('let handler = VNImageRequestHandler(cvPixelBuffer: pixelBuffer, orientation: .up, options: [:])');
    this.emit('do {');
    this.indentLevel++;
    this.emit('try handler.perform([handPoseRequest])');
    this.emit('guard let results = handPoseRequest.results else { return }');
    this.emit('');
    this.emit('DispatchQueue.main.async {');
    this.indentLevel++;
    this.emit('self.handCount = results.count');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('for observation in results {');
    this.indentLevel++;
    this.emit('processObservation(observation)');
    if (hasSkeleton) {
      this.emit('// Update joint positions on main thread');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('// jointPositions updated via processObservation');
      this.indentLevel--;
      this.emit('}');
    }
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('} catch {');
    this.indentLevel++;
    this.emit('print("[HoloScript] Hand pose detection error: \\(error)")');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  // ─── Object Capture (iOS 17+, M.010.10) ────────────────────────────

  private hasObjectCaptureTraits(composition: HoloComposition): boolean {
    const captureNames: ReadonlyArray<string> = IOS_OBJECT_CAPTURE_TRAITS;
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (captureNames.includes(name)) return true;
      }
    }
    return false;
  }

  private collectObjectCaptureTraits(composition: HoloComposition): Set<string> {
    const captureNames: ReadonlyArray<string> = IOS_OBJECT_CAPTURE_TRAITS;
    const found = new Set<string>();
    for (const obj of composition.objects || []) {
      for (const t of obj.traits || []) {
        const name = typeof t === 'string' ? t : t.name;
        if (captureNames.includes(name)) found.add(name);
      }
    }
    return found;
  }

  /**
   * Emit a standalone Swift file wrapping Apple's Object Capture API.
   *
   * Generated code:
   *   1. Imports RealityKit (ObjectCaptureSession, iOS 17+)
   *   2. ObjectCaptureSession setup with Configuration
   *   3. Guided capture overlay with ObjectCapturePointCloudView
   *   4. Real-time quality feedback via session state observation
   *   5. PhotogrammetrySession processing pipeline
   *   6. PBR texture extraction (diffuse, normal, roughness, metallic, AO)
   *   7. LOD generation (.preview, .reduced, .medium, .full, .raw)
   *   8. USDZ output → .holo entity conversion
   *   9. SwiftUI view with capture controls, progress, and preview
   */
  private generateObjectCaptureFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;
    const traits = this.collectObjectCaptureTraits(composition);

    this.emit('// Auto-generated by HoloScript IOSCompiler — Object Capture integration');
    this.emit(
      `// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`
    );
    this.emit('// Requires iOS 17.0+, RealityKit Object Capture API');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import RealityKit');
    this.emit('import SwiftUI');
    this.emit('import Combine');
    this.emit('import os');
    this.emit('');

    // ── HoloCapturedEntity model ──
    this.emit('struct HoloCapturedEntity: Identifiable {');
    this.indentLevel++;
    this.emit('let id = UUID()');
    this.emit('let name: String');
    this.emit('let modelURL: URL');
    this.emit('let boundingBox: BoundingBox');
    this.emit('let detailLevel: DetailLevel');
    this.emit('let pbrMaterials: PBRMaterialSet?');
    this.emit('');
    this.emit('struct BoundingBox {');
    this.indentLevel++;
    this.emit('let center: SIMD3<Float>');
    this.emit('let extents: SIMD3<Float>');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('enum DetailLevel: String, CaseIterable {');
    this.indentLevel++;
    this.emit('case preview');
    this.emit('case reduced');
    this.emit('case medium');
    this.emit('case full');
    this.emit('case raw');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('struct PBRMaterialSet {');
    this.indentLevel++;
    this.emit('let diffuseMap: URL?');
    this.emit('let normalMap: URL?');
    this.emit('let roughnessMap: URL?');
    this.emit('let metallicMap: URL?');
    this.emit('let aoMap: URL?');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ── ObjectCaptureManager class ──
    this.emit(`class ${cls}ObjectCaptureManager: ObservableObject {`);
    this.indentLevel++;
    this.emit('');
    this.emit('private let logger = Logger(subsystem: "net.holoscript", category: "ObjectCapture")');
    this.emit('');
    this.emit('enum CaptureState: Equatable {');
    this.indentLevel++;
    this.emit('case ready');
    this.emit('case capturing');
    this.emit('case processing');
    this.emit('case completed');
    this.emit('case failed(String)');
    this.emit('');
    this.emit('static func == (lhs: CaptureState, rhs: CaptureState) -> Bool {');
    this.indentLevel++;
    this.emit('switch (lhs, rhs) {');
    this.emit('case (.ready, .ready), (.capturing, .capturing),');
    this.emit('     (.processing, .processing), (.completed, .completed): return true');
    this.emit('case (.failed(let a), .failed(let b)): return a == b');
    this.emit('default: return false');
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('@Published var state: CaptureState = .ready');
    this.emit('@Published var progress: Float = 0.0');
    this.emit('@Published var capturedEntity: HoloCapturedEntity?');
    this.emit('@Published var feedbackMessages: [String] = []');
    this.emit('');
    this.emit('private var captureSession: ObjectCaptureSession?');
    this.emit('private var outputDirectory: URL');
    this.emit('private var cancellables = Set<AnyCancellable>()');
    this.emit('');

    // ── init ──
    this.emit('init() {');
    this.indentLevel++;
    this.emit('let docs = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask).first!');
    this.emit('self.outputDirectory = docs.appendingPathComponent("HoloCaptures", isDirectory: true)');
    this.emit('try? FileManager.default.createDirectory(at: outputDirectory, withIntermediateDirectories: true)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ── isSupported ──
    this.emit('var isSupported: Bool {');
    this.indentLevel++;
    this.emit('ObjectCaptureSession.isSupported');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ── startCapture ──
    this.emit('func startCapture() {');
    this.indentLevel++;
    this.emit('guard isSupported else {');
    this.indentLevel++;
    this.emit('state = .failed("Object Capture not supported on this device")');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('let session = ObjectCaptureSession()');
    this.emit('self.captureSession = session');
    this.emit('');
    this.emit('var configuration = ObjectCaptureSession.Configuration()');
    this.emit('configuration.checkpointDirectory = outputDirectory.appendingPathComponent("Checkpoints")');
    this.emit('configuration.isOverCaptureEnabled = true');
    this.emit('session.start(imagesDirectory: outputDirectory.appendingPathComponent("Images"),');
    this.emit('             configuration: configuration)');
    this.emit('');
    this.emit('observeSession(session)');
    this.emit('state = .capturing');
    this.emit('logger.info("[HoloScript] Object Capture session started")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ── stopCapture ──
    this.emit('func stopCapture() {');
    this.indentLevel++;
    this.emit('captureSession?.finish()');
    this.emit('logger.info("[HoloScript] Object Capture session finished")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ── observeSession (feedback) ──
    if (traits.has('object_capture_feedback') || traits.has('object_capture')) {
      this.emit('private func observeSession(_ session: ObjectCaptureSession) {');
      this.indentLevel++;
      this.emit('session.stateUpdates.sink { [weak self] newState in');
      this.indentLevel++;
      this.emit('guard let self = self else { return }');
      this.emit('switch newState {');
      this.emit('case .ready:');
      this.indentLevel++;
      this.emit('self.feedbackMessages.append("Ready to capture")');
      this.indentLevel--;
      this.emit('case .detecting:');
      this.indentLevel++;
      this.emit('self.feedbackMessages.append("Detecting object...")');
      this.indentLevel--;
      this.emit('case .capturing:');
      this.indentLevel++;
      this.emit('self.feedbackMessages.append("Move slowly around the object")');
      this.indentLevel--;
      this.emit('case .finishing:');
      this.indentLevel++;
      this.emit('self.feedbackMessages.append("Finishing capture...")');
      this.indentLevel--;
      this.emit('case .completed:');
      this.indentLevel++;
      this.emit('self.state = .processing');
      this.emit('self.feedbackMessages.append("Capture complete — processing...")');
      this.emit('self.processCapture()');
      this.indentLevel--;
      this.emit('case .failed(let error):');
      this.indentLevel++;
      this.emit('self.state = .failed(error.localizedDescription)');
      this.indentLevel--;
      this.emit('default:');
      this.indentLevel++;
      this.emit('break');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}.store(in: &cancellables)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ── processCapture (PhotogrammetrySession) ──
    if (traits.has('photogrammetry_scan') || traits.has('object_capture')) {
      this.emit('private func processCapture() {');
      this.indentLevel++;
      this.emit('Task {');
      this.indentLevel++;
      this.emit('do {');
      this.indentLevel++;
      this.emit('let inputFolder = outputDirectory.appendingPathComponent("Images")');
      this.emit('let request = makePhotogrammetryRequest()');
      this.emit('let session = try PhotogrammetrySession(input: inputFolder)');
      this.emit('');
      this.emit('try session.process(requests: [request])');
      this.emit('');
      this.emit('for try await output in session.outputs {');
      this.indentLevel++;
      this.emit('switch output {');
      this.emit('case .requestProgress(let request, let fraction):');
      this.indentLevel++;
      this.emit('await MainActor.run { self.progress = Float(fraction) }');
      this.indentLevel--;
      this.emit('case .requestComplete(let request, let result):');
      this.indentLevel++;
      this.emit('await MainActor.run {');
      this.indentLevel++;
      this.emit('self.handlePhotogrammetryResult(result)');
      this.emit('self.state = .completed');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('case .requestError(let request, let error):');
      this.indentLevel++;
      this.emit('await MainActor.run { self.state = .failed(error.localizedDescription) }');
      this.indentLevel--;
      this.emit('default: break');
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('} catch {');
      this.indentLevel++;
      this.emit('await MainActor.run { self.state = .failed(error.localizedDescription) }');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ── makePhotogrammetryRequest with detail level ──
    if (traits.has('object_capture_lod') || traits.has('object_capture')) {
      this.emit('private func makePhotogrammetryRequest() -> PhotogrammetrySession.Request {');
      this.indentLevel++;
      this.emit('let outputURL = outputDirectory.appendingPathComponent("model.usdz")');
      this.emit('// .full detail level captures PBR maps (diffuse, normal, roughness, metallic, AO)');
      this.emit('return .modelFile(url: outputURL, detail: .full)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');

      this.emit('func generateAllLODs() async throws -> [HoloCapturedEntity.DetailLevel: URL] {');
      this.indentLevel++;
      this.emit('var results: [HoloCapturedEntity.DetailLevel: URL] = [:]');
      this.emit('let inputFolder = outputDirectory.appendingPathComponent("Images")');
      this.emit('let session = try PhotogrammetrySession(input: inputFolder)');
      this.emit('');
      this.emit('for level in HoloCapturedEntity.DetailLevel.allCases {');
      this.indentLevel++;
      this.emit('let url = outputDirectory.appendingPathComponent("model_\\(level.rawValue).usdz")');
      this.emit('let detail: PhotogrammetrySession.Request.Detail');
      this.emit('switch level {');
      this.emit('case .preview: detail = .preview');
      this.emit('case .reduced: detail = .reduced');
      this.emit('case .medium: detail = .medium');
      this.emit('case .full: detail = .full');
      this.emit('case .raw: detail = .raw');
      this.emit('}');
      this.emit('let request = PhotogrammetrySession.Request.modelFile(url: url, detail: detail)');
      this.emit('try session.process(requests: [request])');
      this.emit('for try await output in session.outputs {');
      this.indentLevel++;
      this.emit('if case .requestComplete = output { results[level] = url; break }');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('return results');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ── PBR texture extraction ──
    if (traits.has('pbr_texture_extract') || traits.has('object_capture')) {
      this.emit('private func extractPBRMaterials(from modelURL: URL) -> HoloCapturedEntity.PBRMaterialSet? {');
      this.indentLevel++;
      this.emit('let baseDir = modelURL.deletingLastPathComponent()');
      this.emit('let diffuse = baseDir.appendingPathComponent("diffuse.png")');
      this.emit('let normal = baseDir.appendingPathComponent("normal.png")');
      this.emit('let roughness = baseDir.appendingPathComponent("roughness.png")');
      this.emit('let metallic = baseDir.appendingPathComponent("metallic.png")');
      this.emit('let ao = baseDir.appendingPathComponent("ao.png")');
      this.emit('');
      this.emit('return HoloCapturedEntity.PBRMaterialSet(');
      this.emit('    diffuseMap: FileManager.default.fileExists(atPath: diffuse.path) ? diffuse : nil,');
      this.emit('    normalMap: FileManager.default.fileExists(atPath: normal.path) ? normal : nil,');
      this.emit('    roughnessMap: FileManager.default.fileExists(atPath: roughness.path) ? roughness : nil,');
      this.emit('    metallicMap: FileManager.default.fileExists(atPath: metallic.path) ? metallic : nil,');
      this.emit('    aoMap: FileManager.default.fileExists(atPath: ao.path) ? ao : nil');
      this.emit(')');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ── handlePhotogrammetryResult ──
    this.emit('private func handlePhotogrammetryResult(_ result: PhotogrammetrySession.Result) {');
    this.indentLevel++;
    this.emit('switch result {');
    this.emit('case .modelFile(let url):');
    this.indentLevel++;
    this.emit('logger.info("[HoloScript] Model generated at \\(url.path)")');
    if (traits.has('pbr_texture_extract') || traits.has('object_capture')) {
      this.emit('let pbr = extractPBRMaterials(from: url)');
    }
    this.emit('capturedEntity = HoloCapturedEntity(');
    this.emit('    name: "captured_object",');
    this.emit('    modelURL: url,');
    this.emit('    boundingBox: HoloCapturedEntity.BoundingBox(center: .zero, extents: .one),');
    this.emit('    detailLevel: .full,');
    if (traits.has('pbr_texture_extract') || traits.has('object_capture')) {
      this.emit('    pbrMaterials: pbr');
    } else {
      this.emit('    pbrMaterials: nil');
    }
    this.emit(')');
    this.indentLevel--;
    this.emit('default:');
    this.indentLevel++;
    this.emit('logger.warning("[HoloScript] Unexpected photogrammetry result type")');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ── USDZ export ──
    if (traits.has('object_capture_export_usdz') || traits.has('object_capture')) {
      this.emit('func exportUSDZ() -> URL? {');
      this.indentLevel++;
      this.emit('let usdzURL = outputDirectory.appendingPathComponent("export.usdz")');
      this.emit('guard let modelURL = capturedEntity?.modelURL else { return nil }');
      this.emit('do {');
      this.indentLevel++;
      this.emit('try FileManager.default.copyItem(at: modelURL, to: usdzURL)');
      this.emit('logger.info("[HoloScript] USDZ exported to \\(usdzURL.path)")');
      this.emit('return usdzURL');
      this.indentLevel--;
      this.emit('} catch {');
      this.indentLevel++;
      this.emit('logger.error("[HoloScript] USDZ export failed: \\(error.localizedDescription)")');
      this.emit('return nil');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ── Convert to .holo ──
    if (traits.has('object_capture_to_holo') || traits.has('object_capture')) {
      this.emit('func convertToHolo() -> String? {');
      this.indentLevel++;
      this.emit('guard let entity = capturedEntity else { return nil }');
      this.emit('');
      this.emit('var holo = "scene CapturedObject {\\n"');
      this.emit('holo += "  object \\(entity.name) {\\n"');
      this.emit('holo += "    model: \\"\\(entity.modelURL.lastPathComponent)\\"\\n"');
      this.emit('holo += "    position: [\\(entity.boundingBox.center.x), \\(entity.boundingBox.center.y), \\(entity.boundingBox.center.z)]\\n"');
      this.emit('holo += "    scale: [\\(entity.boundingBox.extents.x), \\(entity.boundingBox.extents.y), \\(entity.boundingBox.extents.z)]\\n"');
      this.emit('holo += "    traits: [object_capture, photogrammetry_scan]\\n"');
      if (traits.has('pbr_texture_extract')) {
        this.emit('if let pbr = entity.pbrMaterials {');
        this.indentLevel++;
        this.emit('if pbr.diffuseMap != nil { holo += "    diffuse_map: \\"diffuse.png\\"\\n" }');
        this.emit('if pbr.normalMap != nil { holo += "    normal_map: \\"normal.png\\"\\n" }');
        this.emit('if pbr.roughnessMap != nil { holo += "    roughness_map: \\"roughness.png\\"\\n" }');
        this.emit('if pbr.metallicMap != nil { holo += "    metallic_map: \\"metallic.png\\"\\n" }');
        this.emit('if pbr.aoMap != nil { holo += "    ao_map: \\"ao.png\\"\\n" }');
        this.indentLevel--;
        this.emit('}');
      }
      this.emit('holo += "  }\\n"');
      this.emit('holo += "}\\n"');
      this.emit('return holo');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ── SwiftUI View ──
    if (traits.has('object_capture_guide') || traits.has('object_capture')) {
      this.emit(`struct ${cls}ObjectCaptureView: View {`);
      this.indentLevel++;
      this.emit(`@StateObject private var manager = ${cls}ObjectCaptureManager()`);
      this.emit('');
      this.emit('var body: some View {');
      this.indentLevel++;
      this.emit('ZStack {');
      this.indentLevel++;
      this.emit('if let session = manager.captureSession {');
      this.indentLevel++;
      this.emit('ObjectCapturePointCloudView(session: session)');
      this.emit('    .edgesIgnoringSafeArea(.all)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('VStack {');
      this.indentLevel++;
      this.emit('Spacer()');
      this.emit('');

      // ── Feedback messages ──
      this.emit('// Coverage indicator and feedback');
      this.emit('if !manager.feedbackMessages.isEmpty {');
      this.indentLevel++;
      this.emit('Text(manager.feedbackMessages.last ?? "")');
      this.emit('    .font(.caption)');
      this.emit('    .foregroundColor(.white)');
      this.emit('    .padding(8)');
      this.emit('    .background(Color.black.opacity(0.6))');
      this.emit('    .cornerRadius(8)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');

      // ── Progress indicator ──
      this.emit('if manager.state == .processing {');
      this.indentLevel++;
      this.emit('ProgressView(value: Double(manager.progress))');
      this.emit('    .progressViewStyle(.linear)');
      this.emit('    .padding()');
      this.emit('Text("Processing: \\(Int(manager.progress * 100))%")');
      this.emit('    .foregroundColor(.white)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');

      // ── Capture controls ──
      this.emit('HStack(spacing: 20) {');
      this.indentLevel++;
      this.emit('switch manager.state {');
      this.emit('case .ready:');
      this.indentLevel++;
      this.emit('Button("Start Capture") { manager.startCapture() }');
      this.emit('    .buttonStyle(.borderedProminent)');
      this.indentLevel--;
      this.emit('case .capturing:');
      this.indentLevel++;
      this.emit('Button("Finish Capture") { manager.stopCapture() }');
      this.emit('    .buttonStyle(.borderedProminent)');
      this.emit('    .tint(.red)');
      this.indentLevel--;
      this.emit('case .completed:');
      this.indentLevel++;
      this.emit('if let entity = manager.capturedEntity {');
      this.indentLevel++;
      this.emit('NavigationLink("Preview Model") {');
      this.indentLevel++;
      this.emit(`${cls}ModelPreviewView(url: entity.modelURL)`);
      this.indentLevel--;
      this.emit('}');
      this.emit('.buttonStyle(.borderedProminent)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('case .failed(let message):');
      this.indentLevel++;
      this.emit('Text("Error: \\(message)")');
      this.emit('    .foregroundColor(.red)');
      this.indentLevel--;
      this.emit('default: EmptyView()');
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('.padding()');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');

      // ── Model preview ──
      this.emit(`struct ${cls}ModelPreviewView: View {`);
      this.indentLevel++;
      this.emit('let url: URL');
      this.emit('');
      this.emit('var body: some View {');
      this.indentLevel++;
      this.emit('Model3D(url: url) { model in');
      this.indentLevel++;
      this.emit('model.resizable()');
      this.emit('    .aspectRatio(contentMode: .fit)');
      this.indentLevel--;
      this.emit('} placeholder: {');
      this.indentLevel++;
      this.emit('ProgressView()');
      this.indentLevel--;
      this.emit('}');
      this.emit('.navigationTitle("Captured Object")');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
    }

    return this.lines.join('\n');
  }

  // ─── SharePlay Multi-User AR (M.010.12) ──────────────────────────────

  private hasSharePlayTraits(composition: HoloComposition): boolean {
    const sharePlayNames: ReadonlyArray<string> = SHAREPLAY_TRAITS;
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (sharePlayNames.includes(name)) return true;
      }
    }
    return false;
  }

  private generateSharePlayFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;

    this.emit('// Auto-generated by HoloScript IOSCompiler — SharePlay Multi-User AR');
    this.emit(
      `// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`
    );
    this.emit('// Requires iOS 15.4+, GroupActivities framework');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import GroupActivities');
    this.emit('import ARKit');
    this.emit('import SceneKit');
    this.emit('import RealityKit');
    this.emit('import SwiftUI');
    this.emit('import Combine');
    this.emit('');

    // ─── GroupActivity conforming type ──────────────────────────────
    this.emit('// MARK: - GroupActivity Definition');
    this.emit('');
    this.emit('struct HoloGroupActivity: GroupActivity {');
    this.indentLevel++;
    this.emit(`let sceneName: String`);
    this.emit('');
    this.emit('var metadata: GroupActivityMetadata {');
    this.indentLevel++;
    this.emit('var meta = GroupActivityMetadata()');
    this.emit(`meta.title = NSLocalizedString("HoloScript AR Session", comment: "")`);
    this.emit('meta.type = .generic');
    this.emit('return meta');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Codable messages for scene sync ────────────────────────────
    this.emit('// MARK: - Sync Messages (Codable, Loro CRDT-inspired diffs)');
    this.emit('');
    this.emit('struct SceneStateDiff: Codable {');
    this.indentLevel++;
    this.emit('let senderId: String');
    this.emit('let timestamp: TimeInterval');
    this.emit('let entityUpdates: [EntityUpdate]');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('struct EntityUpdate: Codable {');
    this.indentLevel++;
    this.emit('let entityId: String');
    this.emit('let ownerId: String?');
    this.emit('let position: [Float]?');
    this.emit('let rotation: [Float]?');
    this.emit('let scale: [Float]?');
    this.emit('let properties: [String: String]?');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('struct FullStateSnapshot: Codable {');
    this.indentLevel++;
    this.emit('let senderId: String');
    this.emit('let timestamp: TimeInterval');
    this.emit('let entities: [EntityUpdate]');
    this.emit('let ownershipMap: [String: String]');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Ownership transfer message ─────────────────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_entity_ownership')) {
      this.emit('struct OwnershipTransferRequest: Codable {');
      this.indentLevel++;
      this.emit('let entityId: String');
      this.emit('let fromParticipantId: String');
      this.emit('let toParticipantId: String');
      this.emit('let timestamp: TimeInterval');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── SharePlayManager class ─────────────────────────────────────
    this.emit(`// MARK: - ${cls}SharePlayManager`);
    this.emit('');
    this.emit(`class ${cls}SharePlayManager: NSObject, ObservableObject {`);
    this.indentLevel++;
    this.emit('');

    // Properties
    this.emit('// Session state');
    this.emit('private var groupSession: GroupSession<HoloGroupActivity>?');
    this.emit('private var messenger: GroupSessionMessenger?');
    this.emit('private var subscriptions = Set<AnyCancellable>()');
    this.emit('private var tasks = [Task<Void, Never>]()');
    this.emit('');
    this.emit('@Published var isSessionActive: Bool = false');
    this.emit('@Published var isHost: Bool = false');
    this.emit('@Published var participants: [Participant] = []');
    this.emit('@Published var localParticipantId: String = UUID().uuidString');
    this.emit('');

    // Ownership tracking
    if (this.compositionHasTrait(composition, 'shareplay_entity_ownership')) {
      this.emit('// Entity ownership map: entityId -> participantId');
      this.emit('private var entityOwnership: [String: String] = [:]');
      this.emit('');
    }

    // Scene state for sync
    if (this.compositionHasTrait(composition, 'shareplay_sync')) {
      this.emit('// Scene state for CRDT-inspired sync');
      this.emit('private var sceneEntities: [String: EntityUpdate] = [:]');
      this.emit('private var lastSyncTimestamp: TimeInterval = 0');
      this.emit('');
    }

    // Local anchor mapping
    if (this.compositionHasTrait(composition, 'shareplay_anchor_local')) {
      this.emit('// Local AR anchor mapping — each participant maps shared positions to local space');
      this.emit('private var localWorldTransform: simd_float4x4 = matrix_identity_float4x4');
      this.emit('private var sharedToLocalOffset: simd_float3 = .zero');
      this.emit('');
    }

    // Spatial audio
    if (this.compositionHasTrait(composition, 'shareplay_voice_spatial')) {
      this.emit('// Spatial audio for participant voices');
      this.emit(
        'private var spatialAudioExperience: SpatialAudioExperience = .custom(fixedToScene: true)'
      );
      this.emit('');
    }

    // Mute zones
    if (this.compositionHasTrait(composition, 'shareplay_mute_zone')) {
      this.emit('// Spatial mute zones — regions where audio attenuates');
      this.emit('struct MuteZone {');
      this.indentLevel++;
      this.emit('let center: simd_float3');
      this.emit('let radius: Float');
      this.emit('let attenuation: Float // 0.0 = full mute, 1.0 = no effect');
      this.indentLevel--;
      this.emit('}');
      this.emit('private var muteZones: [MuteZone] = []');
      this.emit('');
    }

    // ─── Init ───────────────────────────────────────────────────────
    this.emit('override init() {');
    this.indentLevel++;
    this.emit('super.init()');
    this.emit('observeGroupSessions()');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Session lifecycle: shareplay_session ───────────────────────
    this.emit('// MARK: - Session Lifecycle');
    this.emit('');
    this.emit('private func observeGroupSessions() {');
    this.indentLevel++;
    this.emit('let activity = HoloGroupActivity(sceneName: "' + cls + '")');
    this.emit('Task {');
    this.indentLevel++;
    this.emit('for await session in activity.sessions() {');
    this.indentLevel++;
    this.emit('configureSession(session)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit('private func configureSession(_ session: GroupSession<HoloGroupActivity>) {');
    this.indentLevel++;
    this.emit('self.groupSession = session');
    this.emit('self.messenger = GroupSessionMessenger(session: session)');
    this.emit('');
    this.emit('// State machine: .waitingForActivation -> .joined');
    this.emit('session.$state.sink { [weak self] state in');
    this.indentLevel++;
    this.emit('guard let self = self else { return }');
    this.emit('switch state {');
    this.emit('case .joined:');
    this.indentLevel++;
    this.emit('self.isSessionActive = true');
    this.emit('self.setupMessageHandlers()');
    this.indentLevel--;
    this.emit('case .waitingForActivation:');
    this.indentLevel++;
    this.emit('session.join()');
    this.indentLevel--;
    this.emit('default:');
    this.indentLevel++;
    this.emit('self.isSessionActive = false');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('.store(in: &subscriptions)');
    this.emit('');

    // Track participants
    this.emit('session.$activeParticipants.sink { [weak self] active in');
    this.indentLevel++;
    this.emit('self?.participants = Array(active)');
    this.indentLevel--;
    this.emit('}');
    this.emit('.store(in: &subscriptions)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Host authority: shareplay_host ──────────────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_host')) {
      this.emit('// MARK: - Host Authority');
      this.emit('');
      this.emit('func becomeHost() {');
      this.indentLevel++;
      this.emit('isHost = true');
      this.emit(
        'broadcastSceneState() // Host pushes authoritative state to all participants'
      );
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('private func broadcastSceneState() {');
      this.indentLevel++;
      this.emit('guard isHost, let messenger = messenger else { return }');
      this.emit('let snapshot = FullStateSnapshot(');
      this.indentLevel++;
      this.emit('senderId: localParticipantId,');
      this.emit('timestamp: Date().timeIntervalSince1970,');
      this.emit('entities: Array(sceneEntities.values),');
      this.emit('ownershipMap: entityOwnership');
      this.indentLevel--;
      this.emit(')');
      this.emit('Task {');
      this.indentLevel++;
      this.emit('try? await messenger.send(snapshot)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Join flow: shareplay_join ───────────────────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_join')) {
      this.emit('// MARK: - Join Flow');
      this.emit('');
      this.emit('func startSharePlay() async throws {');
      this.indentLevel++;
      this.emit('let activity = HoloGroupActivity(sceneName: "' + cls + '")');
      this.emit('let result = try await activity.prepareForActivation()');
      this.emit('switch result {');
      this.emit('case .activationPreferred:');
      this.indentLevel++;
      this.emit('activity.activate()');
      this.indentLevel--;
      this.emit('case .activationDisabled:');
      this.indentLevel++;
      this.emit('break // SharePlay not available in current FaceTime call');
      this.indentLevel--;
      this.emit('default:');
      this.indentLevel++;
      this.emit('break');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Message handlers: shareplay_sync ────────────────────────────
    this.emit('// MARK: - Message Handlers');
    this.emit('');
    this.emit('private func setupMessageHandlers() {');
    this.indentLevel++;
    this.emit('guard let messenger = messenger else { return }');
    this.emit('');

    // Scene state diffs
    if (this.compositionHasTrait(composition, 'shareplay_sync')) {
      this.emit('// Receive scene state diffs (Loro CRDT-inspired: last-writer-wins per entity)');
      this.emit('let diffTask = Task {');
      this.indentLevel++;
      this.emit('for await (diff, _) in messenger.messages(of: SceneStateDiff.self) {');
      this.indentLevel++;
      this.emit('await MainActor.run {');
      this.indentLevel++;
      this.emit('self.applySceneDiff(diff)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('tasks.append(diffTask)');
      this.emit('');
    }

    // Late join snapshots
    if (this.compositionHasTrait(composition, 'shareplay_late_join')) {
      this.emit('// Receive full state snapshots for late-joining participants');
      this.emit('let snapshotTask = Task {');
      this.indentLevel++;
      this.emit('for await (snapshot, _) in messenger.messages(of: FullStateSnapshot.self) {');
      this.indentLevel++;
      this.emit('await MainActor.run {');
      this.indentLevel++;
      this.emit('self.applyFullSnapshot(snapshot)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('tasks.append(snapshotTask)');
      this.emit('');
    }

    // Ownership transfer
    if (this.compositionHasTrait(composition, 'shareplay_entity_ownership')) {
      this.emit('// Receive ownership transfer requests');
      this.emit('let ownershipTask = Task {');
      this.indentLevel++;
      this.emit(
        'for await (request, _) in messenger.messages(of: OwnershipTransferRequest.self) {'
      );
      this.indentLevel++;
      this.emit('await MainActor.run {');
      this.indentLevel++;
      this.emit('self.handleOwnershipTransfer(request)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('tasks.append(ownershipTask)');
      this.emit('');
    }

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Sync helpers ────────────────────────────────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_sync')) {
      this.emit('// MARK: - Scene Sync (Loro CRDT-inspired diff application)');
      this.emit('');
      this.emit('func sendSceneUpdate(_ updates: [EntityUpdate]) {');
      this.indentLevel++;
      this.emit('guard let messenger = messenger else { return }');
      this.emit('let diff = SceneStateDiff(');
      this.indentLevel++;
      this.emit('senderId: localParticipantId,');
      this.emit('timestamp: Date().timeIntervalSince1970,');
      this.emit('entityUpdates: updates');
      this.indentLevel--;
      this.emit(')');
      this.emit('Task {');
      this.indentLevel++;
      this.emit('try? await messenger.send(diff)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('private func applySceneDiff(_ diff: SceneStateDiff) {');
      this.indentLevel++;
      this.emit('guard diff.timestamp > lastSyncTimestamp else { return }');
      this.emit('for update in diff.entityUpdates {');
      this.indentLevel++;
      this.emit('sceneEntities[update.entityId] = update');
      this.indentLevel--;
      this.emit('}');
      this.emit('lastSyncTimestamp = diff.timestamp');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Late join snapshot ──────────────────────────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_late_join')) {
      this.emit('// MARK: - Late Join');
      this.emit('');
      this.emit('func requestFullState() {');
      this.indentLevel++;
      this.emit('// Late joiner requests state — host responds with snapshot');
      this.emit('guard let messenger = messenger else { return }');
      this.emit('Task {');
      this.indentLevel++;
      this.emit('let request = SceneStateDiff(');
      this.indentLevel++;
      this.emit('senderId: localParticipantId,');
      this.emit('timestamp: 0, // Signal: requesting full state');
      this.emit('entityUpdates: []');
      this.indentLevel--;
      this.emit(')');
      this.emit('try? await messenger.send(request)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('private func applyFullSnapshot(_ snapshot: FullStateSnapshot) {');
      this.indentLevel++;
      this.emit('sceneEntities.removeAll()');
      this.emit('for entity in snapshot.entities {');
      this.indentLevel++;
      this.emit('sceneEntities[entity.entityId] = entity');
      this.indentLevel--;
      this.emit('}');

      if (this.compositionHasTrait(composition, 'shareplay_entity_ownership')) {
        this.emit('entityOwnership = snapshot.ownershipMap');
      }

      this.emit('lastSyncTimestamp = snapshot.timestamp');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Entity ownership ────────────────────────────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_entity_ownership')) {
      this.emit('// MARK: - Entity Ownership');
      this.emit('');
      this.emit('func claimOwnership(entityId: String) {');
      this.indentLevel++;
      this.emit('entityOwnership[entityId] = localParticipantId');
      this.emit('guard let messenger = messenger else { return }');
      this.emit('let request = OwnershipTransferRequest(');
      this.indentLevel++;
      this.emit('entityId: entityId,');
      this.emit('fromParticipantId: "",');
      this.emit('toParticipantId: localParticipantId,');
      this.emit('timestamp: Date().timeIntervalSince1970');
      this.indentLevel--;
      this.emit(')');
      this.emit('Task {');
      this.indentLevel++;
      this.emit('try? await messenger.send(request)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func transferOwnership(entityId: String, toParticipant: String) {');
      this.indentLevel++;
      this.emit('guard entityOwnership[entityId] == localParticipantId else { return }');
      this.emit('entityOwnership[entityId] = toParticipant');
      this.emit('guard let messenger = messenger else { return }');
      this.emit('let request = OwnershipTransferRequest(');
      this.indentLevel++;
      this.emit('entityId: entityId,');
      this.emit('fromParticipantId: localParticipantId,');
      this.emit('toParticipantId: toParticipant,');
      this.emit('timestamp: Date().timeIntervalSince1970');
      this.indentLevel--;
      this.emit(')');
      this.emit('Task {');
      this.indentLevel++;
      this.emit('try? await messenger.send(request)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func isOwnedByLocal(entityId: String) -> Bool {');
      this.indentLevel++;
      this.emit('return entityOwnership[entityId] == localParticipantId');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('private func handleOwnershipTransfer(_ request: OwnershipTransferRequest) {');
      this.indentLevel++;
      this.emit('entityOwnership[request.entityId] = request.toParticipantId');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Local anchor mapping ────────────────────────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_anchor_local')) {
      this.emit('// MARK: - Local Anchor Mapping');
      this.emit('');
      this.emit(
        'func calibrateLocalAnchor(arFrame: ARFrame, sharedReferencePoint: simd_float3) {'
      );
      this.indentLevel++;
      this.emit('// Each participant creates a local ARKit anchor and maps shared positions');
      this.emit('let cameraPos = arFrame.camera.transform.columns.3');
      this.emit(
        'sharedToLocalOffset = simd_float3(cameraPos.x, cameraPos.y, cameraPos.z) - sharedReferencePoint'
      );
      this.emit('localWorldTransform = arFrame.camera.transform');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func sharedToLocal(_ sharedPosition: simd_float3) -> simd_float3 {');
      this.indentLevel++;
      this.emit('return sharedPosition + sharedToLocalOffset');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func localToShared(_ localPosition: simd_float3) -> simd_float3 {');
      this.indentLevel++;
      this.emit('return localPosition - sharedToLocalOffset');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Spatial audio: shareplay_voice_spatial ──────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_voice_spatial')) {
      this.emit('// MARK: - Spatial Audio');
      this.emit('');
      this.emit('func configureSpatialAudio() {');
      this.indentLevel++;
      this.emit(
        '// SpatialAudioExperience.custom(fixedToScene: true) for participant voices'
      );
      this.emit('guard let session = groupSession else { return }');
      this.emit('session.spatialAudioExperience = .custom(fixedToScene: true)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Mute zones: shareplay_mute_zone ─────────────────────────────
    if (this.compositionHasTrait(composition, 'shareplay_mute_zone')) {
      this.emit('// MARK: - Mute Zones');
      this.emit('');
      this.emit(
        'func addMuteZone(center: simd_float3, radius: Float, attenuation: Float = 0.0) {'
      );
      this.indentLevel++;
      this.emit(
        'muteZones.append(MuteZone(center: center, radius: radius, attenuation: attenuation))'
      );
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func audioAttenuation(at position: simd_float3) -> Float {');
      this.indentLevel++;
      this.emit('var minAttenuation: Float = 1.0');
      this.emit('for zone in muteZones {');
      this.indentLevel++;
      this.emit('let distance = simd_distance(position, zone.center)');
      this.emit('if distance < zone.radius {');
      this.indentLevel++;
      this.emit('let factor = distance / zone.radius');
      this.emit(
        'let zoneAttenuation = zone.attenuation + (1.0 - zone.attenuation) * factor'
      );
      this.emit('minAttenuation = min(minAttenuation, zoneAttenuation)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('return minAttenuation');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Cleanup ─────────────────────────────────────────────────────
    this.emit('// MARK: - Cleanup');
    this.emit('');
    this.emit('func endSession() {');
    this.indentLevel++;
    this.emit('tasks.forEach { $0.cancel() }');
    this.emit('tasks.removeAll()');
    this.emit('subscriptions.removeAll()');
    this.emit('groupSession?.end()');
    this.emit('groupSession = nil');
    this.emit('messenger = nil');
    this.emit('isSessionActive = false');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── SwiftUI View ────────────────────────────────────────────────
    this.emit(`// MARK: - ${cls}SharePlayView`);
    this.emit('');
    this.emit(`struct ${cls}SharePlayView: View {`);
    this.indentLevel++;
    this.emit(`@StateObject private var manager = ${cls}SharePlayManager()`);
    this.emit('');
    this.emit('var body: some View {');
    this.indentLevel++;
    this.emit('VStack {');
    this.indentLevel++;

    this.emit('// Session status');
    this.emit('HStack {');
    this.indentLevel++;
    this.emit(
      'Image(systemName: manager.isSessionActive ? "person.2.fill" : "person.2")'
    );
    this.emit(
      'Text(manager.isSessionActive ? "SharePlay Active" : "SharePlay Inactive")'
    );
    this.indentLevel--;
    this.emit('}');
    this.emit('.padding()');
    this.emit('');

    this.emit('// Participant list');
    this.emit('if !manager.participants.isEmpty {');
    this.indentLevel++;
    this.emit('List(manager.participants, id: \\.id) { participant in');
    this.indentLevel++;
    this.emit('HStack {');
    this.indentLevel++;
    this.emit('Image(systemName: "person.circle")');
    this.emit('Text("Participant \\(participant.id)")');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('.frame(maxHeight: 200)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit('// Shared AR scene placeholder');
    this.emit('ZStack {');
    this.indentLevel++;
    this.emit('Color.black.opacity(0.3)');
    this.emit('Text("Shared AR Scene")');
    this.emit('    .foregroundColor(.white)');
    this.indentLevel--;
    this.emit('}');
    this.emit('.frame(maxWidth: .infinity, maxHeight: .infinity)');
    this.emit('.cornerRadius(12)');
    this.emit('.padding()');

    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  // ─── TrueDepth Face Tracking (M.010.14) ─────────────────────────────

  private hasFaceTrackingTraits(composition: HoloComposition): boolean {
    const faceTraitNames: ReadonlyArray<string> = FACE_TRACKING_TRAITS;
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (faceTraitNames.includes(name)) return true;
      }
    }
    return false;
  }

  private generateFaceTrackingFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;

    const hasBlendshapes = this.compositionHasTrait(composition, 'face_blendshapes');
    const hasMesh = this.compositionHasTrait(composition, 'face_mesh');
    const hasTransform = this.compositionHasTrait(composition, 'face_transform');
    const hasAvatarDrive = this.compositionHasTrait(composition, 'face_avatar_drive');
    const hasMirror = this.compositionHasTrait(composition, 'face_blendshape_mirror');
    const hasPuppet = this.compositionHasTrait(composition, 'face_puppet');
    const hasRetarget = this.compositionHasTrait(composition, 'face_retarget');
    const hasEmotion = this.compositionHasTrait(composition, 'face_emotion_detect');
    const hasGaze = this.compositionHasTrait(composition, 'face_gaze_direction');
    const hasTongue = this.compositionHasTrait(composition, 'face_tongue_detect');

    this.emit('// Auto-generated by HoloScript IOSCompiler — TrueDepth Face Tracking');
    this.emit(`// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`);
    this.emit('// Requires iOS 11.0+, ARKit, TrueDepth camera');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import ARKit');
    this.emit('import SceneKit');
    this.emit('import SwiftUI');
    this.emit('import Combine');
    this.emit('');

    // DetectedEmotion enum
    if (hasEmotion) {
      this.emit('/// Emotions derived from ARKit blendshape combinations.');
      this.emit('enum DetectedEmotion: String, CaseIterable {');
      this.indentLevel++;
      this.emit('case neutral = "neutral"');
      this.emit('case happy = "happy"');
      this.emit('case sad = "sad"');
      this.emit('case surprised = "surprised"');
      this.emit('case angry = "angry"');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // FaceTrackingManager class
    this.emit(`class ${cls}FaceTrackingManager: NSObject, ObservableObject, ARSessionDelegate {`);
    this.indentLevel++;

    // Published properties
    this.emit('@Published var isTracking: Bool = false');
    if (hasBlendshapes || hasAvatarDrive || hasMirror || hasPuppet) {
      this.emit('@Published var blendShapes: [ARFaceAnchor.BlendShapeLocation: NSNumber] = [:]');
    }
    if (hasMesh) {
      this.emit('@Published var faceVertexCount: Int = 0');
    }
    if (hasTransform) {
      this.emit('@Published var headTransform: simd_float4x4 = matrix_identity_float4x4');
    }
    if (hasEmotion) {
      this.emit('@Published var currentEmotion: DetectedEmotion = .neutral');
    }
    if (hasGaze) {
      this.emit('@Published var gazeDirection: simd_float3 = simd_float3(0, 0, -1)');
    }
    if (hasTongue) {
      this.emit('@Published var isTongueOut: Bool = false');
    }
    if (hasRetarget) {
      this.emit('@Published var retargetedBlendShapes: [String: Float] = [:]');
      this.emit('var retargetMap: [ARFaceAnchor.BlendShapeLocation: (target: String, scale: Float)] = [:]');
    }
    this.emit('');
    this.emit('private var arSession: ARSession?');
    this.emit('');

    // init
    this.emit('override init() {');
    this.indentLevel++;
    this.emit('super.init()');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // startTracking
    this.emit('func startTracking() {');
    this.indentLevel++;
    this.emit('guard ARFaceTrackingConfiguration.isSupported else {');
    this.indentLevel++;
    this.emit('print("[HoloScript] TrueDepth face tracking not supported on this device")');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('let configuration = ARFaceTrackingConfiguration()');
    if (hasMesh) {
      this.emit('configuration.maximumNumberOfTrackedFaces = 1');
    }
    this.emit('');
    this.emit('let session = ARSession()');
    this.emit('session.delegate = self');
    this.emit('session.run(configuration)');
    this.emit('arSession = session');
    this.emit('');
    this.emit('DispatchQueue.main.async {');
    this.indentLevel++;
    this.emit('self.isTracking = true');
    this.indentLevel--;
    this.emit('}');
    this.emit('print("[HoloScript] Face tracking started")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // stopTracking
    this.emit('func stopTracking() {');
    this.indentLevel++;
    this.emit('arSession?.pause()');
    this.emit('arSession = nil');
    this.emit('DispatchQueue.main.async {');
    this.indentLevel++;
    this.emit('self.isTracking = false');
    this.indentLevel--;
    this.emit('}');
    this.emit('print("[HoloScript] Face tracking stopped")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ARSessionDelegate — session(_ didUpdate:)
    this.emit('// MARK: - ARSessionDelegate');
    this.emit('');
    this.emit('func session(_ session: ARSession, didUpdate anchors: [ARAnchor]) {');
    this.indentLevel++;
    this.emit('guard let faceAnchor = anchors.compactMap({ $0 as? ARFaceAnchor }).first else { return }');
    this.emit('');

    if (hasBlendshapes || hasAvatarDrive || hasMirror || hasPuppet) {
      this.emit('let shapes = faceAnchor.blendShapes');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('self.blendShapes = shapes');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasMesh) {
      this.emit('// Face mesh: 1220 vertices topology');
      this.emit('let geometry = faceAnchor.geometry');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('self.faceVertexCount = geometry.vertices.count');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasTransform) {
      this.emit('// Head position and orientation in world space');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('self.headTransform = faceAnchor.transform');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasGaze) {
      this.emit('// Gaze direction from lookAtPoint');
      this.emit('let lookAt = faceAnchor.lookAtPoint');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('self.gazeDirection = simd_float3(lookAt.x, lookAt.y, lookAt.z)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasTongue) {
      this.emit('// Tongue detection');
      this.emit('let tongueValue = shapes[.tongueOut]?.floatValue ?? 0.0');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('self.isTongueOut = tongueValue > 0.5');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasEmotion) {
      this.emit('// Emotion detection from blendshape combinations');
      this.emit('self.detectEmotion(from: faceAnchor.blendShapes)');
      this.emit('');
    }

    if (hasRetarget) {
      this.emit('// Retarget blendshapes to non-human topology');
      this.emit('self.applyRetargeting(from: faceAnchor.blendShapes)');
      this.emit('');
    }

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // Mirror / Avatar Drive methods
    if (hasMirror || hasAvatarDrive) {
      this.emit('// MARK: - Blendshape Mirror');
      this.emit('');
      this.emit('/// Returns all 52 blendshape coefficients as a dictionary of String to Float.');
      this.emit('func getBlendShapeCoefficients() -> [String: Float] {');
      this.indentLevel++;
      this.emit('var result: [String: Float] = [:]');
      this.emit('for (key, value) in blendShapes {');
      this.indentLevel++;
      this.emit('result[key.rawValue] = value.floatValue');
      this.indentLevel--;
      this.emit('}');
      this.emit('return result');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasAvatarDrive) {
      this.emit('/// Drives avatar morph targets with current face blendshapes.');
      this.emit('func driveAvatar(morphTargets: inout [String: Float]) {');
      this.indentLevel++;
      this.emit('for (key, value) in blendShapes {');
      this.indentLevel++;
      this.emit('morphTargets[key.rawValue] = value.floatValue');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasPuppet) {
      this.emit('// MARK: - Puppet Mode');
      this.emit('');
      this.emit('/// Puppet mode: avatar responds exclusively to face input.');
      this.emit('/// Returns a complete morph target dictionary derived solely from face tracking.');
      this.emit('func getPuppetMorphTargets() -> [String: Float] {');
      this.indentLevel++;
      this.emit('var targets: [String: Float] = [:]');
      this.emit('for (key, value) in blendShapes {');
      this.indentLevel++;
      this.emit('targets[key.rawValue] = value.floatValue');
      this.indentLevel--;
      this.emit('}');
      this.emit('return targets');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasRetarget) {
      this.emit('// MARK: - Retargeting');
      this.emit('');
      this.emit('/// Applies retarget map to remap/scale blendshapes for non-human avatar topologies.');
      this.emit('private func applyRetargeting(from shapes: [ARFaceAnchor.BlendShapeLocation: NSNumber]) {');
      this.indentLevel++;
      this.emit('var result: [String: Float] = [:]');
      this.emit('for (arkitShape, value) in shapes {');
      this.indentLevel++;
      this.emit('if let mapping = retargetMap[arkitShape] {');
      this.indentLevel++;
      this.emit('result[mapping.target] = value.floatValue * mapping.scale');
      this.indentLevel--;
      this.emit('} else {');
      this.indentLevel++;
      this.emit('result[arkitShape.rawValue] = value.floatValue');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('self.retargetedBlendShapes = result');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    if (hasEmotion) {
      this.emit('// MARK: - Emotion Detection');
      this.emit('');
      this.emit('/// Derives emotion from blendshape combinations.');
      this.emit('private func detectEmotion(from shapes: [ARFaceAnchor.BlendShapeLocation: NSNumber]) {');
      this.indentLevel++;
      this.emit('let mouthSmileL = shapes[.mouthSmileLeft]?.floatValue ?? 0.0');
      this.emit('let mouthSmileR = shapes[.mouthSmileRight]?.floatValue ?? 0.0');
      this.emit('let mouthFrownL = shapes[.mouthFrownLeft]?.floatValue ?? 0.0');
      this.emit('let mouthFrownR = shapes[.mouthFrownRight]?.floatValue ?? 0.0');
      this.emit('let eyeWideL = shapes[.eyeWideLeft]?.floatValue ?? 0.0');
      this.emit('let eyeWideR = shapes[.eyeWideRight]?.floatValue ?? 0.0');
      this.emit('let jawOpen = shapes[.jawOpen]?.floatValue ?? 0.0');
      this.emit('let browDownL = shapes[.browDownLeft]?.floatValue ?? 0.0');
      this.emit('let browDownR = shapes[.browDownRight]?.floatValue ?? 0.0');
      this.emit('let mouthPressL = shapes[.mouthPressLeft]?.floatValue ?? 0.0');
      this.emit('let mouthPressR = shapes[.mouthPressRight]?.floatValue ?? 0.0');
      this.emit('');
      this.emit('let emotion: DetectedEmotion');
      this.emit('');
      this.emit('// Happy: mouthSmile > 0.5');
      this.emit('if (mouthSmileL + mouthSmileR) / 2.0 > 0.5 {');
      this.indentLevel++;
      this.emit('emotion = .happy');
      this.indentLevel--;
      this.emit('// Sad: mouthFrown > 0.5');
      this.emit('} else if (mouthFrownL + mouthFrownR) / 2.0 > 0.5 {');
      this.indentLevel++;
      this.emit('emotion = .sad');
      this.indentLevel--;
      this.emit('// Surprised: eyeWide + jawOpen');
      this.emit('} else if ((eyeWideL + eyeWideR) / 2.0 > 0.3) && (jawOpen > 0.3) {');
      this.indentLevel++;
      this.emit('emotion = .surprised');
      this.indentLevel--;
      this.emit('// Angry: browDown + mouthPress');
      this.emit('} else if ((browDownL + browDownR) / 2.0 > 0.4) && ((mouthPressL + mouthPressR) / 2.0 > 0.3) {');
      this.indentLevel++;
      this.emit('emotion = .angry');
      this.indentLevel--;
      this.emit('} else {');
      this.indentLevel++;
      this.emit('emotion = .neutral');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('DispatchQueue.main.async {');
      this.indentLevel++;
      this.emit('self.currentEmotion = emotion');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // SwiftUI preview with live blendshape coefficients
    this.emit('// MARK: - SwiftUI Preview');
    this.emit('');
    this.emit(`struct ${cls}FaceTrackingPreview: View {`);
    this.indentLevel++;
    this.emit(`@StateObject private var manager = ${cls}FaceTrackingManager()`);
    this.emit('');
    this.emit('var body: some View {');
    this.indentLevel++;
    this.emit('VStack(alignment: .leading, spacing: 8) {');
    this.indentLevel++;
    this.emit('Text("Face Tracking").font(.headline)');
    this.emit('Text("Tracking: \\(manager.isTracking ? "Active" : "Inactive")")');
    if (hasBlendshapes || hasAvatarDrive || hasMirror || hasPuppet) {
      this.emit('');
      this.emit('Text("Blendshapes (\\(manager.blendShapes.count))").font(.subheadline)');
      this.emit('ScrollView {');
      this.indentLevel++;
      this.emit('ForEach(Array(manager.blendShapes.keys).sorted(by: { $0.rawValue < $1.rawValue }), id: \\.rawValue) { key in');
      this.indentLevel++;
      this.emit('HStack {');
      this.indentLevel++;
      this.emit('Text(key.rawValue).font(.caption).frame(width: 200, alignment: .leading)');
      this.emit('ProgressView(value: Double(truncating: manager.blendShapes[key] ?? 0))');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
    }
    if (hasEmotion) {
      this.emit('Text("Emotion: \\(manager.currentEmotion.rawValue)").font(.subheadline)');
    }
    if (hasGaze) {
      this.emit('Text("Gaze: \\(manager.gazeDirection.x, specifier: "%.2f"), \\(manager.gazeDirection.y, specifier: "%.2f"), \\(manager.gazeDirection.z, specifier: "%.2f")").font(.caption)');
    }
    if (hasTongue) {
      this.emit('Text("Tongue Out: \\(manager.isTongueOut ? "Yes" : "No")").font(.caption)');
    }
    if (hasMesh) {
      this.emit('Text("Face Vertices: \\(manager.faceVertexCount)").font(.caption)');
    }
    this.indentLevel--;
    this.emit('}');
    this.emit('.padding()');
    this.emit('.onAppear { manager.startTracking() }');
    this.emit('.onDisappear { manager.stopTracking() }');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit(`#Preview {`);
    this.indentLevel++;
    this.emit(`${cls}FaceTrackingPreview()`);
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  // ─── UWB Positioning (iOS 16+, Nearby Interaction — M.010.13) ────────

  private hasUWBPositioningTraits(composition: HoloComposition): boolean {
    const uwbNames: ReadonlyArray<string> = UWB_POSITIONING_TRAITS;
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (uwbNames.includes(name)) return true;
      }
    }
    return false;
  }

  private generateUWBPositioningFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;

    this.emit('// Auto-generated by HoloScript IOSCompiler — UWB Positioning');
    this.emit(
      `// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`
    );
    this.emit('// Requires iOS 16+, U1/U2 chip (iPhone 11+), Nearby Interaction framework');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import NearbyInteraction');
    this.emit('import MultipeerConnectivity');
    this.emit('import ARKit');
    this.emit('import SceneKit');
    this.emit('import SwiftUI');
    if (this.options.useCombine) {
      this.emit('import Combine');
    }
    this.emit('');

    // ─── Peer model ────────────────────────────────────────────────
    this.emit('// MARK: - UWB Peer Model');
    this.emit('');
    this.emit('struct UWBPeer: Identifiable, Hashable {');
    this.indentLevel++;
    this.emit('let id: String');
    this.emit('let discoveryToken: NIDiscoveryToken');
    this.emit('var distance: Float?');
    this.emit('var direction: simd_float3?');
    this.emit('var lastSeen: Date = Date()');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Delegate protocol ─────────────────────────────────────────
    this.emit('// MARK: - UWB Positioning Delegate');
    this.emit('');
    this.emit(`protocol ${cls}UWBDelegate: AnyObject {`);
    this.indentLevel++;
    this.emit('func didDiscoverPeer(_ peer: UWBPeer)');
    this.emit('func didUpdatePeer(_ peer: UWBPeer)');
    this.emit('func didLosePeer(_ peerId: String)');
    if (this.compositionHasTrait(composition, 'uwb_handoff')) {
      this.emit('func didReceiveHandoff(entityId: String, from peer: UWBPeer)');
    }
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Main manager class ────────────────────────────────────────
    this.emit('// MARK: - UWB Positioning Manager');
    this.emit('');
    this.emit(`class ${cls}UWBManager: NSObject, ObservableObject {`);
    this.indentLevel++;
    this.emit('');
    this.emit('@Published var peers: [String: UWBPeer] = [:]');
    this.emit('@Published var isScanning = false');
    this.emit('');
    this.emit(`weak var delegate: ${cls}UWBDelegate?`);
    this.emit('');
    this.emit('private var niSession: NISession?');
    this.emit('private var peerTokenMap: [NIDiscoveryToken: String] = [:]');
    this.emit('private var mcSession: MCSession?');
    this.emit('private var mcAdvertiser: MCNearbyServiceAdvertiser?');
    this.emit('private var mcBrowser: MCNearbyServiceBrowser?');
    this.emit('private let serviceType = "holo-uwb"');
    this.emit('private let localPeerId = MCPeerID(displayName: UIDevice.current.name)');
    if (this.options.useCombine) {
      this.emit('private var cancellables = Set<AnyCancellable>()');
    }
    this.emit('');

    // ─── init ──────────────────────────────────────────────────────
    this.emit('override init() {');
    this.indentLevel++;
    this.emit('super.init()');
    this.emit('setupNISession()');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── NISession setup ───────────────────────────────────────────
    this.emit('// MARK: - NISession Setup');
    this.emit('');
    this.emit('private func setupNISession() {');
    this.indentLevel++;
    this.emit('guard NISession.isSupported else {');
    this.indentLevel++;
    this.emit('print("[UWB] Nearby Interaction not supported on this device")');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('niSession = NISession()');
    this.emit('niSession?.delegate = self');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── uwb_discover: start/stop scanning ─────────────────────────
    if (this.compositionHasTrait(composition, 'uwb_discover')) {
      this.emit('// MARK: - Discovery (uwb_discover)');
      this.emit('');
      this.emit('func startDiscovery() {');
      this.indentLevel++;
      this.emit('guard !isScanning else { return }');
      this.emit('');
      this.emit('let session = MCSession(peer: localPeerId, securityIdentity: nil, encryptionPreference: .required)');
      this.emit('session.delegate = self');
      this.emit('mcSession = session');
      this.emit('');
      this.emit('mcAdvertiser = MCNearbyServiceAdvertiser(peer: localPeerId, discoveryInfo: nil, serviceType: serviceType)');
      this.emit('mcAdvertiser?.delegate = self');
      this.emit('mcAdvertiser?.startAdvertisingPeer()');
      this.emit('');
      this.emit('mcBrowser = MCNearbyServiceBrowser(peer: localPeerId, serviceType: serviceType)');
      this.emit('mcBrowser?.delegate = self');
      this.emit('mcBrowser?.startBrowsingForPeers()');
      this.emit('');
      this.emit('isScanning = true');
      this.emit('print("[UWB] Discovery started")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func stopDiscovery() {');
      this.indentLevel++;
      this.emit('mcAdvertiser?.stopAdvertisingPeer()');
      this.emit('mcBrowser?.stopBrowsingForPeers()');
      this.emit('mcSession?.disconnect()');
      this.emit('isScanning = false');
      this.emit('print("[UWB] Discovery stopped")');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── uwb_range: distance measurement ──────────────────────────
    if (this.compositionHasTrait(composition, 'uwb_range')) {
      this.emit('// MARK: - Ranging (uwb_range)');
      this.emit('');
      this.emit('func distanceToPeer(_ peerId: String) -> Float? {');
      this.indentLevel++;
      this.emit('return peers[peerId]?.distance');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func nearestPeer() -> UWBPeer? {');
      this.indentLevel++;
      this.emit('return peers.values');
      this.emit('    .filter { $0.distance != nil }');
      this.emit('    .min { ($0.distance ?? .infinity) < ($1.distance ?? .infinity) }');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── uwb_direction: direction vector ───────────────────────────
    if (this.compositionHasTrait(composition, 'uwb_direction')) {
      this.emit('// MARK: - Direction (uwb_direction)');
      this.emit('');
      this.emit('func directionToPeer(_ peerId: String) -> simd_float3? {');
      this.indentLevel++;
      this.emit('return peers[peerId]?.direction');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func azimuthToPeer(_ peerId: String) -> Float? {');
      this.indentLevel++;
      this.emit('guard let dir = peers[peerId]?.direction else { return nil }');
      this.emit('return atan2(dir.x, dir.z)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func elevationToPeer(_ peerId: String) -> Float? {');
      this.indentLevel++;
      this.emit('guard let dir = peers[peerId]?.direction else { return nil }');
      this.emit('return asin(dir.y)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── uwb_handoff: holographic content handoff ──────────────────
    if (this.compositionHasTrait(composition, 'uwb_handoff')) {
      this.emit('// MARK: - Handoff (uwb_handoff)');
      this.emit('');
      this.emit('func handoffEntity(_ entityId: String, toPeer peerId: String) {');
      this.indentLevel++;
      this.emit('guard let session = mcSession else {');
      this.indentLevel++;
      this.emit('print("[UWB] No active session for handoff")');
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('let payload: [String: String] = [');
      this.indentLevel++;
      this.emit('"type": "uwb_handoff",');
      this.emit('"entityId": entityId,');
      this.emit('"senderId": localPeerId.displayName');
      this.indentLevel--;
      this.emit(']');
      this.emit('');
      this.emit('guard let data = try? JSONSerialization.data(withJSONObject: payload) else { return }');
      this.emit('');
      this.emit('if let mcPeer = session.connectedPeers.first(where: { $0.displayName == peerId }) {');
      this.indentLevel++;
      this.emit('try? session.send(data, toPeers: [mcPeer], with: .reliable)');
      this.emit('print("[UWB] Handoff entity \\(entityId) to \\(peerId)")');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func handoffToNearest(_ entityId: String) {');
      this.indentLevel++;
      this.emit('guard let nearest = nearestPeer() else {');
      this.indentLevel++;
      this.emit('print("[UWB] No nearby peer for handoff")');
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('handoffEntity(entityId, toPeer: nearest.id)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── uwb_anchor_peer: use peer as spatial anchor ───────────────
    if (this.compositionHasTrait(composition, 'uwb_anchor_peer')) {
      this.emit('// MARK: - Peer Anchor (uwb_anchor_peer)');
      this.emit('');
      this.emit('func anchorPosition(for peerId: String) -> SCNVector3? {');
      this.indentLevel++;
      this.emit('guard let peer = peers[peerId],');
      this.emit('      let distance = peer.distance,');
      this.emit('      let direction = peer.direction else { return nil }');
      this.emit('let pos = direction * distance');
      this.emit('return SCNVector3(pos.x, pos.y, pos.z)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── uwb_controller: use device as spatial controller ──────────
    if (this.compositionHasTrait(composition, 'uwb_controller')) {
      this.emit('// MARK: - Spatial Controller (uwb_controller)');
      this.emit('');
      this.emit('func controllerTransform(for peerId: String) -> SCNMatrix4? {');
      this.indentLevel++;
      this.emit('guard let peer = peers[peerId],');
      this.emit('      let distance = peer.distance,');
      this.emit('      let direction = peer.direction else { return nil }');
      this.emit('let pos = direction * distance');
      this.emit('let lookAt = SCNMatrix4LookAt(');
      this.indentLevel++;
      this.emit('SCNVector3(pos.x, pos.y, pos.z),');
      this.emit('SCNVector3Zero,');
      this.emit('SCNVector3(0, 1, 0))');
      this.indentLevel--;
      this.emit('return lookAt');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Shared token exchange helper ──────────────────────────────
    this.emit('// MARK: - Token Exchange');
    this.emit('');
    this.emit('private func shareDiscoveryToken(with mcPeer: MCPeerID) {');
    this.indentLevel++;
    this.emit('guard let token = niSession?.discoveryToken,');
    this.emit('      let data = try? NSKeyedArchiver.archivedData(withRootObject: token, requiringSecureCoding: true) else { return }');
    this.emit('try? mcSession?.send(data, toPeers: [mcPeer], with: .reliable)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    this.emit('private func startRanging(with token: NIDiscoveryToken) {');
    this.indentLevel++;
    this.emit('let config = NINearbyPeerConfiguration(peerToken: token)');
    this.emit('niSession?.run(config)');
    this.emit('print("[UWB] Ranging session started")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Cleanup ───────────────────────────────────────────────────
    this.emit('func invalidate() {');
    this.indentLevel++;
    this.emit('niSession?.invalidate()');
    this.emit('mcAdvertiser?.stopAdvertisingPeer()');
    this.emit('mcBrowser?.stopBrowsingForPeers()');
    this.emit('mcSession?.disconnect()');
    this.emit('peers.removeAll()');
    this.emit('isScanning = false');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── NISessionDelegate ─────────────────────────────────────────
    this.emit('// MARK: - NISessionDelegate');
    this.emit('');
    this.emit(`extension ${cls}UWBManager: NISessionDelegate {`);
    this.indentLevel++;
    this.emit('');
    this.emit('func session(_ session: NISession, didUpdate nearbyObjects: [NINearbyObject]) {');
    this.indentLevel++;
    this.emit('for obj in nearbyObjects {');
    this.indentLevel++;
    this.emit('guard let peerId = peerTokenMap[obj.discoveryToken] else { continue }');
    this.emit('var peer = peers[peerId] ?? UWBPeer(id: peerId, discoveryToken: obj.discoveryToken)');
    this.emit('peer.distance = obj.distance');
    this.emit('peer.direction = obj.direction');
    this.emit('peer.lastSeen = Date()');
    this.emit('peers[peerId] = peer');
    this.emit('delegate?.didUpdatePeer(peer)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('func session(_ session: NISession, didRemove nearbyObjects: [NINearbyObject], reason: NINearbyObject.RemovalReason) {');
    this.indentLevel++;
    this.emit('for obj in nearbyObjects {');
    this.indentLevel++;
    this.emit('if let peerId = peerTokenMap[obj.discoveryToken] {');
    this.indentLevel++;
    this.emit('peers.removeValue(forKey: peerId)');
    this.emit('peerTokenMap.removeValue(forKey: obj.discoveryToken)');
    this.emit('delegate?.didLosePeer(peerId)');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('func sessionWasSuspended(_ session: NISession) {');
    this.indentLevel++;
    this.emit('print("[UWB] Session suspended")');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('func sessionSuspensionEnded(_ session: NISession) {');
    this.indentLevel++;
    this.emit('print("[UWB] Session suspension ended — restarting")');
    this.emit('guard let token = session.discoveryToken else { return }');
    this.emit('let config = NINearbyPeerConfiguration(peerToken: token)');
    this.emit('session.run(config)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('func session(_ session: NISession, didInvalidateWith error: Error) {');
    this.indentLevel++;
    this.emit('print("[UWB] Session invalidated: \\(error.localizedDescription)")');
    this.emit('setupNISession()');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── MCSessionDelegate ─────────────────────────────────────────
    this.emit('// MARK: - MCSessionDelegate');
    this.emit('');
    this.emit(`extension ${cls}UWBManager: MCSessionDelegate {`);
    this.indentLevel++;
    this.emit('');
    this.emit('func session(_ session: MCSession, peer peerID: MCPeerID, didChange state: MCSessionState) {');
    this.indentLevel++;
    this.emit('switch state {');
    this.emit('case .connected:');
    this.indentLevel++;
    this.emit('print("[UWB] Peer connected: \\(peerID.displayName)")');
    this.emit('shareDiscoveryToken(with: peerID)');
    this.indentLevel--;
    this.emit('case .notConnected:');
    this.indentLevel++;
    this.emit('print("[UWB] Peer disconnected: \\(peerID.displayName)")');
    this.emit('peers.removeValue(forKey: peerID.displayName)');
    this.emit('delegate?.didLosePeer(peerID.displayName)');
    this.indentLevel--;
    this.emit('case .connecting:');
    this.indentLevel++;
    this.emit('print("[UWB] Peer connecting: \\(peerID.displayName)")');
    this.indentLevel--;
    this.emit('@unknown default:');
    this.indentLevel++;
    this.emit('break');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('func session(_ session: MCSession, didReceive data: Data, fromPeer peerID: MCPeerID) {');
    this.indentLevel++;

    // Handle discovery token exchange
    this.emit('// Try to decode as NI discovery token');
    this.emit('if let token = try? NSKeyedUnarchiver.unarchivedObject(ofClass: NIDiscoveryToken.self, from: data) {');
    this.indentLevel++;
    this.emit('peerTokenMap[token] = peerID.displayName');
    this.emit('let peer = UWBPeer(id: peerID.displayName, discoveryToken: token)');
    this.emit('peers[peerID.displayName] = peer');
    this.emit('delegate?.didDiscoverPeer(peer)');
    this.emit('startRanging(with: token)');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');

    // Handle handoff messages
    if (this.compositionHasTrait(composition, 'uwb_handoff')) {
      this.emit('');
      this.emit('// Try to decode as handoff payload');
      this.emit('if let json = try? JSONSerialization.jsonObject(with: data) as? [String: String],');
      this.emit('   json["type"] == "uwb_handoff",');
      this.emit('   let entityId = json["entityId"],');
      this.emit('   let peer = peers[peerID.displayName] {');
      this.indentLevel++;
      this.emit('delegate?.didReceiveHandoff(entityId: entityId, from: peer)');
      this.indentLevel--;
      this.emit('}');
    }

    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('func session(_ session: MCSession, didReceive stream: InputStream, withName streamName: String, fromPeer peerID: MCPeerID) {}');
    this.emit('func session(_ session: MCSession, didStartReceivingResourceWithName resourceName: String, fromPeer peerID: MCPeerID, with progress: Progress) {}');
    this.emit('func session(_ session: MCSession, didFinishReceivingResourceWithName resourceName: String, fromPeer peerID: MCPeerID, at localURL: URL?, withError error: Error?) {}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── MCNearbyServiceAdvertiserDelegate ──────────────────────────
    this.emit('// MARK: - MCNearbyServiceAdvertiserDelegate');
    this.emit('');
    this.emit(`extension ${cls}UWBManager: MCNearbyServiceAdvertiserDelegate {`);
    this.indentLevel++;
    this.emit('');
    this.emit('func advertiser(_ advertiser: MCNearbyServiceAdvertiser, didReceiveInvitationFromPeer peerID: MCPeerID, withContext context: Data?, invitationHandler: @escaping (Bool, MCSession?) -> Void) {');
    this.indentLevel++;
    this.emit('invitationHandler(true, mcSession)');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── MCNearbyServiceBrowserDelegate ─────────────────────────────
    this.emit('// MARK: - MCNearbyServiceBrowserDelegate');
    this.emit('');
    this.emit(`extension ${cls}UWBManager: MCNearbyServiceBrowserDelegate {`);
    this.indentLevel++;
    this.emit('');
    this.emit('func browser(_ browser: MCNearbyServiceBrowser, foundPeer peerID: MCPeerID, withDiscoveryInfo info: [String: String]?) {');
    this.indentLevel++;
    this.emit('guard let session = mcSession else { return }');
    this.emit('browser.invitePeer(peerID, to: session, withContext: nil, timeout: 10)');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('func browser(_ browser: MCNearbyServiceBrowser, lostPeer peerID: MCPeerID) {');
    this.indentLevel++;
    this.emit('print("[UWB] Lost peer: \\(peerID.displayName)")');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── SwiftUI Preview ──────────────────────────────────────────
    this.emit('// MARK: - SwiftUI Preview');
    this.emit('');
    this.emit(`struct ${cls}UWBPreview: View {`);
    this.indentLevel++;
    this.emit(`@StateObject private var uwb = ${cls}UWBManager()`);
    this.emit('');
    this.emit('var body: some View {');
    this.indentLevel++;
    this.emit('VStack {');
    this.indentLevel++;
    this.emit('Text("UWB Peers: \\(uwb.peers.count)")');
    this.emit('ForEach(Array(uwb.peers.values)) { peer in');
    this.indentLevel++;
    this.emit('HStack {');
    this.indentLevel++;
    this.emit('Text(peer.id)');
    this.emit('Spacer()');
    this.emit('if let dist = peer.distance {');
    this.indentLevel++;
    this.emit('Text(String(format: "%.2f m", dist))');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('#Preview {');
    this.indentLevel++;
    this.emit(`${cls}UWBPreview()`);
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }

  // === AirPods Spatial Audio (iOS 15+, AVFoundation PHASE + CMHeadphoneMotionManager — M.010.11) ===

  private hasSpatialAudioTraits(composition: HoloComposition): boolean {
    const spatialNames: ReadonlyArray<string> = AIRPODS_SPATIAL_AUDIO_TRAITS;
    for (const obj of composition.objects || []) {
      for (const trait of obj.traits || []) {
        const name = typeof trait === 'string' ? trait : trait.name;
        if (spatialNames.includes(name)) return true;
      }
    }
    return false;
  }

  private collectSpatialAudioTraits(composition: HoloComposition): Set<string> {
    const spatialNames: ReadonlyArray<string> = AIRPODS_SPATIAL_AUDIO_TRAITS;
    const found = new Set<string>();
    for (const obj of composition.objects || []) {
      for (const t of obj.traits || []) {
        const name = typeof t === 'string' ? t : t.name;
        if (spatialNames.includes(name)) found.add(name);
      }
    }
    return found;
  }

  private generateSpatialAudioFile(composition: HoloComposition): string {
    this.lines = [];
    this.indentLevel = 0;

    const cls = this.options.className;
    const traits = this.collectSpatialAudioTraits(composition);

    this.emit('// Auto-generated by HoloScript IOSCompiler — AirPods Spatial Audio');
    this.emit(
      `// Source: composition "${this.escapeStringValue(composition.name as string, 'Swift')}"`
    );
    this.emit('// Requires iOS 15+, AirPods Pro/Max with head tracking');
    this.emit('// Do not edit manually — regenerate from .holo source');
    this.emit('');
    this.emit('import Foundation');
    this.emit('import AVFoundation');
    this.emit('import PHASE');
    this.emit('import ARKit');
    this.emit('import CoreMotion');
    this.emit('import SwiftUI');
    this.emit('import Combine');
    this.emit('');

    // ─── Audio source model ───────────────────────────────────────────
    this.emit('// MARK: - Audio Source Model');
    this.emit('');
    this.emit('struct HoloAudioSource: Identifiable {');
    this.indentLevel++;
    this.emit('let id: String');
    this.emit('var position: simd_float3');
    this.emit('var audioFile: String');
    this.emit('var gain: Float = 1.0');
    if (traits.has('audio_falloff')) {
      this.emit('var falloffModel: AudioFalloffModel = .inverseDistance');
      this.emit('var minDistance: Float = 0.5');
      this.emit('var maxDistance: Float = 20.0');
    }
    if (traits.has('audio_directivity')) {
      this.emit('var innerConeAngle: Float = 90.0');
      this.emit('var outerConeAngle: Float = 180.0');
      this.emit('var outerGain: Float = 0.2');
    }
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    if (traits.has('audio_falloff')) {
      this.emit('enum AudioFalloffModel {');
      this.indentLevel++;
      this.emit('case inverseDistance');
      this.emit('case linear');
      this.emit('case exponential');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Main manager class ──────────────────────────────────────────
    this.emit(`// MARK: - ${cls}SpatialAudioManager`);
    this.emit('');
    this.emit(`class ${cls}SpatialAudioManager: NSObject, ObservableObject {`);
    this.indentLevel++;

    // Properties
    this.emit('');
    this.emit('private var phaseEngine: PHASEEngine?');
    this.emit('private var listener: PHASEListener?');
    this.emit('private var sources: [String: PHASESource] = [:]');
    this.emit('private var soundEvents: [String: PHASESoundEvent] = [:]');
    this.emit('@Published var isActive: Bool = false');
    this.emit('@Published var audioSources: [HoloAudioSource] = []');
    if (traits.has('audio_head_track')) {
      this.emit('');
      this.emit('private let headphoneMotionManager = CMHeadphoneMotionManager()');
      this.emit('@Published var headTrackingActive: Bool = false');
    }
    this.emit('');
    this.emit('private var cancellables = Set<AnyCancellable>()');
    this.emit('private var arSession: ARSession?');
    this.emit('');

    // ─── Setup ──────────────────────────────────────────────────────
    this.emit('// MARK: - Setup');
    this.emit('');
    this.emit('func setupSpatialAudio(arSession: ARSession) {');
    this.indentLevel++;
    this.emit('self.arSession = arSession');
    this.emit('');
    this.emit('let engine = PHASEEngine(updateMode: .automatic)');
    this.emit('self.phaseEngine = engine');
    this.emit('');
    this.emit('// Create listener at camera position');
    this.emit('let listener = PHASEListener(engine: engine)');
    this.emit('self.listener = listener');
    this.emit('try? engine.rootObject.addChild(listener)');
    this.emit('');
    this.emit('try? engine.start()');
    this.emit('isActive = true');
    if (traits.has('audio_head_track')) {
      this.emit('');
      this.emit('startHeadTracking()');
    }
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Head tracking (audio_head_track) ────────────────────────────
    if (traits.has('audio_head_track')) {
      this.emit('// MARK: - Head Tracking (CMHeadphoneMotionManager)');
      this.emit('');
      this.emit('func startHeadTracking() {');
      this.indentLevel++;
      this.emit('guard CMHeadphoneMotionManager.authorizationStatus() == .authorized ||');
      this.emit('      CMHeadphoneMotionManager.authorizationStatus() == .notDetermined else {');
      this.indentLevel++;
      this.emit('print("[SpatialAudio] Head tracking not authorized")');
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('guard headphoneMotionManager.isDeviceMotionAvailable else {');
      this.indentLevel++;
      this.emit('print("[SpatialAudio] AirPods head tracking not available")');
      this.emit('return');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('headphoneMotionManager.startDeviceMotionUpdates(to: .main) { [weak self] motion, error in');
      this.indentLevel++;
      this.emit('guard let self = self, let motion = motion else { return }');
      this.emit('self.headTrackingActive = true');
      this.emit('self.updateListenerOrientation(from: motion)');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func stopHeadTracking() {');
      this.indentLevel++;
      this.emit('headphoneMotionManager.stopDeviceMotionUpdates()');
      this.emit('headTrackingActive = false');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('private func updateListenerOrientation(from motion: CMDeviceMotion) {');
      this.indentLevel++;
      this.emit('guard let listener = self.listener else { return }');
      this.emit('let attitude = motion.attitude');
      this.emit('let rotation = simd_quatf(');
      this.indentLevel++;
      this.emit('angle: Float(attitude.yaw),');
      this.emit('axis: simd_float3(0, 1, 0)');
      this.indentLevel--;
      this.emit(')');
      this.emit('listener.transform = simd_float4x4(rotation)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── 3D audio sources (audio_source_3d) ─────────────────────────
    if (traits.has('audio_source_3d')) {
      this.emit('// MARK: - 3D Audio Sources');
      this.emit('');
      this.emit('func addAudioSource(_ source: HoloAudioSource) {');
      this.indentLevel++;
      this.emit('guard let engine = phaseEngine else { return }');
      this.emit('');
      this.emit('let phaseSource = PHASESource(engine: engine)');
      this.emit('var transform = matrix_identity_float4x4');
      this.emit('transform.columns.3 = simd_float4(source.position, 1.0)');
      this.emit('phaseSource.transform = transform');
      this.emit('try? engine.rootObject.addChild(phaseSource)');
      this.emit('');
      this.emit('sources[source.id] = phaseSource');
      this.emit('audioSources.append(source)');
      this.emit('');
      this.emit('loadAndPlayAudio(sourceId: source.id, audioFile: source.audioFile, gain: source.gain)');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func updateSourcePosition(id: String, position: simd_float3) {');
      this.indentLevel++;
      this.emit('guard let source = sources[id] else { return }');
      this.emit('var transform = matrix_identity_float4x4');
      this.emit('transform.columns.3 = simd_float4(position, 1.0)');
      this.emit('source.transform = transform');
      this.emit('if let idx = audioSources.firstIndex(where: { $0.id == id }) {');
      this.indentLevel++;
      this.emit('audioSources[idx].position = position');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
      this.emit('func removeAudioSource(id: String) {');
      this.indentLevel++;
      this.emit('soundEvents[id]?.stopAndInvalidate()');
      this.emit('soundEvents.removeValue(forKey: id)');
      this.emit('sources[id]?.parent?.removeChild(sources[id]!)');
      this.emit('sources.removeValue(forKey: id)');
      this.emit('audioSources.removeAll { $0.id == id }');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Audio loading & playback pipeline ──────────────────────────
    this.emit('// MARK: - Audio Pipeline');
    this.emit('');
    this.emit('private func loadAndPlayAudio(sourceId: String, audioFile: String, gain: Float) {');
    this.indentLevel++;
    this.emit('guard let engine = phaseEngine, let source = sources[sourceId] else { return }');
    this.emit('');
    this.emit('guard let url = Bundle.main.url(forResource: audioFile, withExtension: nil) else {');
    this.indentLevel++;
    this.emit('print("[SpatialAudio] Audio file not found: \\(audioFile)")');
    this.emit('return');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit('do {');
    this.indentLevel++;
    this.emit('try engine.assetRegistry.registerSoundAsset(');
    this.indentLevel++;
    this.emit('url: url,');
    this.emit('identifier: sourceId,');
    this.emit('assetType: .resident,');
    this.emit('channelLayout: nil,');
    this.emit('normalizationMode: .dynamic');
    this.indentLevel--;
    this.emit(')');
    this.emit('');

    // Build spatial pipeline
    this.emit('let channelLayout = AVAudioChannelLayout(layoutTag: kAudioChannelLayoutTag_Mono)!');
    this.emit('let samplerNodeDef = PHASESamplerNodeDefinition(');
    this.indentLevel++;
    this.emit('soundAssetIdentifier: sourceId,');
    this.emit('channelLayout: channelLayout');
    this.indentLevel--;
    this.emit(')');
    this.emit('samplerNodeDef.playbackMode = .looping');
    this.emit('samplerNodeDef.setCalibrationMode(calibrationMode: .relativeSpl, level: Double(gain * 20))');
    this.emit('');

    // Spatial pipeline with distance attenuation
    if (traits.has('audio_falloff')) {
      this.emit('// Distance-based falloff (audio_falloff)');
      this.emit('let distanceModelParameters = PHASEGeometricSpreadingDistanceModelParameters()');
      this.emit('distanceModelParameters.fadeOutParameters = PHASEDistanceModelFadeOutParameters(');
      this.indentLevel++;
      this.emit('cullDistance: 20.0');
      this.indentLevel--;
      this.emit(')');
      this.emit('distanceModelParameters.rolloffFactor = 1.0');
      this.emit('');
      this.emit('let spatialPipelineDef = PHASESpatialPipeline(flags: [.directPathTransmission])!');
      this.emit('spatialPipelineDef.entries[PHASESpatialCategory.directPath]!.sendLevel = Double(gain)');
      this.emit('');
      this.emit('let spatialMixerDef = PHASESpatialMixerDefinition(');
      this.indentLevel++;
      this.emit('spatialPipeline: spatialPipelineDef');
      this.indentLevel--;
      this.emit(')');
      this.emit('spatialMixerDef.distanceModelParameters = distanceModelParameters');
    } else {
      this.emit('let spatialPipelineDef = PHASESpatialPipeline(flags: [.directPathTransmission])!');
      this.emit('spatialPipelineDef.entries[PHASESpatialCategory.directPath]!.sendLevel = Double(gain)');
      this.emit('');
      this.emit('let spatialMixerDef = PHASESpatialMixerDefinition(');
      this.indentLevel++;
      this.emit('spatialPipeline: spatialPipelineDef');
      this.indentLevel--;
      this.emit(')');
    }
    this.emit('');

    // Directivity cone
    if (traits.has('audio_directivity')) {
      this.emit('// Directional audio cone (audio_directivity)');
      this.emit('let directivityModelParameters = PHASECardioidDirectivityModelParameters(');
      this.indentLevel++;
      this.emit('subbandParameters: [');
      this.indentLevel++;
      this.emit('PHASECardioidDirectivityModelSubbandParameters()');
      this.indentLevel--;
      this.emit(']');
      this.indentLevel--;
      this.emit(')');
      this.emit('spatialMixerDef.sourceDirectivityModelParameters = directivityModelParameters');
      this.emit('');
    }

    this.emit('let soundEventNodeDef = PHASEMixerNodeDefinition(');
    this.indentLevel++;
    this.emit('spatialMixerDefinition: spatialMixerDef');
    this.indentLevel--;
    this.emit(')');
    this.emit('');
    this.emit('let soundEvent = try PHASESoundEvent(');
    this.indentLevel++;
    this.emit('engine: engine,');
    this.emit('rootNode: samplerNodeDef,');
    this.emit('mixerNode: soundEventNodeDef,');
    this.emit('source: source,');
    this.emit('listener: self.listener!');
    this.indentLevel--;
    this.emit(')');
    this.emit('');
    this.emit('soundEvents[sourceId] = soundEvent');
    this.emit('soundEvent.start()');
    this.indentLevel--;
    this.emit('} catch {');
    this.indentLevel++;
    this.emit('print("[SpatialAudio] Error: \\(error)")');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── ARKit world tracking integration ────────────────────────────
    this.emit('// MARK: - ARKit Integration');
    this.emit('');
    this.emit('func updateListenerFromCamera(frame: ARFrame) {');
    this.indentLevel++;
    this.emit('listener?.transform = frame.camera.transform');
    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── Reverb matching (audio_reverb_match) ───────────────────────
    if (traits.has('audio_reverb_match')) {
      this.emit('// MARK: - Room Reverb Matching (audio_reverb_match)');
      this.emit('');
      this.emit('func configureReverbFromRoom(preset: PHASEReverbPreset = .mediumRoom) {');
      this.indentLevel++;
      this.emit('guard let engine = phaseEngine else { return }');
      this.emit('let reverb = PHASEMediumParameters()');
      this.emit('reverb.preset = preset');
      this.emit('engine.defaultReverbPreset = preset');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Audio occlusion (audio_occlusion) ───────────────────────────
    if (traits.has('audio_occlusion')) {
      this.emit('// MARK: - Audio Occlusion (audio_occlusion)');
      this.emit('');
      this.emit('func updateOcclusionForSource(id: String, occluded: Bool, attenuationDb: Float = -12.0) {');
      this.indentLevel++;
      this.emit('guard let event = soundEvents[id] else { return }');
      this.emit('if occluded {');
      this.indentLevel++;
      this.emit('// Reduce level when occluded by physical/holographic objects');
      this.emit('event.setMixerParameter(');
      this.indentLevel++;
      this.emit('value: Double(attenuationDb),');
      this.emit('parameter: PHASESoundEventNodeDefinition.MixerParameter.leftInput');
      this.indentLevel--;
      this.emit(')');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
      this.emit('');
    }

    // ─── Cleanup ─────────────────────────────────────────────────────
    this.emit('// MARK: - Cleanup');
    this.emit('');
    this.emit('func teardown() {');
    this.indentLevel++;
    this.emit('for (_, event) in soundEvents {');
    this.indentLevel++;
    this.emit('event.stopAndInvalidate()');
    this.indentLevel--;
    this.emit('}');
    this.emit('soundEvents.removeAll()');
    this.emit('sources.removeAll()');
    if (traits.has('audio_head_track')) {
      this.emit('stopHeadTracking()');
    }
    this.emit('phaseEngine?.stop()');
    this.emit('phaseEngine = nil');
    this.emit('isActive = false');
    this.indentLevel--;
    this.emit('}');

    this.indentLevel--;
    this.emit('}');
    this.emit('');

    // ─── SwiftUI View ────────────────────────────────────────────────
    this.emit(`// MARK: - ${cls}SpatialAudioView`);
    this.emit('');
    this.emit(`struct ${cls}SpatialAudioView: View {`);
    this.indentLevel++;
    this.emit(`@ObservedObject var manager: ${cls}SpatialAudioManager`);
    this.emit('');
    this.emit('var body: some View {');
    this.indentLevel++;
    this.emit('VStack {');
    this.indentLevel++;
    this.emit('HStack {');
    this.indentLevel++;
    this.emit('Image(systemName: manager.isActive ? "airpodspro" : "airpods")');
    this.emit('Text(manager.isActive ? "Spatial Audio Active" : "Spatial Audio Inactive")');
    this.indentLevel--;
    this.emit('}');
    this.emit('.font(.headline)');
    if (traits.has('audio_head_track')) {
      this.emit('');
      this.emit('if manager.headTrackingActive {');
      this.indentLevel++;
      this.emit('Label("Head Tracking", systemImage: "person.wave.2")');
      this.indentLevel--;
      this.emit('}');
    }
    if (traits.has('audio_source_3d')) {
      this.emit('');
      this.emit('List(manager.audioSources) { source in');
      this.indentLevel++;
      this.emit('HStack {');
      this.indentLevel++;
      this.emit('Image(systemName: "speaker.wave.3")');
      this.emit('Text(source.id)');
      this.emit('Spacer()');
      this.emit('Text(String(format: "(%.1f, %.1f, %.1f)", source.position.x, source.position.y, source.position.z))');
      this.indentLevel--;
      this.emit('}');
      this.indentLevel--;
      this.emit('}');
    }
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.indentLevel--;
    this.emit('}');
    this.emit('');
    this.emit(`#Preview {`);
    this.indentLevel++;
    this.emit(`${cls}SpatialAudioView(manager: ${cls}SpatialAudioManager())`);
    this.indentLevel--;
    this.emit('}');

    return this.lines.join('\n');
  }
}

export function compileToIOS(
  composition: HoloComposition,
  options?: IOSCompilerOptions
): IOSCompileResult {
  const compiler = new IOSCompiler(options);
  return compiler.compile(composition, 'test-token');
}
