/**
 * @holoscript/core — perceptual color science (non-Riemannian) — public barrel.
 * See ./perceptualColor for the full honesty ledger (exact vs. approximated).
 */
export * from './perceptualColor';
