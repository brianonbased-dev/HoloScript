@trait {
  name: "@live_metric",
  category: "ui",
  description: "Real-time data display with adaptive styling (uses holoscript-net service).",
  props: {
    stream: @reactive_stream,
    format: string,
    adaptive_color: boolean = true,
    threshold_alert: number
  }
}
