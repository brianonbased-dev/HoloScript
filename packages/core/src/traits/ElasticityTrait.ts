import { createElasticityTraitHandler } from './elasticityCore';

/** Canonical VR trait: @elastic — coefficient-of-restitution collision response. */
export const elasticityHandler = createElasticityTraitHandler('elastic');
