composition "PipelineEditor" {
  @page { route: "/pipeline" }

  object "Shell" {
    @layout { flex: "column" }
    @theme { className: "min-h-screen bg-gray-950 text-white" }

    object "Main" {
      @layout { flex: "column" }
      @theme { className: "flex-1" }

      object "PipelineWorkbench" {
        @slot
      }
    }
  }
}
