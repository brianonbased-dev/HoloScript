# TetGen WASM Fallback Path

Fallback runtime location for TetGen binary:

- `/assets/tetgen.wasm`

Preferred path remains:

- `/wasm/tetgen.wasm`

`TetGenWasmMesher` now attempts both locations in order.
