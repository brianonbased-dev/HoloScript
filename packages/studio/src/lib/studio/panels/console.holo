@view({ id: "console", title: "Console", icon: "Terminal", category: "debug", placement: "right-rail", scope: "project", gate: "expert", surfaceClass: "lab", defaultOpen: false, exclusiveWith: [] })
composition "console" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
