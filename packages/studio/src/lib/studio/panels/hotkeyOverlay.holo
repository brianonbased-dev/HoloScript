@view({ id: "hotkeyOverlay", title: "Hotkey Overlay", icon: "Keyboard", category: "authoring", placement: "modal", scope: "global", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [] })
composition "hotkeyOverlay" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
