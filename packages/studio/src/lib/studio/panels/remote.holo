@view({ id: "remote", title: "Mobile Remote", icon: "Smartphone", category: "authoring", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "lab", defaultOpen: false, exclusiveWith: ["registry"] })
composition "remote" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
