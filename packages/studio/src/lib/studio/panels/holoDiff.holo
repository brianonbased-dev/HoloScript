@view({ id: "holoDiff", title: "HoloDiff", icon: "GitCompare", category: "authoring", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [], order: 65 })
composition "holoDiff" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
