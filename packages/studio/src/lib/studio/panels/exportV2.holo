@view({ id: "exportV2", title: "Export Pipeline", icon: "Package", category: "publishing", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: ["templateGallery","audio"] })
composition "exportV2" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
