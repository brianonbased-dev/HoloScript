@view({ id: "runtimeTier", title: "Runtime Tier", icon: "Gauge", category: "simulation", placement: "right-rail", scope: "project", gate: "expert", surfaceClass: "lab", defaultOpen: false, exclusiveWith: [] })
composition "runtimeTier" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
