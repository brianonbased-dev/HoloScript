@view({ id: "hotkey", title: "Hotkey Map", icon: "Keyboard", category: "authoring", placement: "modal", scope: "global", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [], order: 41 })
composition "hotkey" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
