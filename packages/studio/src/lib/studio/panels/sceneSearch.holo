@view({ id: "sceneSearch", title: "Scene Search", icon: "SearchCode", category: "authoring", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [] })
composition "sceneSearch" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
