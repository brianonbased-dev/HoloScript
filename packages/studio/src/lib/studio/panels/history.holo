@view({ id: "history", title: "History", icon: "History", category: "authoring", placement: "bottom-panel", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [] })
composition "history" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
