@view({ id: "physics", title: "Physics", icon: "Atom", category: "simulation", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [], order: 34 })
composition "physics" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
