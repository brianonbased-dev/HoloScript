@view({ id: "multiTransform", title: "Multi-Transform", icon: "Move3d", category: "authoring", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [] })
composition "multiTransform" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
