@view({ id: "share", title: "Share", icon: "Share2", category: "collaboration", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: ["aiMaterial"] })
composition "share" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
