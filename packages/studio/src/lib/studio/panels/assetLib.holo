@view({ id: "assetLib", title: "Asset Library", icon: "Library", category: "assets", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: ["multiplayer","debugger","snapshots"] })
composition "assetLib" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
