@view({ id: "eventMonitor", title: "Event Monitor", icon: "Activity", category: "debug", placement: "top-overlay", scope: "global", gate: "always", surfaceClass: "lab", defaultOpen: false, exclusiveWith: [] })
composition "eventMonitor" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
