@view({ id: "undoHistory", title: "Undo History", icon: "Clock", category: "authoring", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [] })
composition "undoHistory" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
