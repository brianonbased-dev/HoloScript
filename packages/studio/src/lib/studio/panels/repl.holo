@view({ id: "repl", title: "REPL", icon: "Terminal", category: "authoring", placement: "right-rail", scope: "project", gate: "expert", surfaceClass: "lab", defaultOpen: false, exclusiveWith: ["versions"] })
composition "repl" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
