@view({ id: "export", title: "Export", icon: "Download", category: "publishing", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: ["generator","profiler"], order: 15 })
composition "export" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
