@view({ id: "nodeGraph", title: "Node Graph", icon: "Network", category: "authoring", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: ["keyframes"] })
composition "nodeGraph" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
