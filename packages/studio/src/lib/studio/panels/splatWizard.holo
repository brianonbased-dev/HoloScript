@view({ id: "splatWizard", title: "Splat Wizard", icon: "PaintBucket", category: "assets", placement: "modal", scope: "project", gate: "always", surfaceClass: "lab", defaultOpen: false, exclusiveWith: [] })
composition "splatWizard" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
