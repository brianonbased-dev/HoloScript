@view({ id: "palette", title: "Command Palette", icon: "Search", category: "authoring", placement: "modal", scope: "global", gate: "always", surfaceClass: "core-workbench", defaultOpen: false, exclusiveWith: [] })
composition "palette" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
