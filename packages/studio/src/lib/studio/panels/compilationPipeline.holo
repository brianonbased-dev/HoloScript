@view({ id: "compilationPipeline", title: "Compilation Pipeline", icon: "Network", category: "authoring", placement: "right-rail", scope: "project", gate: "always", surfaceClass: "lab", defaultOpen: false, exclusiveWith: [], order: 71 })
composition "compilationPipeline" {
  // @slot widget mount pending — registry metadata migrated, render-wiring is the follow-on step
}
