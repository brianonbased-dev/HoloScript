import React, { useState } from 'react';

// @generated by HoloScript Native2DCompiler — DO NOT EDIT
export function TwinProofComponent() {
  const [altitude, setAltitude] = useState(20);

  return (
    <div
      className="holoscript-2d-root w-full h-full"
      data-holo-view-contract="affbd601cf5431ce12aeab4c1ed9560ae476b7fcc1953e3b98422b1bdd772710"
    >
      <div
        style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}
        className="p-3 h-full overflow-y-auto text-xs text-studio-text"
      >
        <h3 className="text-xl font-semibold text-sm font-semibold text-studio-text">
          {`Twin proof — is this screen telling the truth?`}
        </h3>
        <span className="text-sm text-[10px] text-studio-muted">
          {`This panel shows the altitude it was built with. The real craft is tracked elsewhere. Change the real altitude and the verdict below catches this screen showing a number the craft does not have.`}
        </span>
        <div style={{ display: 'flex', flexDirection: 'row', alignItems: 'center', gap: '8px' }}>
          <span className="text-sm text-[10px] text-studio-muted">{`Altitude on screen (m)`}</span>
          <span
            data-holo-projects="altitude"
            className="text-sm text-[10px] font-semibold text-studio-text"
          >
            {altitude ?? '0'}
          </span>
        </div>
        <div
          data-proof-claim={'altitude < 100'}
          data-proof-label="Craft below ceiling"
          data-proof-independence="verified"
          data-proof-anchors={'[{"input":"altitude","node":"altitude","entity":"craft-1"}]'}
          data-proof-faults={
            '[{"overrides":{"altitude":900},"because":"a craft far above its ceiling must never read as below it"}]'
          }
          className="flex flex-col gap-2"
        >
          <div
            data-proof-state={altitude < 100 ? 'pass' : 'falsified'}
            className={`rounded-md p-2 text-xs font-semibold ${altitude < 100 ? 'bg-studio-success/10 text-studio-success' : 'bg-studio-error/10 text-studio-error'}`}
          >
            {altitude < 100 ? '✓ Craft below ceiling holds' : '✗ Craft below ceiling FALSIFIED'}
          </div>
          <span className="text-[10px] text-studio-muted">
            {
              'Broken on purpose 1 way when this was built — the check caught it. Press one to watch it fail.'
            }
          </span>
          <span className="text-[10px] text-studio-muted">
            {
              'Every number behind this is also checked against the real thing (craft-1), so this verdict is not just the screen agreeing with itself.'
            }
          </span>
          <div className="flex flex-wrap gap-1">
            <button
              type="button"
              onClick={() => {
                setAltitude(900);
              }}
              className="rounded border border-studio-border bg-studio-panel px-2 py-1 text-[10px] text-studio-text hover:border-studio-error"
            >
              {'Break it: a craft far above its ceiling must never read as below it'}
            </button>
            <button
              type="button"
              onClick={() => {
                setAltitude(20);
              }}
              className="rounded border border-studio-border bg-studio-panel px-2 py-1 text-[10px] text-studio-muted hover:border-studio-accent"
            >
              {'Put it back'}
            </button>
          </div>
        </div>
      </div>
    </div>
  );
}

export default TwinProofComponent;

export const holoViewContract = {
  version: 'holo-view-contract-v1',
  projections: [{ element: 'AltitudeValue', node: 'altitude', entity: 'craft-1', identity: true }],
  stateRoots: ['altitude'],
  liveProofs: [
    {
      claim: 'altitude < 100',
      label: 'Craft below ceiling',
      independence: 'verified',
      inputs: ['altitude'],
      anchors: [{ input: 'altitude', node: 'altitude', entity: 'craft-1' }],
      unanchored: [],
    },
  ],
  contractHash: 'affbd601cf5431ce12aeab4c1ed9560ae476b7fcc1953e3b98422b1bdd772710',
} as const;
