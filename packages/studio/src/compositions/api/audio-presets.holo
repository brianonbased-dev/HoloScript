// HoloScript source for GET /api/audio-presets
// Compiled to: packages/studio/src/app/api/audio-presets/route.ts
// Compiler:    NextJSAPICompiler (compileToNextJSAPI)

composition "AudioPresetsAPI" {
  @http {
    method: "GET"
    description: "Returns audio visualizer waveform and frequency band presets"
  }

  route: "/api/audio-presets"
  category: "presets"
  version: "1.0.0"
  stable: true

  object "Response" {
    type: "AudioPreset[]"
    statusCode: 200
  }
}
