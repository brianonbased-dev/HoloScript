# Regenerable test fixture — rebuilt by vault-ops.cjs buildSandbox() on every verifier run.
vault-sandbox/
# Gate 4 fixture + persisted NPC memory — regenerated by gate-4-causal-memory-verify.mjs on every run.
vault-sandbox-g4/
# Gate 8 fixture — regenerated by gate-8-mesh-economy-verify.mjs on every run.
vault-sandbox-g8/
# Transient review screenshot (not the verified-render proof).
current-look.png
vault-sandbox-g19/
vault-sandbox-g20/
# Gate 27 fixture — regenerated by gate-27-safe-mutation-verify.mjs; cleaned on exit.
vault-sandbox-g27/
