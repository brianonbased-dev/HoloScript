// @generated by HoloScript HSIIRKotlinStateMachineEmitter — DO NOT EDIT
// HSI-IR digest: sha256:00767cccfc3d02b0f9bd0c11fce7640ecbf18e35a5115b092f598be9d301b7cb

package net.holoscript.qrscanner

internal class ScannerLifecycleMachine {
  enum class State(val sourceName: String) {
    ACTION("action"),
    CLASSIFIED("classified"),
    CONSENT("consent"),
    DECODED("decoded"),
    IDLE("idle")
  }

  data class Transition(val id: String, val from: State, val to: State)

  var state: State = State.IDLE
    private set

  private var _inputDecodeReady: Boolean = false

  private var _inputClassificationReady: Boolean = false

  private var _inputInvalidPayload: Boolean = false

  private var _inputConsentRequested: Boolean = false

  private var _inputConsentGranted: Boolean = false

  private var _inputActionReady: Boolean = false

  private var _inputUserActionRequested: Boolean = false

  private var _inputDismissed: Boolean = false

  private var _inputActionComplete: Boolean = false

  fun fireDecodeReady(): Transition? {
    _inputDecodeReady = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireClassificationReady(): Transition? {
    _inputClassificationReady = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireInvalidPayload(): Transition? {
    _inputInvalidPayload = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireConsentRequested(): Transition? {
    _inputConsentRequested = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireConsentGranted(): Transition? {
    _inputConsentGranted = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireActionReady(): Transition? {
    _inputActionReady = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireUserActionRequested(): Transition? {
    _inputUserActionRequested = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireDismissed(): Transition? {
    _inputDismissed = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireActionComplete(): Transition? {
    _inputActionComplete = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  private fun clearTriggers() {
    _inputDecodeReady = false
    _inputClassificationReady = false
    _inputInvalidPayload = false
    _inputConsentRequested = false
    _inputConsentGranted = false
    _inputActionReady = false
    _inputUserActionRequested = false
    _inputDismissed = false
    _inputActionComplete = false
  }

  private fun evaluateTransition(): Transition? {
    val from = state
    val transition = when (state) {
      State.ACTION -> when {
        _inputActionComplete -> Transition("transition:ScannerLifecycle.action->idle#0", from, State.IDLE)
        else -> null
      }
      State.CLASSIFIED -> when {
        _inputConsentRequested -> Transition("transition:ScannerLifecycle.classified->consent#0", from, State.CONSENT)
        _inputActionReady -> Transition("transition:ScannerLifecycle.classified->action#0", from, State.ACTION)
        else -> null
      }
      State.CONSENT -> when {
        _inputConsentGranted -> Transition("transition:ScannerLifecycle.consent->action#0", from, State.ACTION)
        _inputDismissed -> Transition("transition:ScannerLifecycle.consent->idle#0", from, State.IDLE)
        else -> null
      }
      State.DECODED -> when {
        _inputClassificationReady -> Transition("transition:ScannerLifecycle.decoded->classified#0", from, State.CLASSIFIED)
        _inputInvalidPayload -> Transition("transition:ScannerLifecycle.decoded->idle#0", from, State.IDLE)
        else -> null
      }
      State.IDLE -> when {
        _inputDecodeReady -> Transition("transition:ScannerLifecycle.idle->decoded#0", from, State.DECODED)
        _inputUserActionRequested -> Transition("transition:ScannerLifecycle.idle->action#0", from, State.ACTION)
        else -> null
      }
    }
    if (transition != null) state = transition.to
    return transition
  }
}
