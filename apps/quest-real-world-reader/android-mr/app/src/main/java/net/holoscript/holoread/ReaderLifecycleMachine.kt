// @generated by HoloScript HSIIRKotlinStateMachineEmitter — DO NOT EDIT
// HSI-IR digest: sha256:1d4705652516ce5c768314ccd3e90b50ae1e3dda3581258e46ef7d51ad78ebee

package net.holoscript.holoread

internal class ReaderLifecycleMachine {
  enum class State(val sourceName: String) {
    CAPTURING("capturing"),
    ERROR("error"),
    EXPLAINING("explaining"),
    IDLE("idle"),
    RECOGNIZING("recognizing"),
    RESULT("result"),
    TRANSLATING("translating")
  }

  data class Transition(val id: String, val from: State, val to: State)

  var state: State = State.IDLE
    private set

  private var _inputScanRequested: Boolean = false

  private var _inputCaptureReady: Boolean = false

  private var _inputRecognitionSucceeded: Boolean = false

  private var _inputRecognitionFailed: Boolean = false

  private var _inputContextRequested: Boolean = false

  private var _inputContextReady: Boolean = false

  private var _inputContextFailed: Boolean = false

  private var _inputTranslationRequested: Boolean = false

  private var _inputTranslationSucceeded: Boolean = false

  private var _inputTranslationFailed: Boolean = false

  private var _inputDismissed: Boolean = false

  fun fireScanRequested(): Transition? {
    _inputScanRequested = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireCaptureReady(): Transition? {
    _inputCaptureReady = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireRecognitionSucceeded(): Transition? {
    _inputRecognitionSucceeded = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireRecognitionFailed(): Transition? {
    _inputRecognitionFailed = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireContextRequested(): Transition? {
    _inputContextRequested = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireContextReady(): Transition? {
    _inputContextReady = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireContextFailed(): Transition? {
    _inputContextFailed = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireTranslationRequested(): Transition? {
    _inputTranslationRequested = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireTranslationSucceeded(): Transition? {
    _inputTranslationSucceeded = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireTranslationFailed(): Transition? {
    _inputTranslationFailed = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  fun fireDismissed(): Transition? {
    _inputDismissed = true
    return try {
      evaluateTransition()
    } finally {
      clearTriggers()
    }
  }

  private fun clearTriggers() {
    _inputScanRequested = false
    _inputCaptureReady = false
    _inputRecognitionSucceeded = false
    _inputRecognitionFailed = false
    _inputContextRequested = false
    _inputContextReady = false
    _inputContextFailed = false
    _inputTranslationRequested = false
    _inputTranslationSucceeded = false
    _inputTranslationFailed = false
    _inputDismissed = false
  }

  private fun evaluateTransition(): Transition? {
    val from = state
    val transition = when (state) {
      State.CAPTURING -> when {
        _inputCaptureReady -> Transition("transition:ReaderLifecycle.capturing->recognizing#0", from, State.RECOGNIZING)
        _inputRecognitionFailed -> Transition("transition:ReaderLifecycle.capturing->error#0", from, State.ERROR)
        else -> null
      }
      State.ERROR -> when {
        _inputScanRequested -> Transition("transition:ReaderLifecycle.error->capturing#0", from, State.CAPTURING)
        _inputDismissed -> Transition("transition:ReaderLifecycle.error->idle#0", from, State.IDLE)
        else -> null
      }
      State.EXPLAINING -> when {
        _inputContextReady -> Transition("transition:ReaderLifecycle.explaining->result#0", from, State.RESULT)
        _inputContextFailed -> Transition("transition:ReaderLifecycle.explaining->result#1", from, State.RESULT)
        else -> null
      }
      State.IDLE -> when {
        _inputScanRequested -> Transition("transition:ReaderLifecycle.idle->capturing#0", from, State.CAPTURING)
        else -> null
      }
      State.RECOGNIZING -> when {
        _inputRecognitionSucceeded -> Transition("transition:ReaderLifecycle.recognizing->result#0", from, State.RESULT)
        _inputRecognitionFailed -> Transition("transition:ReaderLifecycle.recognizing->error#0", from, State.ERROR)
        else -> null
      }
      State.RESULT -> when {
        _inputContextRequested -> Transition("transition:ReaderLifecycle.result->explaining#0", from, State.EXPLAINING)
        _inputTranslationRequested -> Transition("transition:ReaderLifecycle.result->translating#0", from, State.TRANSLATING)
        _inputScanRequested -> Transition("transition:ReaderLifecycle.result->capturing#0", from, State.CAPTURING)
        _inputDismissed -> Transition("transition:ReaderLifecycle.result->idle#0", from, State.IDLE)
        else -> null
      }
      State.TRANSLATING -> when {
        _inputTranslationSucceeded -> Transition("transition:ReaderLifecycle.translating->result#0", from, State.RESULT)
        _inputTranslationFailed -> Transition("transition:ReaderLifecycle.translating->result#1", from, State.RESULT)
        else -> null
      }
    }
    if (transition != null) state = transition.to
    return transition
  }
}
