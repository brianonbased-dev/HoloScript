// Auto-generated by HoloScript AndroidCompiler
// Source: composition "AndroidReference"
// Do not edit manually — regenerate from .holo (apps/android-reference/generate-native.mts)
//
// SceneView (Apache 2.0) Compose-native AR. Declarative ARScene { } with one node per
// HoloScript object — no ArFragment, no R.layout, no NodeFactory/ViewModel.

package net.holoscript.android

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import io.github.sceneview.ar.ARScene
import io.github.sceneview.ar.rememberARCameraNode
import io.github.sceneview.math.Position
import io.github.sceneview.math.Size
import io.github.sceneview.node.CubeNode
import io.github.sceneview.node.SphereNode
import io.github.sceneview.rememberEngine
import io.github.sceneview.rememberMaterialLoader

class GeneratedARSceneActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            val engine = rememberEngine()
            val materialLoader = rememberMaterialLoader(engine)
            val cameraNode = rememberARCameraNode(engine)
            
            ARScene(
                modifier = Modifier.fillMaxSize(),
                engine = engine,
                cameraNode = cameraNode,
                materialLoader = materialLoader,
                planeRenderer = true,
            ) {
                // anchor_cube — geometry: cube
                CubeNode(
                    size = Size(0.2f),
                    materialInstance = materialLoader.createColorInstance(Color(0xFF4488FF)),
                    position = Position(x = 0f, y = 0f, z = -1f),
                )
                
                // target_sphere — geometry: sphere
                SphereNode(
                    radius = 0.1f,
                    materialInstance = materialLoader.createColorInstance(Color(0xFFFF8844)),
                    position = Position(x = 0.3f, y = 0f, z = -1f),
                )
            }
        }
    }
}
