{
  "$schema": "https://modelcontextprotocol.io/schemas/mcp-config.json",
  "mcpServers": {
    "holoscript-mcp": {
      "type": "http",
      "url": "https://mcp.holoscript.net/mcp",
      "headers": {
        "Authorization": "Bearer ${MCP_API_KEY}"
      },
      "description": "HoloScript MCP — 156 tools: parse, compile, render, codebase, team board, knowledge"
    },
    "mcp-orchestrator": {
      "type": "http",
      "url": "https://mcp-orchestrator-production-45f9.up.railway.app/mcp",
      "headers": {
        "Authorization": "Bearer ${MCP_API_KEY}"
      },
      "description": "MCP Orchestrator — tool discovery, knowledge federation (2K+ entries)"
    },
    "holoscript-local": {
      "command": "node",
      "args": ["${workspaceFolder}/packages/mcp-server/start.js"],
      "description": "HoloScript MCP — local dev (fastest, use for development). Falls back to holoscript-mcp (production) if local fails to start."
    }
  }
}
